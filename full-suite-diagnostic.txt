=== CURRENT HEAD ===
exit_code=1
--- lines 2634-2677 ---
  ...
# Subtest: M4 CV review shows a neutral empty state when AI returns no changed block
ok 439 - M4 CV review shows a neutral empty state when AI returns no changed block
  ---
  duration_ms: 0.904677
  type: 'test'
  ...
# Subtest: Source CV upload entry points keep the current four-step Search Profile flow
not ok 440 - Source CV upload entry points keep the current four-step Search Profile flow
  ---
  duration_ms: 1.681754
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:117:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Upload CV<\/button>/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'\n" +
      "import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
--- lines 2682-2783 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      "  const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw='... 51895 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'
    import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
--- lines 2788-2877 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 2917-3001 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 3032-3079 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 3173-3366 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          const syncResult=await attemptNightFlightProfileSync({sync:requestNightFlightProfileSync,searchProfile:saved,cv:cvData?{text:cvData.cvText,sourceVersion:cvData.sourceVersion}:null})
          setNightFlightSyncWarning(syncResult.stale?syncResult.error:'')
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 3371-3444 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        {nightFlightSyncWarning&&<div className="warningBox"><b>Night Flight backend is not synced</b><span>{nightFlightSyncWarning}</span></div>}
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
--- lines 3629-3687 ---
  ...
# Subtest: Version 2 Step 1 refuses a truncated legacy preview instead of tailoring a partial Summary
ok 448 - Version 2 Step 1 refuses a truncated legacy preview instead of tailoring a partial Summary
  ---
  duration_ms: 0.157917
  type: 'test'
  ...
# Subtest: page promotes the saved Union Search Plan to LIVE while preserving legacy fallback payload
not ok 449 - page promotes the saved Union Search Plan to LIVE while preserving legacy fallback payload
  ---
  duration_ms: 8.636807
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-shadow-wiring.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /setJobs\(Array\.isArray\(data\.jobs\)\?data\.jobs:\[\]\)/. Input:
    
    'async function search(){\n' +
      '  if(!resumeLoaded){\n' +
      "    setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      '  const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]\n' +
      '  const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]\n' +
      '  if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){\n' +
      "    setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      "  setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0\n' +
      '  try{\n' +
      '    const tasks=[]\n' +
      '\n' +
      "    if(selectedSources.includes('linkedin')) tasks.push((async()=>{\n" +
      '      let res\n' +
      '      if(hasProfilePlan){\n' +
      '        const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint\n' +
      '        const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})\n' +
      "        res=await fetch('/api/linkedin-profile-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({\n' +
      '            freshnessDays,\n' +
      '            unionSearchPlan:profile.unionSearchPlan,\n' +
      '            exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '            previousCandidates:poolSnapshot.candidates,\n' +
      '            previousVerifiedJobs:poolSnapshot.verifiedJobs,\n' +
      '          }),\n' +
      '        })\n' +
      '      }else{\n' +
      "        res=await fetch('/api/linkedin-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),\n' +
      '        })\n' +
      '      }\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'LinkedIn search failed')\n" +
--- lines 3781-3843 ---
      '      masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),\n' +
      '      fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),\n' +
      '      returned:mergedJobs.length,\n' +
      '    }\n' +
      '    const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))\n' +
      "    const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')\n" +
      '    setState({\n' +
      '      loading:false,\n' +
      "      error:'',\n" +
      "      coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},\n" +
      '      stats,\n' +
      '      fetchedAt:new Date().toISOString(),\n' +
      '      audit,\n' +
      '    })\n' +
      "  }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }\n" +
      '}'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 3937-4013 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-shadow-wiring.test.mjs:18:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: parse-cv endpoint accepts resume uploads instead of being retired
ok 450 - parse-cv endpoint accepts resume uploads instead of being retired
  ---
  duration_ms: 1.080426
  type: 'test'
  ...
# Subtest: Feature 2 UI persists the complete active Source CV and supports legacy migration
ok 451 - Feature 2 UI persists the complete active Source CV and supports legacy migration
  ---
  duration_ms: 0.303198
  type: 'test'
  ...
# Subtest: Feature 2 retains the extracted Professional Summary separately from the short preview
ok 452 - Feature 2 retains the extracted Professional Summary separately from the short preview
  ---
  duration_ms: 0.16487
  type: 'test'
  ...
# Subtest: right panel uses dedicated Expertise Match analysis and keeps the agreed vacancy header
not ok 453 - right panel uses dedicated Expertise Match analysis and keeps the agreed vacancy header
  ---
  duration_ms: 3.093412
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/right-panel-expertise-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Source: LinkedIn/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'\n" +
      "import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
--- lines 4018-4119 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      "  const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw='... 51895 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'
    import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
--- lines 4124-4213 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 4253-4337 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 4368-4415 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 4509-4702 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          const syncResult=await attemptNightFlightProfileSync({sync:requestNightFlightProfileSync,searchProfile:saved,cv:cvData?{text:cvData.cvText,sourceVersion:cvData.sourceVersion}:null})
          setNightFlightSyncWarning(syncResult.stale?syncResult.error:'')
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 4707-4780 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        {nightFlightSyncWarning&&<div className="warningBox"><b>Night Flight backend is not synced</b><span>{nightFlightSyncWarning}</span></div>}
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
--- lines 4933-4976 ---
  ...
# Subtest: job conditions never alter Expertise Match
ok 456 - job conditions never alter Expertise Match
  ---
  duration_ms: 22.072934
  type: 'test'
  ...
# Subtest: page wires API audit rows into a closed-by-default Search Audit component
not ok 457 - page wires API audit rows into a closed-by-default Search Audit component
  ---
  duration_ms: 8.109178
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-audit-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /audit:Array\.isArray\(data\.audit\)\?data\.audit:\[\]/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'\n" +
      "import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
--- lines 4981-5082 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      "  const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw='... 51895 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'
    import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
--- lines 5087-5176 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 5216-5300 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 5331-5378 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 5472-5665 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          const syncResult=await attemptNightFlightProfileSync({sync:requestNightFlightProfileSync,searchProfile:saved,cv:cvData?{text:cvData.cvText,sourceVersion:cvData.sourceVersion}:null})
          setNightFlightSyncWarning(syncResult.stale?syncResult.error:'')
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 5670-5743 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        {nightFlightSyncWarning&&<div className="warningBox"><b>Night Flight backend is not synced</b><span>{nightFlightSyncWarning}</span></div>}
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
--- lines 5948-5991 ---
  ...
# Subtest: blank exclusions use zero AI calls
ok 469 - blank exclusions use zero AI calls
  ---
  duration_ms: 0.262623
  type: 'test'
  ...
# Subtest: custom exclusions are compiled only on Save profile and then passed to profile-driven LIVE Search
not ok 470 - custom exclusions are compiled only on Save profile and then passed to profile-driven LIVE Search
  ---
  duration_ms: 3.404064
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-exclusions-save-ui.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JSON\.stringify\(\{freshnessDays,unionSearchPlan:profile\.unionSearchPlan,exclusionRules:Array\.isArray\(profile\.exclusionRules\)\?profile\.exclusionRules:\[\]\}\)/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'\n" +
      "import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
--- lines 5996-6097 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      "  const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw='... 51895 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'
    import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
--- lines 6102-6191 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 6231-6315 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 6346-6393 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 6487-6680 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          const syncResult=await attemptNightFlightProfileSync({sync:requestNightFlightProfileSync,searchProfile:saved,cv:cvData?{text:cvData.cvText,sourceVersion:cvData.sourceVersion}:null})
          setNightFlightSyncWarning(syncResult.stale?syncResult.error:'')
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 6685-6758 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        {nightFlightSyncWarning&&<div className="warningBox"><b>Night Flight backend is not synced</b><span>{nightFlightSyncWarning}</span></div>}
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
--- lines 6953-6996 ---
  ...
# Subtest: library fingerprint is stable by slot and changes only with CV membership/sourceVersion or builder version
ok 480 - library fingerprint is stable by slot and changes only with CV membership/sourceVersion or builder version
  ---
  duration_ms: 0.465974
  type: 'test'
  ...
# Subtest: Search Profile wizard has five steps and no salary screen or salary scoring label
not ok 481 - Search Profile wizard has five steps and no salary screen or salary scoring label
  ---
  duration_ms: 3.558634
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-no-salary-ui.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /profileStep\/5\*100/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'\n" +
      "import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
--- lines 7001-7102 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      "  const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw='... 51895 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'
    import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
--- lines 7107-7196 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 7236-7320 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 7351-7398 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 7492-7685 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          const syncResult=await attemptNightFlightProfileSync({sync:requestNightFlightProfileSync,searchProfile:saved,cv:cvData?{text:cvData.cvText,sourceVersion:cvData.sourceVersion}:null})
          setNightFlightSyncWarning(syncResult.stale?syncResult.error:'')
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 7690-7763 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        {nightFlightSyncWarning&&<div className="warningBox"><b>Night Flight backend is not synced</b><span>{nightFlightSyncWarning}</span></div>}
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
--- lines 7928-7971 ---
  ...
# Subtest: Search Profile step 2 combines all ready CVs while legacy Search keeps CV 1
ok 486 - Search Profile step 2 combines all ready CVs while legacy Search keeps CV 1
  ---
  duration_ms: 2.806723
  type: 'test'
  ...
# Subtest: Search Profile Step 3 separates location from work model
not ok 487 - Search Profile Step 3 separates location from work model
  ---
  duration_ms: 3.29017
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-step3-ui-contract.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /<SearchProfileLocationStep/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'\n" +
      "import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
--- lines 7976-8077 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      "  const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw='... 51895 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'
    import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
--- lines 8082-8171 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 8211-8295 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 8326-8373 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 8467-8660 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          const syncResult=await attemptNightFlightProfileSync({sync:requestNightFlightProfileSync,searchProfile:saved,cv:cvData?{text:cvData.cvText,sourceVersion:cvData.sourceVersion}:null})
          setNightFlightSyncWarning(syncResult.stale?syncResult.error:'')
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 8665-8738 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        {nightFlightSyncWarning&&<div className="warningBox"><b>Night Flight backend is not synced</b><span>{nightFlightSyncWarning}</span></div>}
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
--- lines 9203-9261 ---
  ...
# Subtest: page wires the edited draft into a saved Union Search Plan preview
ok 542 - page wires the edited draft into a saved Union Search Plan preview
  ---
  duration_ms: 5.547153
  type: 'test'
  ...
# Subtest: saved Union Search Plan drives profile LIVE while legacy fallback stays isolated
not ok 543 - saved Union Search Plan drives profile LIVE while legacy fallback stays isolated
  ---
  duration_ms: 4.74976
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/union-search-plan-wiring.test.mjs:22:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JSON\.stringify\(\{freshnessDays,unionSearchPlan:profile\.unionSearchPlan,exclusionRules:Array\.isArray\(profile\.exclusionRules\)\?profile\.exclusionRules:\[\]\}\)/. Input:
    
    'async function search(){\n' +
      '  if(!resumeLoaded){\n' +
      "    setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      '  const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]\n' +
      '  const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]\n' +
      '  if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){\n' +
      "    setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      "  setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0\n' +
      '  try{\n' +
      '    const tasks=[]\n' +
      '\n' +
      "    if(selectedSources.includes('linkedin')) tasks.push((async()=>{\n" +
      '      let res\n' +
      '      if(hasProfilePlan){\n' +
      '        const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint\n' +
      '        const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})\n' +
      "        res=await fetch('/api/linkedin-profile-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({\n' +
      '            freshnessDays,\n' +
      '            unionSearchPlan:profile.unionSearchPlan,\n' +
      '            exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '            previousCandidates:poolSnapshot.candidates,\n' +
      '            previousVerifiedJobs:poolSnapshot.verifiedJobs,\n' +
      '          }),\n' +
      '        })\n' +
      '      }else{\n' +
      "        res=await fetch('/api/linkedin-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),\n' +
      '        })\n' +
      '      }\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'LinkedIn search failed')\n" +
--- lines 9355-9417 ---
      '      masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),\n' +
      '      fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),\n' +
      '      returned:mergedJobs.length,\n' +
      '    }\n' +
      '    const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))\n' +
      "    const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')\n" +
      '    setState({\n' +
      '      loading:false,\n' +
      "      error:'',\n" +
      "      coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},\n" +
      '      stats,\n' +
      '      fetchedAt:new Date().toISOString(),\n' +
      '      audit,\n' +
      '    })\n' +
      "  }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }\n" +
      '}'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 9511-9554 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/union-search-plan-wiring.test.mjs:30:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: compiles edited roles in primary-then-adjacent order and primary wins duplicates
ok 544 - compiles edited roles in primary-then-adjacent order and primary wins duplicates
  ---
  duration_ms: 3.59404
  type: 'test'
  ...
# Subtest: removed raw CV-proposed roles do not survive the edited plan
ok 545 - removed raw CV-proposed roles do not survive the edited plan
  ---
  duration_ms: 2.13825
  type: 'test'
  ...
# Subtest: manual roles are preserved without invented CV provenance
ok 546 - manual roles are preserved without invented CV provenance
  ---
  duration_ms: 0.449102
  type: 'test'
  ...
# Subtest: CV-supported roles preserve all supporting CV ids, slots and raw support kinds
ok 547 - CV-supported roles preserve all supporting CV ids, slots and raw support kinds
  ---
  duration_ms: 0.451055
--- lines 9652-9695 ---
  ...
# Subtest: CV Update Review keeps a focused ORIGINAL UPDATED review without the obsolete Truth Guard panel
ok 564 - CV Update Review keeps a focused ORIGINAL UPDATED review without the obsolete Truth Guard panel
  ---
  duration_ms: 0.434525
  type: 'test'
  ...
# Subtest: review cards show Original, Updated, Why changed and independent decisions
not ok 565 - review cards show Original, Updated, Why changed and independent decisions
  ---
  duration_ms: 2.460454
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:24:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /change\.updated/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'\n" +
      "import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
--- lines 9700-9801 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      "  const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw='... 51895 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {requestNightFlightProfileSync} from './lib/night-flight-profile-client.js'
    import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
--- lines 9806-9895 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [nightFlightSyncWarning,setNightFlightSyncWarning]=useState('')
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 9935-10019 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 10050-10097 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 10191-10384 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          const syncResult=await attemptNightFlightProfileSync({sync:requestNightFlightProfileSync,searchProfile:saved,cv:cvData?{text:cvData.cvText,sourceVersion:cvData.sourceVersion}:null})
          setNightFlightSyncWarning(syncResult.stale?syncResult.error:'')
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 10389-10462 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        {nightFlightSyncWarning&&<div className="warningBox"><b>Night Flight backend is not synced</b><span>{nightFlightSyncWarning}</span></div>}
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}

=== BASELINE 57e8ac08 ===
exit_code=1
--- lines 588-631 ---
  ...
# Subtest: the mistaken floating company discovery drawer is absent
ok 98 - the mistaken floating company discovery drawer is absent
  ---
  duration_ms: 0.205325
  type: 'test'
  ...
# Subtest: Step 5 labels CV1 explicitly as the Primary Search CV
not ok 99 - Step 5 labels CV1 explicitly as the Primary Search CV
  ---
  duration_ms: 2.145728
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/cosmetic-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /<span>Primary Search CV<\/span>/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 634-737 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 740-828 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 868-952 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 983-1030 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 1124-1315 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 1320-1393 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 1706-1749 ---
  ...
# Subtest: Accept and Keep handlers mutate decision state only, not CV Library or Source CV
ok 129 - Accept and Keep handlers mutate decision state only, not CV Library or Source CV
  ---
  duration_ms: 0.322576
  type: 'test'
  ...
# Subtest: review UI renders the three-block contract and WHY CHANGED from direct AI review blocks
not ok 130 - review UI renders the three-block contract and WHY CHANGED from direct AI review blocks
  ---
  duration_ms: 1.387896
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/cv-adaptation-review-ui.contract.test.mjs:41:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /change\.updated/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 1752-1855 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 1858-1946 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 1986-2070 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 2101-2148 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 2242-2433 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 2438-2511 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 3210-3253 ---
  ...
# Subtest: Help opens in a separate tab from the authenticated app utility navigation
ok 224 - Help opens in a separate tab from the authenticated app utility navigation
  ---
  duration_ms: 5.238545
  type: 'test'
  ...
# Subtest: Help page covers the complete MVP user flow and current screenshot
not ok 225 - Help page covers the complete MVP user flow and current screenshot
  ---
  duration_ms: 6.496793
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/help-page.contract.test.mjs:17:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /\/help\/applypilot-live-results\.jpg/. Input:
    
    "import styles from './help.module.css'\n" +
      '\n' +
      'export const metadata={\n' +
      "  title:'Help · ApplyPilot',\n" +
      "  description:'ApplyPilot user manual and quick start guide'\n" +
      '}\n' +
      '\n' +
      'const quickSteps=[\n' +
      "  ['1','Load your CVs','Open Search Profile and add up to three ready CVs to the CV Library.'],\n" +
      "  ['2','Build the Search Profile','Review primary roles, adjacent roles, locations, work models and exclusions, then save the profile.'],\n" +
      "  ['3','Choose freshness','Select 1, 3, 7 or 14 days under Posted Within.'],\n" +
      "  ['4','Search LinkedIn','Run the search. ApplyPilot reads public LinkedIn vacancies and returns the worthwhile matches it found.'],\n" +
      "  ['5','Review the shortlist','Use Filters, status controls and the vacancy panel to decide which roles deserve attention.'],\n" +
      "  ['6','Compare CV and job','Run Expertise Match, then Find best CV when more than one CV is ready.'],\n" +
      "  ['7','Adapt and export','Run CV Adaptation, review every suggested change, pass Truth Guard and download the tailored DOCX.']\n" +
      ']\n' +
      '\n' +
      'const sections=[\n' +
      '  {\n' +
      "    id:'search-profile',\n" +
      "    kicker:'SETUP',\n" +
      "    title:'Search Profile',\n" +
      "    body:'The Search Profile tells ApplyPilot what to search for. It is the source of truth for role directions and job-search preferences.',\n" +
      '    bullets:[\n' +
      "      'Primary roles are your main search directions.',\n" +
      "      'Adjacent roles expand discovery without replacing the primary roles.',\n" +
      "      'Locations and work models define where you are willing to work.',\n" +
--- lines 3400-3438 ---
      '    <section id="troubleshooting" className={styles.section}>\n' +
      '      <div className={styles.sectionHead}><p className={styles.eyebrow}>WHEN SOMETHING LOOKS WRONG</p><h2>Troubleshooting</h2></div>\n' +
      '      <div className={styles.troubleshooting}>\n' +
      '        <article><h3>LIVE sends me to Sign in</h3><p>That is expected when there is no valid production session. Request a new magic link and open it in the same browser.</p></article>\n' +
      '        <article><h3>TEST asks for a magic link</h3><p>Make sure you are using the TEST preview URL rather than the production LIVE URL.</p></article>\n' +
      '        <article><h3>No jobs appear</h3><p>Check that a Search Profile is saved, a CV is ready, the freshness window is appropriate and the current filters are not hiding all results.</p></article>\n' +
      '        <arti'... 744 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import styles from './help.module.css'
    
    export const metadata={
      title:'Help · ApplyPilot',
      description:'ApplyPilot user manual and quick start guide'
    }
    
    const quickSteps=[
      ['1','Load your CVs','Open Search Profile and add up to three ready CVs to the CV Library.'],
      ['2','Build the Search Profile','Review primary roles, adjacent roles, locations, work models and exclusions, then save the profile.'],
      ['3','Choose freshness','Select 1, 3, 7 or 14 days under Posted Within.'],
      ['4','Search LinkedIn','Run the search. ApplyPilot reads public LinkedIn vacancies and returns the worthwhile matches it found.'],
      ['5','Review the shortlist','Use Filters, status controls and the vacancy panel to decide which roles deserve attention.'],
      ['6','Compare CV and job','Run Expertise Match, then Find best CV when more than one CV is ready.'],
      ['7','Adapt and export','Run CV Adaptation, review every suggested change, pass Truth Guard and download the tailored DOCX.']
    ]
    
    const sections=[
      {
        id:'search-profile',
        kicker:'SETUP',
        title:'Search Profile',
        body:'The Search Profile tells ApplyPilot what to search for. It is the source of truth for role directions and job-search preferences.',
        bullets:[
          'Primary roles are your main search directions.',
          'Adjacent roles expand discovery without replacing the primary roles.',
          'Locations and work models define where you are willing to work.',
--- lines 3738-3796 ---
  ...
# Subtest: manual job statuses are local informational metadata with three explicit states
ok 248 - manual job statuses are local informational metadata with three explicit states
  ---
  duration_ms: 1.129249
  type: 'test'
  ...
# Subtest: manual job statuses never enter Search and only filter the post-search list
not ok 249 - manual job statuses never enter Search and only filter the post-search list
  ---
  duration_ms: 2.092698
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/job-list-ui-milestone.contract.test.mjs:26:1'
  failureType: 'testCodeFailure'
  error: |-
    The input was expected to not match the regular expression /jobStatuses|JOB_STATUS_FILTERS|selectedStatuses/. Input:
    
    'async function search(){\n' +
      '  if(!resumeLoaded){\n' +
      "    setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      '  const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]\n' +
      '  const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]\n' +
      '  if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){\n' +
      "    setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      "  setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0\n' +
      '  try{\n' +
      '    const tasks=[]\n' +
      '\n' +
      "    if(selectedSources.includes('linkedin')) tasks.push((async()=>{\n" +
      '      let res\n' +
      '      if(hasProfilePlan){\n' +
      '        const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint\n' +
      '        const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})\n' +
      "        res=await fetch('/api/linkedin-profile-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({\n' +
      '            freshnessDays,\n' +
      '            unionSearchPlan:profile.unionSearchPlan,\n' +
      '            exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '            previousCandidates:poolSnapshot.candidates,\n' +
      '            previousVerifiedJobs:poolSnapshot.verifiedJobs,\n' +
      '          }),\n' +
      '        })\n' +
      '      }else{\n' +
      "        res=await fetch('/api/linkedin-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),\n' +
      '        })\n' +
      '      }\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'LinkedIn search failed')\n" +
--- lines 3890-3952 ---
      '      masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),\n' +
      '      fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),\n' +
      '      returned:mergedJobs.length,\n' +
      '    }\n' +
      '    const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))\n' +
      "    const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')\n" +
      '    setState({\n' +
      '      loading:false,\n' +
      "      error:'',\n" +
      "      coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},\n" +
      '      stats,\n' +
      '      fetchedAt:new Date().toISOString(),\n' +
      '      audit,\n' +
      '    })\n' +
      "  }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }\n" +
      '}'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 4046-4089 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
  operator: 'doesNotMatch'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/job-list-ui-milestone.contract.test.mjs:31:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: Area, Employment type and Work model render directly under the vacancy header before Best CV
ok 250 - Area, Employment type and Work model render directly under the vacancy header before Best CV
  ---
  duration_ms: 0.260329
  type: 'test'
  ...
# Subtest: All filters is the first filter row and controls only Search Areas and Work Model
ok 251 - All filters is the first filter row and controls only Search Areas and Work Model
  ---
  duration_ms: 0.348594
  type: 'test'
  ...
# Subtest: Status is a third independent filter group and replaces Show ignored
ok 252 - Status is a third independent filter group and replaces Show ignored
  ---
  duration_ms: 0.299873
  type: 'test'
  ...
--- lines 4323-4372 ---
  ...
# Subtest: profile-driven search returns legacy-compatible result shape and full-JD statistics
ok 292 - profile-driven search returns legacy-compatible result shape and full-JD statistics
  ---
  duration_ms: 17.59459
  type: 'test'
  ...
# Subtest: 7-day profile discovery uses repeated deep LinkedIn pages instead of shadow start=0 only
not ok 293 - 7-day profile discovery uses repeated deep LinkedIn pages instead of shadow start=0 only
  ---
  duration_ms: 6.448395
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/linkedin-profile-search.test.mjs:82:1'
  failureType: 'testCodeFailure'
  error: |-
    The expression evaluated to a falsy value:
    
      assert.ok(searchStarts.includes(25))
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/linkedin-profile-search.test.mjs:92:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: role evaluation uses the full approved Search Profile, not only the discovery path
ok 294 - role evaluation uses the full approved Search Profile, not only the discovery path
  ---
  duration_ms: 1.842989
  type: 'test'
  ...
# Subtest: same verified job and Search Profile keep the same role decision and score across different discovery paths
ok 295 - same verified job and Search Profile keep the same role decision and score across different discovery paths
  ---
  duration_ms: 3.183306
  type: 'test'
  ...
# Subtest: each freshness view performs fresh LinkedIn discovery for its selected window
ok 296 - each freshness view performs fresh LinkedIn discovery for its selected window
  ---
  duration_ms: 4.571572
  type: 'test'
  ...
# Subtest: 1/3/7/14-day views are local subsets over the same discovered jobs
ok 297 - 1/3/7/14-day views are local subsets over the same discovered jobs
  ---
--- lines 4411-4460 ---
  ...
# Subtest: exact Technical Project Manager in a non-enterprise engineering context is demoted but kept
ok 304 - exact Technical Project Manager in a non-enterprise engineering context is demoted but kept
  ---
  duration_ms: 1.671638
  type: 'test'
  ...
# Subtest: exact Technical Project Manager with strong enterprise IT context remains High
not ok 305 - exact Technical Project Manager with strong enterprise IT context remains High
  ---
  duration_ms: 1.477724
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/linkedin-profile-search.test.mjs:391:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    
    0 !== 0.2
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 0.2
  actual: 0
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/linkedin-profile-search.test.mjs:404:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: specialist adjacent Implementation Manager remains visible but payroll context lowers top-score inflation
ok 306 - specialist adjacent Implementation Manager remains visible but payroll context lowers top-score inflation
  ---
  duration_ms: 2.158202
  type: 'test'
  ...
# Subtest: generic hands-on engineer titles are rejected before JD fetch
ok 307 - generic hands-on engineer titles are rejected before JD fetch
  ---
  duration_ms: 4.883788
  type: 'test'
  ...
# Subtest: director and stream-owner career tracks are rejected before JD fetch
ok 308 - director and stream-owner career tracks are rejected before JD fetch
  ---
  duration_ms: 0.594416
  type: 'test'
  ...
# Subtest: people-manager hybrid title is rejected even though it contains Project Manager
ok 309 - people-manager hybrid title is rejected even though it contains Project Manager
  ---
--- lines 4613-4720 ---
  ...
# Subtest: audit helper allow-list cannot copy CV or JD source payloads
ok 335 - audit helper allow-list cannot copy CV or JD source payloads
  ---
  duration_ms: 1.83752
  type: 'test'
  ...
# Subtest: creates a privacy-safe discovery audit record
not ok 336 - creates a privacy-safe discovery audit record
  ---
  duration_ms: 3.181923
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/linkedin-search-audit.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly deep-equal:
    + actual - expected
    
      {
        company: 'Example',
        decision: 'PENDING',
    +   discoveryProvenance: [],
        jobId: '123',
        reason: null,
        score: null,
        stage: 'DISCOVERED',
        title: 'Senior Project Manager'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
    jobId: '123'
    title: 'Senior Project Manager'
    company: 'Example'
    stage: 'DISCOVERED'
    decision: 'PENDING'
    reason: ~
    score: ~
  actual:
    jobId: '123'
    title: 'Senior Project Manager'
    company: 'Example'
    stage: 'DISCOVERED'
    decision: 'PENDING'
    reason: ~
    score: ~
    discoveryProvenance:
  operator: 'deepStrictEqual'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/linkedin-search-audit.test.mjs:7:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: updates one audit record without leaking arbitrary source fields
not ok 337 - updates one audit record without leaking arbitrary source fields
  ---
  duration_ms: 0.729951
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/linkedin-search-audit.test.mjs:13:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly deep-equal:
    + actual - expected
    
      [
        {
          company: 'Co',
          decision: 'REJECT',
    +     discoveryProvenance: [],
          jobId: '123',
          reason: 'Poor fit',
          score: 5.8,
          stage: 'BELOW_60',
          title: 'PM'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
    0:
      jobId: '123'
      title: 'PM'
      company: 'Co'
      stage: 'BELOW_60'
      decision: 'REJECT'
      reason: 'Poor fit'
      score: 5.8
  actual:
    0:
      jobId: '123'
      title: 'PM'
      company: 'Co'
      stage: 'BELOW_60'
      decision: 'REJECT'
      reason: 'Poor fit'
      score: 5.8
      discoveryProvenance:
  operator: 'deepStrictEqual'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/linkedin-search-audit.test.mjs:16:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
--- lines 5080-5227 ---
  ...
# Subtest: requestPreviousRoleOverview fails safely if the previous-role stage is rejected
ok 398 - requestPreviousRoleOverview fails safely if the previous-role stage is rejected
  ---
  duration_ms: 1.103632
  type: 'test'
  ...
# Subtest: writePreviousRoleOverview uses only evidence from the detected previous employment section
not ok 399 - writePreviousRoleOverview uses only evidence from the detected previous employment section
  ---
  duration_ms: 6.295779
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:54:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:56:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: writePreviousRoleOverview rejects evidence IDs from the latest employment role
not ok 400 - writePreviousRoleOverview rejects evidence IDs from the latest employment role
  ---
  duration_ms: 0.549591
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:86:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:88:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: writePreviousRoleOverview rejects numbers absent from previous-role evidence
not ok 401 - writePreviousRoleOverview rejects numbers absent from previous-role evidence
  ---
  duration_ms: 0.507283
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:98:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:100:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: writePreviousRoleOverview enforces the existing previous-role overview length window
not ok 402 - writePreviousRoleOverview enforces the existing previous-role overview length window
  ---
  duration_ms: 0.507593
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:110:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:112:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: writePreviousRoleOverview returns unavailable when the selected CV has no previous role
not ok 403 - writePreviousRoleOverview returns unavailable when the selected CV has no previous role
  ---
  duration_ms: 1.587991
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:119:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/previous-role-overview.test.mjs:121:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: LIVE Search uses the profile-driven endpoint when a saved Union Search Plan exists
ok 404 - LIVE Search uses the profile-driven endpoint when a saved Union Search Plan exists
  ---
  duration_ms: 2.373956
  type: 'test'
  ...
# Subtest: LIVE Search keeps the frozen legacy endpoint as fallback
ok 405 - LIVE Search keeps the frozen legacy endpoint as fallback
  ---
  duration_ms: 0.193834
  type: 'test'
  ...
# Subtest: profile-driven LIVE endpoint exists separately from the frozen legacy endpoint
ok 406 - profile-driven LIVE endpoint exists separately from the frozen legacy endpoint
  ---
  duration_ms: 0.332223
  type: 'test'
  ...
# Subtest: 1 day means the current Denmark calendar day from 00:00, not yesterday
ok 407 - 1 day means the current Denmark calendar day from 00:00, not yesterday
  ---
--- lines 5266-5309 ---
  ...
# Subtest: review helpers accept current live LinkedIn result shape
ok 414 - review helpers accept current live LinkedIn result shape
  ---
  duration_ms: 1.597669
  type: 'test'
  ...
# Subtest: merged UI restores Search Profile persistence without wiring it into LinkedIn search
not ok 415 - merged UI restores Search Profile persistence without wiring it into LinkedIn search
  ---
  duration_ms: 2.239294
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/profile-review.test.mjs:77:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Step \{profileStep\} of 5/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 5312-5415 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 5418-5506 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 5546-5630 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 5661-5708 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 5802-5993 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 5998-6071 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 6206-6249 ---
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: merged UI keeps Application Pack and exposes the direct M4 review flow
not ok 416 - merged UI keeps Application Pack and exposes the direct M4 review flow
  ---
  duration_ms: 4.908825
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/profile-review.test.mjs:86:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Adapt & review CV/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 6252-6355 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 6358-6446 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 6486-6570 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 6601-6648 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 6742-6933 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 6938-7011 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 7158-7201 ---
  ...
# Subtest: M4 CV review shows a neutral empty state when AI returns no changed block
ok 418 - M4 CV review shows a neutral empty state when AI returns no changed block
  ---
  duration_ms: 1.323084
  type: 'test'
  ...
# Subtest: Source CV upload entry points keep the current five-step Search Profile flow
not ok 419 - Source CV upload entry points keep the current five-step Search Profile flow
  ---
  duration_ms: 1.225701
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/profile-review.test.mjs:116:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Upload CV<\/button>/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 7204-7307 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 7310-7398 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 7438-7522 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 7553-7600 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 7694-7885 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 7890-7963 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 8146-8204 ---
  ...
# Subtest: Version 2 Step 1 refuses a truncated legacy preview instead of tailoring a partial Summary
ok 427 - Version 2 Step 1 refuses a truncated legacy preview instead of tailoring a partial Summary
  ---
  duration_ms: 0.186781
  type: 'test'
  ...
# Subtest: page promotes the saved Union Search Plan to LIVE while preserving legacy fallback payload
not ok 428 - page promotes the saved Union Search Plan to LIVE while preserving legacy fallback payload
  ---
  duration_ms: 7.069361
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/profile-shadow-wiring.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /setJobs\(Array\.isArray\(data\.jobs\)\?data\.jobs:\[\]\)/. Input:
    
    'async function search(){\n' +
      '  if(!resumeLoaded){\n' +
      "    setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      '  const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]\n' +
      '  const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]\n' +
      '  if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){\n' +
      "    setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      "  setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0\n' +
      '  try{\n' +
      '    const tasks=[]\n' +
      '\n' +
      "    if(selectedSources.includes('linkedin')) tasks.push((async()=>{\n" +
      '      let res\n' +
      '      if(hasProfilePlan){\n' +
      '        const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint\n' +
      '        const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})\n' +
      "        res=await fetch('/api/linkedin-profile-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({\n' +
      '            freshnessDays,\n' +
      '            unionSearchPlan:profile.unionSearchPlan,\n' +
      '            exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '            previousCandidates:poolSnapshot.candidates,\n' +
      '            previousVerifiedJobs:poolSnapshot.verifiedJobs,\n' +
      '          }),\n' +
      '        })\n' +
      '      }else{\n' +
      "        res=await fetch('/api/linkedin-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),\n' +
      '        })\n' +
      '      }\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'LinkedIn search failed')\n" +
--- lines 8298-8360 ---
      '      masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),\n' +
      '      fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),\n' +
      '      returned:mergedJobs.length,\n' +
      '    }\n' +
      '    const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))\n' +
      "    const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')\n" +
      '    setState({\n' +
      '      loading:false,\n' +
      "      error:'',\n" +
      "      coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},\n" +
      '      stats,\n' +
      '      fetchedAt:new Date().toISOString(),\n' +
      '      audit,\n' +
      '    })\n' +
      "  }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }\n" +
      '}'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 8454-8530 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/profile-shadow-wiring.test.mjs:18:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: parse-cv endpoint accepts resume uploads instead of being retired
ok 429 - parse-cv endpoint accepts resume uploads instead of being retired
  ---
  duration_ms: 1.527257
  type: 'test'
  ...
# Subtest: Feature 2 UI persists the complete active Source CV and supports legacy migration
ok 430 - Feature 2 UI persists the complete active Source CV and supports legacy migration
  ---
  duration_ms: 0.274916
  type: 'test'
  ...
# Subtest: Feature 2 retains the extracted Professional Summary separately from the short preview
ok 431 - Feature 2 retains the extracted Professional Summary separately from the short preview
  ---
  duration_ms: 0.162715
  type: 'test'
  ...
# Subtest: right panel uses dedicated Expertise Match analysis and keeps the agreed vacancy header
not ok 432 - right panel uses dedicated Expertise Match analysis and keeps the agreed vacancy header
  ---
  duration_ms: 2.171265
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/right-panel-expertise-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Source: LinkedIn/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 8533-8636 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 8639-8727 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 8767-8851 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 8882-8929 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 9023-9214 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 9219-9292 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 9443-9486 ---
  ...
# Subtest: job conditions never alter Expertise Match
ok 435 - job conditions never alter Expertise Match
  ---
  duration_ms: 25.490672
  type: 'test'
  ...
# Subtest: page wires API audit rows into a closed-by-default Search Audit component
not ok 436 - page wires API audit rows into a closed-by-default Search Audit component
  ---
  duration_ms: 8.884479
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/search-audit-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /audit:Array\.isArray\(data\.audit\)\?data\.audit:\[\]/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 9489-9592 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 9595-9683 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 9723-9807 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 9838-9885 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 9979-10170 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 10175-10248 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 10451-10494 ---
  ...
# Subtest: blank exclusions use zero AI calls
ok 448 - blank exclusions use zero AI calls
  ---
  duration_ms: 0.248326
  type: 'test'
  ...
# Subtest: custom exclusions are compiled only on Save profile and then passed to profile-driven LIVE Search
not ok 449 - custom exclusions are compiled only on Save profile and then passed to profile-driven LIVE Search
  ---
  duration_ms: 4.585418
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/search-profile-exclusions-save-ui.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JSON\.stringify\(\{freshnessDays,unionSearchPlan:profile\.unionSearchPlan,exclusionRules:Array\.isArray\(profile\.exclusionRules\)\?profile\.exclusionRules:\[\]\}\)/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 10497-10600 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 10603-10691 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 10731-10815 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 10846-10893 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 10987-11178 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 11183-11256 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 11449-11492 ---
  ...
# Subtest: library fingerprint is stable by slot and changes only with CV membership/sourceVersion or builder version
ok 459 - library fingerprint is stable by slot and changes only with CV membership/sourceVersion or builder version
  ---
  duration_ms: 0.406283
  type: 'test'
  ...
# Subtest: Search Profile wizard has five steps and no salary screen or salary scoring label
not ok 460 - Search Profile wizard has five steps and no salary screen or salary scoring label
  ---
  duration_ms: 4.409758
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/search-profile-no-salary-ui.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /profileStep\/5\*100/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 11495-11598 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 11601-11689 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 11729-11813 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 11844-11891 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 11985-12176 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 12181-12254 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 12417-12460 ---
  ...
# Subtest: Search Profile step 2 combines all ready CVs while legacy Search keeps CV 1
ok 465 - Search Profile step 2 combines all ready CVs while legacy Search keeps CV 1
  ---
  duration_ms: 2.079654
  type: 'test'
  ...
# Subtest: Search Profile Step 3 separates location from work model
not ok 466 - Search Profile Step 3 separates location from work model
  ---
  duration_ms: 3.100871
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/search-profile-step3-ui-contract.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /<SearchProfileLocationStep/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 12463-12566 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 12569-12657 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 12697-12781 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 12812-12859 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 12953-13144 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 13149-13222 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
--- lines 13685-13743 ---
  ...
# Subtest: page wires the edited draft into a saved Union Search Plan preview
ok 521 - page wires the edited draft into a saved Union Search Plan preview
  ---
  duration_ms: 7.550303
  type: 'test'
  ...
# Subtest: saved Union Search Plan drives profile LIVE while legacy fallback stays isolated
not ok 522 - saved Union Search Plan drives profile LIVE while legacy fallback stays isolated
  ---
  duration_ms: 6.246446
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/union-search-plan-wiring.test.mjs:22:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JSON\.stringify\(\{freshnessDays,unionSearchPlan:profile\.unionSearchPlan,exclusionRules:Array\.isArray\(profile\.exclusionRules\)\?profile\.exclusionRules:\[\]\}\)/. Input:
    
    'async function search(){\n' +
      '  if(!resumeLoaded){\n' +
      "    setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      '  const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]\n' +
      '  const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]\n' +
      '  if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){\n' +
      "    setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      "  setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0\n' +
      '  try{\n' +
      '    const tasks=[]\n' +
      '\n' +
      "    if(selectedSources.includes('linkedin')) tasks.push((async()=>{\n" +
      '      let res\n' +
      '      if(hasProfilePlan){\n' +
      '        const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint\n' +
      '        const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})\n' +
      "        res=await fetch('/api/linkedin-profile-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({\n' +
      '            freshnessDays,\n' +
      '            unionSearchPlan:profile.unionSearchPlan,\n' +
      '            exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '            previousCandidates:poolSnapshot.candidates,\n' +
      '            previousVerifiedJobs:poolSnapshot.verifiedJobs,\n' +
      '          }),\n' +
      '        })\n' +
      '      }else{\n' +
      "        res=await fetch('/api/linkedin-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),\n' +
      '        })\n' +
      '      }\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'LinkedIn search failed')\n" +
--- lines 13837-13899 ---
      '      masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),\n' +
      '      fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),\n' +
      '      returned:mergedJobs.length,\n' +
      '    }\n' +
      '    const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))\n' +
      "    const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')\n" +
      '    setState({\n' +
      '      loading:false,\n' +
      "      error:'',\n" +
      "      coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},\n" +
      '      stats,\n' +
      '      fetchedAt:new Date().toISOString(),\n' +
      '      audit,\n' +
      '    })\n' +
      "  }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }\n" +
      '}'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 13993-14036 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///tmp/applypilot-baseline/app/lib/union-search-plan-wiring.test.mjs:30:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: compiles edited roles in primary-then-adjacent order and primary wins duplicates
ok 523 - compiles edited roles in primary-then-adjacent order and primary wins duplicates
  ---
  duration_ms: 3.459354
  type: 'test'
  ...
# Subtest: removed raw CV-proposed roles do not survive the edited plan
ok 524 - removed raw CV-proposed roles do not survive the edited plan
  ---
  duration_ms: 2.139826
  type: 'test'
  ...
# Subtest: manual roles are preserved without invented CV provenance
ok 525 - manual roles are preserved without invented CV provenance
  ---
  duration_ms: 0.498485
  type: 'test'
  ...
# Subtest: CV-supported roles preserve all supporting CV ids, slots and raw support kinds
ok 526 - CV-supported roles preserve all supporting CV ids, slots and raw support kinds
  ---
  duration_ms: 0.501853
--- lines 14134-14177 ---
  ...
# Subtest: CV Update Review keeps a focused ORIGINAL UPDATED review without the obsolete Truth Guard panel
ok 543 - CV Update Review keeps a focused ORIGINAL UPDATED review without the obsolete Truth Guard panel
  ---
  duration_ms: 0.424777
  type: 'test'
  ...
# Subtest: review cards show Original, Updated, Why changed and independent decisions
not ok 544 - review cards show Original, Updated, Why changed and independent decisions
  ---
  duration_ms: 2.278638
  type: 'test'
  location: '/tmp/applypilot-baseline/app/lib/v16-cv-review.contract.test.mjs:24:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /change\.updated/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'\n" +
      "import AppliedJobsArchive from './components/applied-jobs-archive.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'\n" +
      "import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'\n" +
--- lines 14180-14283 ---
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())\n' +
      '  const [companyWatchOpen,setCompanyWatchOpen]=useState(false)\n' +
      '  const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())\n' +
      '  const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      '  const [appliedJobs,setAppliedJobs]=useState([])\n' +
      '  const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPri'... 51254 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readAppliedJobs,archiveAppliedJob,syncAppliedArchive} from './lib/applied-jobs.js'
    import AppliedJobsArchive from './components/applied-jobs-archive.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import {companyConnection,connectedCompanyNames,defaultCompanyWatch,readCompanyWatch,writeCompanyWatch,TARGET_COMPANIES} from './lib/company-watch.js'
    import {CONSULTANT_PORTALS,connectedConsultantPortalIds,defaultConsultantPortals,readConsultantPortals,writeConsultantPortals} from './lib/consultant-portals.js'
--- lines 14286-14374 ---
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='consultant_portal')return `Consultant portal · ${job?.consultantPortal?.name||job?.company||'Consultant'}`;if(source==='company_site')return `Company site · ${job?.company||'Official'}`;if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const currentOfficial=String(current?.job?.source||'')==='company_site';const itemOfficial=String(item?.job?.source||'')==='company_site';if(itemOfficial&&!currentOfficial){out[index]=item;continue}if(currentOfficial&&!itemOfficial)continue;const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [companyWatch,setCompanyWatch]=useState(()=>defaultCompanyWatch())
      const [companyWatchOpen,setCompanyWatchOpen]=useState(false)
      const [consultantPortals,setConsultantPortals]=useState(()=>defaultConsultantPortals())
      const [consultantPortalsOpen,setConsultantPortalsOpen]=useState(false)
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [appliedJobs,setAppliedJobs]=useState([])
      const [appliedArchiveOpen,setAppliedArchiveOpen]=useState(false)
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCompanyWatch(readCompanyWatch(localStorage))
          setConsultantPortals(readConsultantPortals(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
--- lines 14414-14498 ---
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        const item=jobs.find(candidate=>candidate?.job?.sourceJobId===jobId)
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
        if(status==='applied'&&item){
          setAppliedJobs(current=>archiveAppliedJob({storage:localStorage,archive:current,job:item.job,evaluation:item.evaluation}))
        }
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
--- lines 14529-14576 ---
        setConsultantPortals(current=>{
          const selected=current.selected.includes(id)?current.selected.filter(value=>value!==id):[...current.selected,id]
          return writeConsultantPortals(localStorage,{...current,selected})
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      const activeCompanySites=companyWatch.enabled?companyWatch.selected.filter(name=>connectedCompanyNames().includes(name)):[]
      const activeConsultantPortals=consultantPortals.enabled?consultantPortals.selected.filter(id=>connectedConsultantPortalIds().includes(id)):[]
      if(!selectedSources.length&&!activeCompanySites.length&&!activeConsultantPortals.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
--- lines 14670-14861 ---
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:[...selectedSources,...(activeCompanySites.length?['company-sites']:[]),...(activeConsultantPortals.length?['consultant-portals']:[])].join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
--- lines 14866-14939 ---
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">MULTI-SOURCE + COMPANY WATCH · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        <section className="companyWatch">
          <div className="companyWatchMain">
            <label className="companyWatchToggle"><input type="checkbox" checked={companyWatch.enabled} onChange={toggleCompanyWatchEnabled}/><span><small>DIRECT COMPANY WATCH</small><b>Company career sites</b></span></label>
            <div className="companyWatchActions"><span>{companyWatch.selected.length} companies selected</span><button className="secondary companyManage" onClick={()=>setCompanyWatchOpen(open=>!open)}>{companyWatchOpen?'Close':'Manage'}</button></div>
          </div>
          {companyWatchOpen&&<div className="companyWatchList">
            {TARGET_COMPANIES.map(name=>{const connection=companyConnection(name);return <label key={name}><input type="checkbox" checked={companyWatch.selected.includes(name)} onChange={()=>toggleCompany(name)}/><span>{name}</span><small className={connection.status==='connected'?'ready':''}>{connection.status==='connected'?('Connected · '+connection.connector):'Connection pending'}</small></label>})}
          </div>}
        </section>
    
