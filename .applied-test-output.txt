
> applypilot@1.0.0 test
> node --test app/lib/*.test.mjs

TAP version 13
# Subtest: callStructuredAi passes stage, instructions, input and schema to injected modelCall
ok 1 - callStructuredAi passes stage, instructions, input and schema to injected modelCall
  ---
  duration_ms: 2.895878
  type: 'test'
  ...
# Subtest: callStructuredAi sanitizes provider errors without echoing sensitive input
ok 2 - callStructuredAi sanitizes provider errors without echoing sensitive input
  ---
  duration_ms: 2.774198
  type: 'test'
  ...
# Subtest: callStructuredAi preserves a safe provider failure code without leaking provider details
ok 3 - callStructuredAi preserves a safe provider failure code without leaking provider details
  ---
  duration_ms: 0.599267
  type: 'test'
  ...
# Subtest: production AI path classifies missing API configuration safely
ok 4 - production AI path classifies missing API configuration safely
  ---
  duration_ms: 0.694251
  type: 'test'
  ...
# Subtest: production AI path classifies provider HTTP status without exposing response body
ok 5 - production AI path classifies provider HTTP status without exposing response body
  ---
  duration_ms: 0.717491
  type: 'test'
  ...
# Subtest: production AI path uses the caller-specific output-token budget
ok 6 - production AI path uses the caller-specific output-token budget
  ---
  duration_ms: 0.637041
  type: 'test'
  ...
# Subtest: production AI path classifies max-output-token incomplete responses safely
ok 7 - production AI path classifies max-output-token incomplete responses safely
  ---
  duration_ms: 0.657296
  type: 'test'
  ...
# Subtest: accepts a JD analysis with 3 to 5 grounded priorities
ok 8 - accepts a JD analysis with 3 to 5 grounded priorities
  ---
  duration_ms: 3.097227
  type: 'test'
  ...
# Subtest: rejects fewer than 3 priorities
ok 9 - rejects fewer than 3 priorities
  ---
  duration_ms: 0.665671
  type: 'test'
  ...
# Subtest: rejects more than 5 priorities
ok 10 - rejects more than 5 priorities
  ---
  duration_ms: 0.362346
  type: 'test'
  ...
# Subtest: requires exact JD evidence for every priority
ok 11 - requires exact JD evidence for every priority
  ---
  duration_ms: 0.356576
  type: 'test'
  ...
# Subtest: requires an explicit mustHaves array separate from hiring priorities
ok 12 - requires an explicit mustHaves array separate from hiring priorities
  ---
  duration_ms: 0.316979
  type: 'test'
  ...
# Subtest: must-haves use their own qualification requirements and JD evidence
ok 13 - must-haves use their own qualification requirements and JD evidence
  ---
  duration_ms: 0.32918
  type: 'test'
  ...
# Subtest: professional summary writer contract accepts grounded claims
ok 14 - professional summary writer contract accepts grounded claims
  ---
  duration_ms: 0.378004
  type: 'test'
  ...
# Subtest: professional summary writer contract rejects claims without evidence IDs
ok 15 - professional summary writer contract rejects claims without evidence IDs
  ---
  duration_ms: 0.35841
  type: 'test'
  ...
# Subtest: truth guard assessment contract accepts only declared M4.10 issue codes
ok 16 - truth guard assessment contract accepts only declared M4.10 issue codes
  ---
  duration_ms: 0.607551
  type: 'test'
  ...
# Subtest: truth guard PASS cannot contain unresolved issues and FAIL must contain at least one issue
ok 17 - truth guard PASS cannot contain unresolved issues and FAIL must contain at least one issue
  ---
  duration_ms: 0.705039
  type: 'test'
  ...
# Subtest: archives full vacancy snapshot when marked applied
ok 18 - archives full vacancy snapshot when marked applied
  ---
  duration_ms: 2.292485
  type: 'test'
  ...
# Subtest: re-applying updates snapshot without duplicate and preserves first applied date
ok 19 - re-applying updates snapshot without duplicate and preserves first applied date
  ---
  duration_ms: 0.333247
  type: 'test'
  ...
# Subtest: sync backfills existing APPLIED statuses from current search items
ok 20 - sync backfills existing APPLIED statuses from current search items
  ---
  duration_ms: 0.836075
  type: 'test'
  ...
# Subtest: confirmation route reads token_hash
ok 21 - confirmation route reads token_hash
  ---
  duration_ms: 7.186233
  type: 'test'
  ...
# Subtest: confirmation route validates OTP type
ok 22 - confirmation route validates OTP type
  ---
  duration_ms: 1.577889
  type: 'test'
  ...
# Subtest: confirmation route verifies token hash with Supabase
ok 23 - confirmation route verifies token hash with Supabase
  ---
  duration_ms: 1.293507
  type: 'test'
  ...
# Subtest: confirmation route uses safe local next redirect
ok 24 - confirmation route uses safe local next redirect
  ---
  duration_ms: 1.171986
  type: 'test'
  ...
# Subtest: confirmation route sends invalid links back to login safely
ok 25 - confirmation route sends invalid links back to login safely
  ---
  duration_ms: 2.023209
  type: 'test'
  ...
# Subtest: confirmation route does not echo provider error messages
ok 26 - confirmation route does not echo provider error messages
  ---
  duration_ms: 1.195007
  type: 'test'
  ...
# Subtest: login is passwordless email-only UI
ok 27 - login is passwordless email-only UI
  ---
  duration_ms: 7.024419
  type: 'test'
  ...
# Subtest: magic-link request forbids automatic user creation
ok 28 - magic-link request forbids automatic user creation
  ---
  duration_ms: 1.784496
  type: 'test'
  ...
# Subtest: magic-link request uses the auth confirm route on the current deployment
ok 29 - magic-link request uses the auth confirm route on the current deployment
  ---
  duration_ms: 2.059682
  type: 'test'
  ...
# Subtest: login response copy does not reveal whether email exists
ok 30 - login response copy does not reveal whether email exists
  ---
  duration_ms: 1.560779
  type: 'test'
  ...
# Subtest: login page identifies access as invite-only
ok 31 - login page identifies access as invite-only
  ---
  duration_ms: 2.807375
  type: 'test'
  ...
# Subtest: root middleware delegates to updateSession
ok 32 - root middleware delegates to updateSession
  ---
  duration_ms: 11.104745
  type: 'test'
  ...
# Subtest: middleware excludes framework static assets from matcher
ok 33 - middleware excludes framework static assets from matcher
  ---
  duration_ms: 1.945465
  type: 'test'
  ...
# Subtest: session middleware calls Supabase getUser for protected pages
ok 34 - session middleware calls Supabase getUser for protected pages
  ---
  duration_ms: 1.201348
  type: 'test'
  ...
# Subtest: session middleware redirects anonymous protected pages to login
ok 35 - session middleware redirects anonymous protected pages to login
  ---
  duration_ms: 1.246476
  type: 'test'
  ...
# Subtest: session middleware lets API routes reach route-level 401 guard
ok 36 - session middleware lets API routes reach route-level 401 guard
  ---
  duration_ms: 4.958015
  type: 'test'
  ...
# Subtest: company-search imports the shared route-level auth guard
ok 37 - company-search imports the shared route-level auth guard
  ---
  duration_ms: 11.606854
  type: 'test'
  ...
# Subtest: company-search authorizes before request parsing or provider/business work
ok 38 - company-search authorizes before request parsing or provider/business work
  ---
  duration_ms: 4.147773
  type: 'test'
  ...
# Subtest: expertise-match imports the shared route-level auth guard
ok 39 - expertise-match imports the shared route-level auth guard
  ---
  duration_ms: 1.616716
  type: 'test'
  ...
# Subtest: expertise-match authorizes before request parsing or provider/business work
ok 40 - expertise-match authorizes before request parsing or provider/business work
  ---
  duration_ms: 1.957996
  type: 'test'
  ...
# Subtest: linkedin-search imports the shared route-level auth guard
ok 41 - linkedin-search imports the shared route-level auth guard
  ---
  duration_ms: 4.499683
  type: 'test'
  ...
# Subtest: linkedin-search authorizes before request parsing or provider/business work
ok 42 - linkedin-search authorizes before request parsing or provider/business work
  ---
  duration_ms: 2.495325
  type: 'test'
  ...
# Subtest: open-job imports the shared route-level auth guard
ok 43 - open-job imports the shared route-level auth guard
  ---
  duration_ms: 0.964187
  type: 'test'
  ...
# Subtest: open-job authorizes before request parsing or provider/business work
ok 44 - open-job authorizes before request parsing or provider/business work
  ---
  duration_ms: 1.019193
  type: 'test'
  ...
# Subtest: parse-cv imports the shared route-level auth guard
ok 45 - parse-cv imports the shared route-level auth guard
  ---
  duration_ms: 4.607268
  type: 'test'
  ...
# Subtest: parse-cv authorizes before request parsing or provider/business work
ok 46 - parse-cv authorizes before request parsing or provider/business work
  ---
  duration_ms: 4.115738
  type: 'test'
  ...
# Subtest: search-jobs imports the shared route-level auth guard
ok 47 - search-jobs imports the shared route-level auth guard
  ---
  duration_ms: 3.475721
  type: 'test'
  ...
# Subtest: search-jobs authorizes before request parsing or provider/business work
ok 48 - search-jobs authorizes before request parsing or provider/business work
  ---
  duration_ms: 0.746141
  type: 'test'
  ...
# Subtest: tailor-cv imports the shared route-level auth guard
ok 49 - tailor-cv imports the shared route-level auth guard
  ---
  duration_ms: 0.541436
  type: 'test'
  ...
# Subtest: tailor-cv authorizes before request parsing or provider/business work
ok 50 - tailor-cv authorizes before request parsing or provider/business work
  ---
  duration_ms: 2.366368
  type: 'test'
  ...
# Subtest: shared guard returns a uniform 401 when no authenticated user exists
ok 51 - shared guard returns a uniform 401 when no authenticated user exists
  ---
  duration_ms: 5.693998
  type: 'test'
  ...
# Subtest: shared guard exposes normalized role and an admin-only guard
ok 52 - shared guard exposes normalized role and an admin-only guard
  ---
  duration_ms: 0.947018
  type: 'test'
  ...
# Subtest: API paths are delegated to route-level auth guards
ok 53 - API paths are delegated to route-level auth guards
  ---
  duration_ms: 2.633084
  type: 'test'
  ...
# Subtest: non-API page is not classified as API
ok 54 - non-API page is not classified as API
  ---
  duration_ms: 0.287547
  type: 'test'
  ...
# Subtest: login is public
ok 55 - login is public
  ---
  duration_ms: 0.201388
  type: 'test'
  ...
# Subtest: auth confirm callback is public
ok 56 - auth confirm callback is public
  ---
  duration_ms: 0.163773
  type: 'test'
  ...
# Subtest: root application page is private
ok 57 - root application page is private
  ---
  duration_ms: 0.189528
  type: 'test'
  ...
# Subtest: Next static assets are public
ok 58 - Next static assets are public
  ---
  duration_ms: 0.215954
  type: 'test'
  ...
# Subtest: favicon is public
ok 59 - favicon is public
  ---
  duration_ms: 0.166438
  type: 'test'
  ...
# Subtest: safe relative next path is preserved
ok 60 - safe relative next path is preserved
  ---
  duration_ms: 0.240236
  type: 'test'
  ...
# Subtest: absolute redirect is rejected
ok 61 - absolute redirect is rejected
  ---
  duration_ms: 0.460045
  type: 'test'
  ...
# Subtest: protocol-relative redirect is rejected
ok 62 - protocol-relative redirect is rejected
  ---
  duration_ms: 0.610997
  type: 'test'
  ...
# Subtest: backslash redirect is rejected
ok 63 - backslash redirect is rejected
  ---
  duration_ms: 0.250133
  type: 'test'
  ...
# Subtest: email OTP type is accepted
ok 64 - email OTP type is accepted
  ---
  duration_ms: 0.188005
  type: 'test'
  ...
# Subtest: invite OTP type is accepted
ok 65 - invite OTP type is accepted
  ---
  duration_ms: 0.133051
  type: 'test'
  ...
# Subtest: unsupported OTP type is rejected
ok 66 - unsupported OTP type is rejected
  ---
  duration_ms: 0.126458
  type: 'test'
  ...
# Subtest: role policy exposes a server-safe ApplyPilot role resolver
ok 67 - role policy exposes a server-safe ApplyPilot role resolver
  ---
  duration_ms: 0.134864
  type: 'test'
  ...
# Subtest: role policy recognizes admin only from protected app_metadata
ok 68 - role policy recognizes admin only from protected app_metadata
  ---
  duration_ms: 0.153927
  type: 'test'
  ...
# Subtest: role policy fails closed and never trusts user-editable metadata
ok 69 - role policy fails closed and never trusts user-editable metadata
  ---
  duration_ms: 0.152274
  type: 'test'
  ...
# Subtest: signout route is POST-only and calls Supabase signOut
ok 70 - signout route is POST-only and calls Supabase signOut
  ---
  duration_ms: 7.381897
  type: 'test'
  ...
# Subtest: signout route redirects to login after clearing session
ok 71 - signout route redirects to login after clearing session
  ---
  duration_ms: 1.646487
  type: 'test'
  ...
# Subtest: root layout includes compact sign-out control without modifying Home page
ok 72 - root layout includes compact sign-out control without modifying Home page
  ---
  duration_ms: 2.392107
  type: 'test'
  ...
# Subtest: browser client module uses @supabase/ssr createBrowserClient
ok 73 - browser client module uses @supabase/ssr createBrowserClient
  ---
  duration_ms: 10.19989
  type: 'test'
  ...
# Subtest: server client module uses @supabase/ssr createServerClient
ok 74 - server client module uses @supabase/ssr createServerClient
  ---
  duration_ms: 1.531499
  type: 'test'
  ...
# Subtest: server cookie adapter reads all cookies
ok 75 - server cookie adapter reads all cookies
  ---
  duration_ms: 1.969276
  type: 'test'
  ...
# Subtest: server cookie adapter writes every refreshed cookie
ok 76 - server cookie adapter writes every refreshed cookie
  ---
  duration_ms: 0.528264
  type: 'test'
  ...
# Subtest: server cookie adapter safely tolerates read-only cookie stores
ok 77 - server cookie adapter safely tolerates read-only cookie stores
  ---
  duration_ms: 0.530277
  type: 'test'
  ...
# Subtest: Supabase public config uses static NEXT_PUBLIC env references for client bundling
ok 78 - Supabase public config uses static NEXT_PUBLIC env references for client bundling
  ---
  duration_ms: 7.162718
  type: 'test'
  ...
# Subtest: Supabase config keeps explicit env injection for server-side tests and callers
ok 79 - Supabase config keeps explicit env injection for server-side tests and callers
  ---
  duration_ms: 1.031103
  type: 'test'
  ...
# Subtest: Best CV API is protected and delegates only to the dedicated selector
ok 80 - Best CV API is protected and delegates only to the dedicated selector
  ---
  duration_ms: 1.362887
  type: 'test'
  ...
# Subtest: Best CV API exposes safe AI failure categories without logging CV or JD payloads
ok 81 - Best CV API exposes safe AI failure categories without logging CV or JD payloads
  ---
  duration_ms: 0.602912
  type: 'test'
  ...
# Subtest: Best CV cache changes when JD, candidate membership or any CV sourceVersion changes
ok 82 - Best CV cache changes when JD, candidate membership or any CV sourceVersion changes
  ---
  duration_ms: 1.426477
  type: 'test'
  ...
# Subtest: unchanged vacancy and CV library reuse the saved Best CV analysis
ok 83 - unchanged vacancy and CV library reuse the saved Best CV analysis
  ---
  duration_ms: 1.621284
  type: 'test'
  ...
# Subtest: Best CV cache exposes analysis persistence only, not obsolete user-selection persistence
ok 84 - Best CV cache exposes analysis persistence only, not obsolete user-selection persistence
  ---
  duration_ms: 0.151892
  type: 'test'
  ...
# Subtest: requestBestCv sends one request containing the Full JD and all candidate CV records
ok 85 - requestBestCv sends one request containing the Full JD and all candidate CV records
  ---
  duration_ms: 1.570647
  type: 'test'
  ...
# Subtest: requestBestCv surfaces safe server errors
ok 86 - requestBestCv surfaces safe server errors
  ---
  duration_ms: 0.717071
  type: 'test'
  ...
# Subtest: selector packet preserves recruiter positioning without AI and is deterministic
ok 87 - selector packet preserves recruiter positioning without AI and is deterministic
  ---
  duration_ms: 8.902787
  type: 'test'
  ...
# Subtest: unsafe CV structure falls back to complete parsed CV text rather than losing evidence
ok 88 - unsafe CV structure falls back to complete parsed CV text rather than losing evidence
  ---
  duration_ms: 0.833321
  type: 'test'
  ...
# Subtest: Best CV validation only accepts supplied CV ids, complete unique ranking and valid advice
ok 89 - Best CV validation only accepts supplied CV ids, complete unique ranking and valid advice
  ---
  duration_ms: 0.923677
  type: 'test'
  ...
# Subtest: Best CV compares all ready CVs in one structured AI call and never requests a merged CV
ok 90 - Best CV compares all ready CVs in one structured AI call and never requests a merged CV
  ---
  duration_ms: 3.408134
  type: 'test'
  ...
# Subtest: right panel mounts Best CV flow while UX polish orders Expertise Match first
ok 91 - right panel mounts Best CV flow while UX polish orders Expertise Match first
  ---
  duration_ms: 1.247618
  type: 'test'
  ...
# Subtest: Best CV component is manual, cached and has no Best CV percentage
ok 92 - Best CV component is manual, cached and has no Best CV percentage
  ---
  duration_ms: 0.270578
  type: 'test'
  ...
# Subtest: Best CV result is recommendation-only; selection remains a separate user choice
ok 93 - Best CV result is recommendation-only; selection remains a separate user choice
  ---
  duration_ms: 0.254921
  type: 'test'
  ...
# Subtest: Best CV component cannot call Expertise Match or LinkedIn Search
ok 94 - Best CV component cannot call Expertise Match or LinkedIn Search
  ---
  duration_ms: 0.228525
  type: 'test'
  ...
# company-search core tests passed
# Subtest: app/lib/company-search.test.mjs
ok 16 - app/lib/company-search.test.mjs
  ---
  duration_ms: 43.775979
  type: 'test'
  ...
# Subtest: Step 5 labels CV1 explicitly as the Primary Search CV
not ok 96 - Step 5 labels CV1 explicitly as the Primary Search CV
  ---
  duration_ms: 2.876725
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cosmetic-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /<span>Primary Search CV<\/span>/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cosmetic-ui.contract.test.mjs:9:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: profile strip shows a dynamic saved Union Search Plan summary instead of legacy role and score copy
ok 97 - profile strip shows a dynamic saved Union Search Plan summary instead of legacy role and score copy
  ---
  duration_ms: 0.361375
  type: 'test'
  ...
# Subtest: Search and Shadow diagnostics live together at the bottom under AUDIT LOG with technical styling
ok 98 - Search and Shadow diagnostics live together at the bottom under AUDIT LOG with technical styling
  ---
  duration_ms: 0.554098
  type: 'test'
  ...
# Subtest: generated CV updates are kept by baseline key so reopening review does not call AI again
ok 99 - generated CV updates are kept by baseline key so reopening review does not call AI again
  ---
  duration_ms: 0.916135
  type: 'test'
  ...
# Subtest: Generate and View are separate controls with Generate disabled after success
ok 100 - Generate and View are separate controls with Generate disabled after success
  ---
  duration_ms: 0.224719
  type: 'test'
  ...
# Subtest: adaptation action row is passed into Best CV panel and removed from Application pack
ok 101 - adaptation action row is passed into Best CV panel and removed from Application pack
  ---
  duration_ms: 0.405842
  type: 'test'
  ...
# Subtest: Best CV passes action controls into the CV chooser
ok 102 - Best CV passes action controls into the CV chooser
  ---
  duration_ms: 0.170194
  type: 'test'
  ...
# Subtest: CV chooser renders action controls inside the integrated CV workflow section
ok 103 - CV chooser renders action controls inside the integrated CV workflow section
  ---
  duration_ms: 0.135594
  type: 'test'
  ...
# Subtest: builds one immutable vacancy-to-CV baseline from the selected CV only
ok 104 - builds one immutable vacancy-to-CV baseline from the selected CV only
  ---
  duration_ms: 4.812398
  type: 'test'
  ...
# Subtest: baseline key is stable for the same vacancy and selected CV snapshot
ok 105 - baseline key is stable for the same vacancy and selected CV snapshot
  ---
  duration_ms: 0.557925
  type: 'test'
  ...
# Subtest: changing vacancy invalidates the old baseline
ok 106 - changing vacancy invalidates the old baseline
  ---
  duration_ms: 2.087089
  type: 'test'
  ...
# Subtest: replacing the selected CV invalidates the old baseline when sourceVersion changes
ok 107 - replacing the selected CV invalidates the old baseline when sourceVersion changes
  ---
  duration_ms: 0.292696
  type: 'test'
  ...
# Subtest: rejects an invalid vacancy or CV instead of fabricating a baseline
ok 108 - rejects an invalid vacancy or CV instead of fabricating a baseline
  ---
  duration_ms: 0.721197
  type: 'test'
  ...
# Subtest: requestSelectedCvEvidence sends only the selected CV through the adaptation stage chain
ok 109 - requestSelectedCvEvidence sends only the selected CV through the adaptation stage chain
  ---
  duration_ms: 4.64876
  type: 'test'
  ...
# Subtest: requestSelectedCvEvidence fails safely when the server rejects the selected-CV stage
ok 110 - requestSelectedCvEvidence fails safely when the server rejects the selected-CV stage
  ---
  duration_ms: 0.892153
  type: 'test'
  ...
# Subtest: requestProfessionalSummary continues the selected-CV chain into Summary writing only
ok 111 - requestProfessionalSummary continues the selected-CV chain into Summary writing only
  ---
  duration_ms: 0.696856
  type: 'test'
  ...
# Subtest: requestTruthGuard continues through all three writers and then runs the bound Truth Guard stage
ok 112 - requestTruthGuard continues through all three writers and then runs the bound Truth Guard stage
  ---
  duration_ms: 0.928836
  type: 'test'
  ...
# Subtest: decision key binds job, CV, source version and block independently
ok 113 - decision key binds job, CV, source version and block independently
  ---
  duration_ms: 1.393961
  type: 'test'
  ...
# Subtest: CV1 decisions never apply to CV2
ok 114 - CV1 decisions never apply to CV2
  ---
  duration_ms: 0.241819
  type: 'test'
  ...
# Subtest: vacancy A decisions never apply to vacancy B
ok 115 - vacancy A decisions never apply to vacancy B
  ---
  duration_ms: 0.283971
  type: 'test'
  ...
# Subtest: a new sourceVersion cannot see stale decisions
ok 116 - a new sourceVersion cannot see stale decisions
  ---
  duration_ms: 1.8679
  type: 'test'
  ...
# Subtest: Accept and Keep return new decision state without mutating the previous state
ok 117 - Accept and Keep return new decision state without mutating the previous state
  ---
  duration_ms: 1.091387
  type: 'test'
  ...
# Subtest: only original or accepted are valid decision values
ok 118 - only original or accepted are valid decision values
  ---
  duration_ms: 0.64198
  type: 'test'
  ...
# Subtest: review exposes only generated Truth-Guard PASS blocks and uses safeText as UPDATED
ok 119 - review exposes only generated Truth-Guard PASS blocks and uses safeText as UPDATED
  ---
  duration_ms: 0.369238
  type: 'test'
  ...
# Subtest: UPDATED review text is editable and exported from the edited value
ok 120 - UPDATED review text is editable and exported from the edited value
  ---
  duration_ms: 1.15635
  type: 'test'
  ...
# Subtest: buildAdaptationInput exposes exactly one selected CV and the current job
ok 121 - buildAdaptationInput exposes exactly one selected CV and the current job
  ---
  duration_ms: 2.825096
  type: 'test'
  ...
# Subtest: buildAdaptationInput rejects a different vacancy instead of reusing the old baseline
ok 122 - buildAdaptationInput rejects a different vacancy instead of reusing the old baseline
  ---
  duration_ms: 2.419925
  type: 'test'
  ...
# Subtest: M4 review runs direct selected-CV adaptation before opening review
ok 123 - M4 review runs direct selected-CV adaptation before opening review
  ---
  duration_ms: 1.187013
  type: 'test'
  ...
# Subtest: review decisions use exact baseline identity and original or accepted values
ok 124 - review decisions use exact baseline identity and original or accepted values
  ---
  duration_ms: 0.296793
  type: 'test'
  ...
# Subtest: switching vacancy or selected CV cannot expose a stale adaptation result
ok 125 - switching vacancy or selected CV cannot expose a stale adaptation result
  ---
  duration_ms: 0.205155
  type: 'test'
  ...
# Subtest: Accept and Keep handlers mutate decision state only, not CV Library or Source CV
ok 126 - Accept and Keep handlers mutate decision state only, not CV Library or Source CV
  ---
  duration_ms: 0.395304
  type: 'test'
  ...
# Subtest: review UI renders the three-block contract and WHY CHANGED from direct AI review blocks
not ok 127 - review UI renders the three-block contract and WHY CHANGED from direct AI review blocks
  ---
  duration_ms: 2.083954
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:41:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /change\.updated/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:46:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: BestCvPanel no longer owns adaptation selection state
ok 128 - BestCvPanel no longer owns adaptation selection state
  ---
  duration_ms: 0.231311
  type: 'test'
  ...
# Subtest: returns only ready CVs and preserves stable cv-N ids
ok 129 - returns only ready CVs and preserves stable cv-N ids
  ---
  duration_ms: 2.653469
  type: 'test'
  ...
# Subtest: allows any ready CV to be selected for a job
ok 130 - allows any ready CV to be selected for a job
  ---
  duration_ms: 0.579372
  type: 'test'
  ...
# Subtest: refuses a CV that is missing or not ready
ok 131 - refuses a CV that is missing or not ready
  ---
  duration_ms: 0.720536
  type: 'test'
  ...
# Subtest: Best CV recommendation does not auto-select a CV
ok 132 - Best CV recommendation does not auto-select a CV
  ---
  duration_ms: 0.27216
  type: 'test'
  ...
# Subtest: user selection remains independent from the recommended CV
ok 133 - user selection remains independent from the recommended CV
  ---
  duration_ms: 0.382491
  type: 'test'
  ...
# Subtest: CV remove uses ApplyPilot confirmation, keeps Search Profile open and preserves fixed slots
ok 134 - CV remove uses ApplyPilot confirmation, keeps Search Profile open and preserves fixed slots
  ---
  duration_ms: 2.429161
  type: 'test'
  ...
# Subtest: Search Profile step 1 exposes three CV slots while CV 1 remains the Search source
ok 135 - Search Profile step 1 exposes three CV slots while CV 1 remains the Search source
  ---
  duration_ms: 3.243629
  type: 'test'
  ...
# Subtest: CV library has a configurable MVP limit of three slots
ok 136 - CV library has a configurable MVP limit of three slots
  ---
  duration_ms: 2.067025
  type: 'test'
  ...
# Subtest: legacy single Source CV migrates into CV 1 without changing its source data
ok 137 - legacy single Source CV migrates into CV 1 without changing its source data
  ---
  duration_ms: 2.184358
  type: 'test'
  ...
# Subtest: CV 2 and CV 3 are stored independently while CV 1 remains the Search source
ok 138 - CV 2 and CV 3 are stored independently while CV 1 remains the Search source
  ---
  duration_ms: 0.512165
  type: 'test'
  ...
# Subtest: replacing one slot does not modify the other CVs
ok 139 - replacing one slot does not modify the other CVs
  ---
  duration_ms: 0.408226
  type: 'test'
  ...
# Subtest: same sourceVersion cannot occupy two different slots
ok 140 - same sourceVersion cannot occupy two different slots
  ---
  duration_ms: 0.689683
  type: 'test'
  ...
# Subtest: removing CV 2 leaves CV 1 and CV 3 in their original slots
ok 141 - removing CV 2 leaves CV 1 and CV 3 in their original slots
  ---
  duration_ms: 0.300419
  type: 'test'
  ...
# Subtest: removing CV 1 does not promote CV 2 or CV 3
ok 142 - removing CV 1 does not promote CV 2 or CV 3
  ---
  duration_ms: 0.270367
  type: 'test'
  ...
# Subtest: slot number must stay within the MVP limit
ok 143 - slot number must stay within the MVP limit
  ---
  duration_ms: 0.355725
  type: 'test'
  ...
# Subtest: review decision buttons visibly confirm the selected choice
ok 144 - review decision buttons visibly confirm the selected choice
  ---
  duration_ms: 0.768118
  type: 'test'
  ...
# Subtest: detects summary plus latest and previous roles from separate-line headers
ok 145 - detects summary plus latest and previous roles from separate-line headers
  ---
  duration_ms: 7.56867
  type: 'test'
  ...
# Subtest: detects inline role headers and open-ended latest role
ok 146 - detects inline role headers and open-ended latest role
  ---
  duration_ms: 0.959029
  type: 'test'
  ...
# Subtest: keeps multiple roles at the same employer separate
ok 147 - keeps multiple roles at the same employer separate
  ---
  duration_ms: 0.655704
  type: 'test'
  ...
# Subtest: missing summary does not invalidate detected employment roles
ok 148 - missing summary does not invalidate detected employment roles
  ---
  duration_ms: 0.559498
  type: 'test'
  ...
# Subtest: one employment role produces latest role and no previous role
ok 149 - one employment role produces latest role and no previous role
  ---
  duration_ms: 0.594889
  type: 'test'
  ...
# Subtest: role length window uses the greater of 15 percent or eight words
ok 150 - role length window uses the greater of 15 percent or eight words
  ---
  duration_ms: 0.971821
  type: 'test'
  ...
# Subtest: parses common CV header format with title/company before location and stops overview before Key Achievements
ok 151 - parses common CV header format with title/company before location and stops overview before Key Achievements
  ---
  duration_ms: 0.798661
  type: 'test'
  ...
# Subtest: requestCvAdaptation sends selected CV and JD in one adaptation request
ok 152 - requestCvAdaptation sends selected CV and JD in one adaptation request
  ---
  duration_ms: 4.44059
  type: 'test'
  ...
# Subtest: one AI call receives selected CV and JD and returns all three updated blocks
ok 153 - one AI call receives selected CV and JD and returns all three updated blocks
  ---
  duration_ms: 7.251281
  type: 'test'
  ...
# Subtest: review shows AI tailoredText directly without Truth Guard
ok 154 - review shows AI tailoredText directly without Truth Guard
  ---
  duration_ms: 1.63097
  type: 'test'
  ...
# Subtest: replaces only requested DOCX text blocks while preserving paragraph structure
ok 155 - replaces only requested DOCX text blocks while preserving paragraph structure
  ---
  duration_ms: 5.380575
  type: 'test'
  ...
# Subtest: fails safely when a requested source block is not in the DOCX
ok 156 - fails safely when a requested source block is not in the DOCX
  ---
  duration_ms: 1.126117
  type: 'test'
  ...
# Subtest: accepts priority evidence that occurs in normalized JD text
ok 157 - accepts priority evidence that occurs in normalized JD text
  ---
  duration_ms: 3.463886
  type: 'test'
  ...
# Subtest: rejects an invented JD evidence excerpt
ok 158 - rejects an invented JD evidence excerpt
  ---
  duration_ms: 0.632756
  type: 'test'
  ...
# Subtest: normalization tolerates whitespace differences but not semantic invention
ok 159 - normalization tolerates whitespace differences but not semantic invention
  ---
  duration_ms: 0.398149
  type: 'test'
  ...
# Subtest: rejects prompt-injection-like text as hiring evidence even when it appears in the JD
ok 160 - rejects prompt-injection-like text as hiring evidence even when it appears in the JD
  ---
  duration_ms: 0.438279
  type: 'test'
  ...
# Subtest: selected-CV stage binding rejects a token issued for a different CV or source version
ok 161 - selected-CV stage binding rejects a token issued for a different CV or source version
  ---
  duration_ms: 0.488804
  type: 'test'
  ...
# Subtest: CV evidence must be an exact excerpt from the section it claims to come from
ok 162 - CV evidence must be an exact excerpt from the section it claims to come from
  ---
  duration_ms: 0.493921
  type: 'test'
  ...
# Subtest: deterministicTruthCheck rejects an invented metric
ok 163 - deterministicTruthCheck rejects an invented metric
  ---
  duration_ms: 0.805392
  type: 'test'
  ...
# Subtest: deterministicTruthCheck rejects evidence from the wrong role
ok 164 - deterministicTruthCheck rejects evidence from the wrong role
  ---
  duration_ms: 0.384349
  type: 'test'
  ...
# Subtest: deterministicTruthCheck rejects unknown evidence IDs
ok 165 - deterministicTruthCheck rejects unknown evidence IDs
  ---
  duration_ms: 0.542063
  type: 'test'
  ...
# Subtest: deterministicTruthCheck rejects evidence not grounded in the selected baseline CV
ok 166 - deterministicTruthCheck rejects evidence not grounded in the selected baseline CV
  ---
  duration_ms: 0.611815
  type: 'test'
  ...
# Subtest: Bug 6 JOE: specialist transferable context is a gap, never a strength
ok 167 - Bug 6 JOE: specialist transferable context is a gap, never a strength
  ---
  duration_ms: 2.640999
  type: 'test'
  ...
# Subtest: Bug 6 ABB: payroll-domain transferability cannot become payroll expertise
ok 168 - Bug 6 ABB: payroll-domain transferability cannot become payroll expertise
  ---
  duration_ms: 0.556564
  type: 'test'
  ...
# Subtest: Bug 6: TRANSFERABLE is limited scoring signal at 25 percent credit
ok 169 - Bug 6: TRANSFERABLE is limited scoring signal at 25 percent credit
  ---
  duration_ms: 0.372271
  type: 'test'
  ...
# Subtest: Bug 6: supporting-only breakdown category is N/A rather than 100 percent
ok 170 - Bug 6: supporting-only breakdown category is N/A rather than 100 percent
  ---
  duration_ms: 0.391018
  type: 'test'
  ...
# Subtest: Bug 6: evaluator prompt protects category boundaries and generic-vs-specialist splitting
ok 171 - Bug 6: evaluator prompt protects category boundaries and generic-vs-specialist splitting
  ---
  duration_ms: 0.505077
  type: 'test'
  ...
# Subtest: Bug 6: Expertise Match uses fresh v3 cache namespace
ok 172 - Bug 6: Expertise Match uses fresh v3 cache namespace
  ---
  duration_ms: 0.250561
  type: 'test'
  ...
# Subtest: passes Source CV and structured requirements to a dedicated semantic evaluation stage
ok 173 - passes Source CV and structured requirements to a dedicated semantic evaluation stage
  ---
  duration_ms: 1.836568
  type: 'test'
  ...
# Subtest: rejects invented CV evidence instead of allowing the evaluator to hallucinate fit
ok 174 - rejects invented CV evidence instead of allowing the evaluator to hallucinate fit
  ---
  duration_ms: 1.89878
  type: 'test'
  ...
# Subtest: Root layout loads isolated Expertise Match loader animation styles
ok 175 - Root layout loads isolated Expertise Match loader animation styles
  ---
  duration_ms: 1.250591
  type: 'test'
  ...
# Subtest: Expertise loader animates three dots only while analysis is loading
ok 176 - Expertise loader animates three dots only while analysis is loading
  ---
  duration_ms: 0.278332
  type: 'test'
  ...
# Subtest: expertise-match API delegates JD interpretation and deterministic CV comparison to the expertise service
ok 177 - expertise-match API delegates JD interpretation and deterministic CV comparison to the expertise service
  ---
  duration_ms: 0.942675
  type: 'test'
  ...
# Subtest: expertise-match API does not modify or call LinkedIn search code
ok 178 - expertise-match API does not modify or call LinkedIn search code
  ---
  duration_ms: 0.138545
  type: 'test'
  ...
# Subtest: expertise-match API surfaces only safe actionable AI failure categories
ok 179 - expertise-match API surfaces only safe actionable AI failure categories
  ---
  duration_ms: 0.424508
  type: 'test'
  ...
# Subtest: cache key is scoped by LinkedIn job id and CV source version
ok 180 - cache key is scoped by LinkedIn job id and CV source version
  ---
  duration_ms: 1.140548
  type: 'test'
  ...
# Subtest: Bug 6 uses a fresh v3 cache namespace
ok 181 - Bug 6 uses a fresh v3 cache namespace
  ---
  duration_ms: 0.293574
  type: 'test'
  ...
# Subtest: saved Expertise Match is reused for the same job id and CV source version
ok 182 - saved Expertise Match is reused for the same job id and CV source version
  ---
  duration_ms: 1.465409
  type: 'test'
  ...
# Subtest: cached result is not reused after CV source version changes
ok 183 - cached result is not reused after CV source version changes
  ---
  duration_ms: 0.263831
  type: 'test'
  ...
# Subtest: requestExpertiseMatch sends selected full JD and active Source CV to the dedicated endpoint
ok 184 - requestExpertiseMatch sends selected full JD and active Source CV to the dedicated endpoint
  ---
  duration_ms: 2.051877
  type: 'test'
  ...
# Subtest: requestExpertiseMatch surfaces safe server errors
ok 185 - requestExpertiseMatch surfaces safe server errors
  ---
  duration_ms: 0.889817
  type: 'test'
  ...
# Subtest: Expertise Match is not auto-requested from a useEffect
ok 186 - Expertise Match is not auto-requested from a useEffect
  ---
  duration_ms: 2.243711
  type: 'test'
  ...
# Subtest: Expertise Match has an explicit Run Expertise Match button
ok 187 - Expertise Match has an explicit Run Expertise Match button
  ---
  duration_ms: 0.40546
  type: 'test'
  ...
# Subtest: manual Expertise Match flow reads cache before calling API and writes successful result
ok 188 - manual Expertise Match flow reads cache before calling API and writes successful result
  ---
  duration_ms: 0.257942
  type: 'test'
  ...
# Subtest: classifies direct CV evidence as MATCHED and transferable evidence only as PARTIAL
ok 189 - classifies direct CV evidence as MATCHED and transferable evidence only as PARTIAL
  ---
  duration_ms: 2.820174
  type: 'test'
  ...
# Subtest: a minimum-years requirement is not fully matched unless duration is evidenced for the same capability
ok 190 - a minimum-years requirement is not fully matched unless duration is evidenced for the same capability
  ---
  duration_ms: 6.1481
  type: 'test'
  ...
# Subtest: calculates Expertise Match only from professional requirements and returns the five approved breakdown dimensions
ok 191 - calculates Expertise Match only from professional requirements and returns the five approved breakdown dimensions
  ---
  duration_ms: 1.981833
  type: 'test'
  ...
# Subtest: critical missing expertise has more scoring impact than an otherwise identical supporting gap
ok 192 - critical missing expertise has more scoring impact than an otherwise identical supporting gap
  ---
  duration_ms: 0.60791
  type: 'test'
  ...
# Subtest: Why you fit contains strongest evidenced capabilities and Expertise gaps prioritise critical missing evidence
ok 193 - Why you fit contains strongest evidenced capabilities and Expertise gaps prioritise critical missing evidence
  ---
  duration_ms: 1.014622
  type: 'test'
  ...
# Subtest: compound OR requirement matches when any acceptable alternative is evidenced
ok 194 - compound OR requirement matches when any acceptable alternative is evidenced
  ---
  duration_ms: 1.037346
  type: 'test'
  ...
# Subtest: compound AND requirement gives PARTIAL when Source CV evidences some but not all required capability groups
ok 195 - compound AND requirement gives PARTIAL when Source CV evidences some but not all required capability groups
  ---
  duration_ms: 0.617033
  type: 'test'
  ...
# Subtest: relevant business or technical education recognizes documented business degrees
ok 196 - relevant business or technical education recognizes documented business degrees
  ---
  duration_ms: 0.697092
  type: 'test'
  ...
# Subtest: Novo-style AI Product Manager fixture credits real delivery, data, leadership and education while preserving AI gaps
ok 197 - Novo-style AI Product Manager fixture credits real delivery, data, leadership and education while preserving AI gaps
  ---
  duration_ms: 8.139948
  type: 'test'
  ...
# Subtest: minimum years can be evidenced from dated CV employment sections containing the matched capability
ok 198 - minimum years can be evidenced from dated CV employment sections containing the matched capability
  ---
  duration_ms: 3.639987
  type: 'test'
  ...
# Subtest: evaluates JD and Source CV in one AI call and returns deterministic-score inputs
ok 199 - evaluates JD and Source CV in one AI call and returns deterministic-score inputs
  ---
  duration_ms: 3.288237
  type: 'test'
  ...
# Subtest: prompt requires atomic split of generic delivery from specialist campaign or production context
ok 200 - prompt requires atomic split of generic delivery from specialist campaign or production context
  ---
  duration_ms: 0.384979
  type: 'test'
  ...
# Subtest: rejects invented CV evidence
ok 201 - rejects invented CV evidence
  ---
  duration_ms: 0.789228
  type: 'test'
  ...
# Subtest: rejects requirement evidence not grounded in the JD
ok 202 - rejects requirement evidence not grounded in the JD
  ---
  duration_ms: 0.397358
  type: 'test'
  ...
# Subtest: allows harmless punctuation differences in verbatim CV evidence
ok 203 - allows harmless punctuation differences in verbatim CV evidence
  ---
  duration_ms: 0.306724
  type: 'test'
  ...
# Subtest: extracts grounded structured professional requirements using injected AI only for JD interpretation
ok 204 - extracts grounded structured professional requirements using injected AI only for JD interpretation
  ---
  duration_ms: 4.548903
  type: 'test'
  ...
# Subtest: rejects AI requirements whose quoted evidence is not present in the JD
ok 205 - rejects AI requirements whose quoted evidence is not present in the JD
  ---
  duration_ms: 1.400393
  type: 'test'
  ...
# Subtest: rejects invented minimum years when the grounded JD evidence does not state that duration
ok 206 - rejects invented minimum years when the grounded JD evidence does not state that duration
  ---
  duration_ms: 0.404939
  type: 'test'
  ...
# Subtest: accepts all five approved expertise categories and three importance levels
ok 207 - accepts all five approved expertise categories and three importance levels
  ---
  duration_ms: 0.353874
  type: 'test'
  ...
# Subtest: Expertise requirement extraction gets a dedicated larger output-token budget
ok 208 - Expertise requirement extraction gets a dedicated larger output-token budget
  ---
  duration_ms: 0.383398
  type: 'test'
  ...
# Subtest: semantic MATCHED, TRANSFERABLE, PARTIAL and NOT_EVIDENCED receive conservative deterministic credits
ok 209 - semantic MATCHED, TRANSFERABLE, PARTIAL and NOT_EVIDENCED receive conservative deterministic credits
  ---
  duration_ms: 1.737252
  type: 'test'
  ...
# Subtest: TRANSFERABLE is a conservative expertise gap and never a visible strength
ok 210 - TRANSFERABLE is a conservative expertise gap and never a visible strength
  ---
  duration_ms: 1.28394
  type: 'test'
  ...
# Subtest: Annapurna-style evidence stays high while specialist M&A context remains explicit as a gap
ok 211 - Annapurna-style evidence stays high while specialist M&A context remains explicit as a gap
  ---
  duration_ms: 0.703391
  type: 'test'
  ...
# Subtest: uses exactly one semantic AI stage and deterministic code for final percentage
ok 212 - uses exactly one semantic AI stage and deterministic code for final percentage
  ---
  duration_ms: 2.97254
  type: 'test'
  ...
# Subtest: vacancy list renders fit labels instead of percentages
ok 213 - vacancy list renders fit labels instead of percentages
  ---
  duration_ms: 1.036254
  type: 'test'
  ...
# Subtest: maps displayed fit score to High Medium Low thresholds
ok 214 - maps displayed fit score to High Medium Low thresholds
  ---
  duration_ms: 0.752935
  type: 'test'
  ...
# Subtest: Help opens in a separate tab from the authenticated app utility navigation
ok 215 - Help opens in a separate tab from the authenticated app utility navigation
  ---
  duration_ms: 6.094611
  type: 'test'
  ...
# Subtest: Help page covers the complete MVP user flow and current screenshot
not ok 216 - Help page covers the complete MVP user flow and current screenshot
  ---
  duration_ms: 3.964588
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/help-page.contract.test.mjs:17:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /\/help\/applypilot-live-results\.jpg/. Input:
    
    "import styles from './help.module.css'\n" +
      '\n' +
      'export const metadata={\n' +
      "  title:'Help · ApplyPilot',\n" +
      "  description:'ApplyPilot user manual and quick start guide'\n" +
      '}\n' +
      '\n' +
      'const quickSteps=[\n' +
      "  ['1','Load your CVs','Open Search Profile and add up to three ready CVs to the CV Library.'],\n" +
      "  ['2','Build the Search Profile','Review primary roles, adjacent roles, locations, work models and exclusions, then save the profile.'],\n" +
      "  ['3','Choose freshness','Select 1, 3, 7 or 14 days under Posted Within.'],\n" +
      "  ['4','Search LinkedIn','Run the search. ApplyPilot reads public LinkedIn vacancies and returns the worthwhile matches it found.'],\n" +
      "  ['5','Review the shortlist','Use Filters, status controls and the vacancy panel to decide which roles deserve attention.'],\n" +
      "  ['6','Compare CV and job','Run Expertise Match, then Find best CV when more than one CV is ready.'],\n" +
      "  ['7','Adapt and export','Run CV Adaptation, review every suggested change, pass Truth Guard and download the tailored DOCX.']\n" +
      ']\n' +
      '\n' +
      'const sections=[\n' +
      '  {\n' +
      "    id:'search-profile',\n" +
      "    kicker:'SETUP',\n" +
      "    title:'Search Profile',\n" +
      "    body:'The Search Profile tells ApplyPilot what to search for. It is the source of truth for role directions and job-search preferences.',\n" +
      '    bullets:[\n' +
      "      'Primary roles are your main search directions.',\n" +
      "      'Adjacent roles expand discovery without replacing the primary roles.',\n" +
      "      'Locations and work models define where you are willing to work.',\n" +
      "      'Exclusions help remove clearly unwanted job families or patterns.',\n" +
      "      'Save the profile before running a new search so the current choices are used.'\n" +
      '    ]\n' +
      '  },\n' +
      '  {\n' +
      "    id:'cv-library',\n" +
      "    kicker:'SETUP',\n" +
      "    title:'CV Library',\n" +
      "    body:'The CV Library holds the source CVs ApplyPilot is allowed to use. A source CV stays unchanged; vacancy-specific versions are derived from it.',\n" +
      '    bullets:[\n' +
      "      'You can keep up to three CVs ready.',\n" +
      "      'Each CV is parsed and kept as its own source. CVs are never merged.',\n" +
      "      'The primary CV supports profile building and remains a separate source document.',\n" +
      "      'Replacing a CV creates a new source version so stale adaptation results are not reused.'\n" +
      '    ]\n' +
      '  },\n' +
      '  {\n' +
      "    id:'search-filters',\n" +
      "    kicker:'SEARCH',\n" +
      "    title:'Search & Filters',\n" +
      "    body:'Posted Within controls the LinkedIn freshness window. Filters work on the result list after the search has returned jobs.',\n" +
      '    bullets:[\n' +
      "      'Choose 1, 3, 7 or 14 days, then press Search LinkedIn.',\n" +
      "      'The search summary shows jobs discovered, full JDs read and worthwhile jobs evaluated.',\n" +
      "      'Use Search Areas and Work Model filters to narrow the current result list.',\n" +
      "      'All filters restores the complete current shortlist. Filters do not rewrite the Search Profile.'\n" +
      '    ]\n' +
      '  },\n' +
      '  {\n' +
      "    id:'vacancy-review',\n" +
      "    kicker:'REVIEW',\n" +
      "    title:'Vacancy review',\n" +
      "    body:'Select a vacancy in Live matches to open its review panel. The left list is for triage; the right panel is for evidence and action.',\n" +
      '    bullets:[\n' +
      "      'High, Medium and Low describe search relevance for the returned vacancy.',\n" +
      "      'Area, employment type and work model are shown separately from professional expertise.',\n" +
      "      'Open LinkedIn vacancy takes you to the public job page.',\n" +
      "      'Use Status to keep track of your own decision or application state.'\n" +
      '    ]\n' +
      '  },\n' +
      '  {\n' +
      "    id:'expertise-match',\n" +
      "    kicker:'COMPARE',\n" +
      "    title:'Expertise Match',\n" +
      "    body:'Expertise Match compares the full job description with the selected source CV for professional evidence only.',\n" +
      '    bullets:[\n' +
      "      'Location, salary, commute, work model and posting date are not part of the expertise score.',\n" +
      "      'Matched means the Source CV contains supporting evidence.',\n" +
      "      'Partial means related evidence exists but does not fully cover the requirement.',\n" +
      "      'Not evidenced means the requirement is not supported by the Source CV; it does not claim that the candidate lacks the skill.'\n" +
      '    ]\n' +
      '  },\n' +
      '  {\n' +
      "    id:'best-cv',\n" +
      "    kicker:'COMPARE',\n" +
      "    title:'Best CV',\n" +
      "    body:'Find best CV compares the ready CVs for the selected vacancy and recommends the strongest source for adaptation.',\n" +
      '    bullets:[\n' +
      "      'The comparison uses each CV independently.',\n" +
      "      'No content is copied from one CV into another.',\n" +
      "      'You can keep the recommended CV or deliberately choose another ready CV before adaptation.'\n" +
      '    ]\n' +
      '  },\n' +
      '  {\n' +
      "    id:'adaptation',\n" +
      "    kicker:'TAILOR',\n" +
      "    title:'CV Adaptation',\n" +
      "    body:'CV Adaptation creates a vacancy-specific derived version. It does not edit the stored Source CV.',\n" +
      '    bullets:[\n' +
      "      'Only the approved adaptation blocks are changed: Professional Summary and the overviews of the two most recent roles.',\n" +
      "      'Review every proposed change against the original text.',\n" +
      "      'Choose Accept or Keep for every block. Keep leaves the Source CV wording unchanged.',\n" +
      "      'You may edit an AI suggestion before accepting it, but the final text still has to pass evidence checks.'\n" +
      '    ]\n' +
      '  },\n' +
      '  {\n' +
      "    id:'truth-guard',\n" +
      "    kicker:'SAFETY',\n" +
      "    title:'Truth Guard',\n" +
      "    body:'Truth Guard is the factual gate between AI suggestions and a ready tailored CV.',\n" +
      '    bullets:[\n' +
      "      'Unsupported metrics, achievements or responsibilities must not be introduced.',\n" +
      "      'Evidence for a role must come from that role when adapting a role overview.',\n" +
      "      'A CV is only ready when the selected job, CV and source version still match and all review decisions are complete.'\n" +
      '    ]\n' +
      '  },\n' +
      '  {\n' +
      "    id:'download',\n" +
      "    kicker:'EXPORT',\n" +
      "    title:'Download tailored CV',\n" +
      "    body:'When the adaptation is ready, attach the matching source DOCX and let ApplyPilot create the vacancy-specific document.',\n" +
      '    bullets:[\n' +
      "      'Use the DOCX that corresponds to the selected Source CV.',\n" +
      "      'Only accepted replacements are written into the export.',\n" +
      "      'The original source file remains unchanged.'\n" +
      '    ]\n' +
      '  }\n' +
      ']\n' +
      '\n' +
      'export default function HelpPage(){\n' +
      '  return <main className={styles.page}>\n' +
      '    <header className={styles.hero}>\n' +
      '      <p className={styles.eyebrow}>APPLYPILOT · HELP</p>\n' +
      '      <h1>From search profile to tailored CV.</h1>\n' +
      '      <p className={styles.lede}>A practical user manual for the complete ApplyPilot MVP flow. Start with Quick start, then use the detailed sections when you need them.</p>\n' +
      '      <nav className={styles.jump} aria-label="Help sections">\n' +
      '        <a href="#quick-start">Quick start</a>\n' +
      '        <a href="#search-profile">Search Profile</a>\n' +
      '        <a href="#search-filters">Search & Filters</a>\n' +
      '        <a href="#vacancy-review">Vacancy review</a>\n' +
      '        <a href="#adaptation">CV Adaptation</a>\n' +
      '        <a href="#troubleshooting">Troubleshooting</a>\n' +
      '      </nav>\n' +
      '    </header>\n' +
      '\n' +
      '    <section id="quick-start" className={styles.section}>\n' +
      '      <div className={styles.sectionHead}><p className={styles.eyebrow}>START HERE</p><h2>Quick start</h2><p>Seven steps cover the normal end-to-end workflow.</p></div>\n' +
      '      <div className={styles.steps}>{quickSteps.map(([number,title,text])=><article className={styles.step} key={number}><span>{number}</span><div><h3>{title}</h3><p>{text}</p></div></article>)}</div>\n' +
      '    </section>\n' +
      '\n' +
      '    <section className={styles.screenshotSection} aria-labelledby="results-example">\n' +
      '      <div className={styles.sectionHead}><p className={styles.eyebrow}>CURRENT MVP</p><h2 id="results-example">Search results and vacancy review</h2><p>The shortlist stays on the left; the selected vacancy, conditions, Expertise Match and CV actions stay on the right.</p></div>\n' +
      '      <figure className={styles.figure}>\n' +
      '        <img src="/help/applypilot-live-results.webp" alt="ApplyPilot Live matches list and selected vacancy review panel"/>\n' +
      '        <figcaption>Example of the current V16 result-review workspace. Additional screenshots can be added to this Help page as the walkthrough evolves.</figcaption>\n' +
      '      </figure>\n' +
      '    </section>\n' +
      '\n' +
      '    <section className={styles.manualGrid} aria-label="Detailed user manual">\n' +
      '      {sections.map(section=><article id={section.id} className={styles.card} key={section.id}>\n' +
      '        <p className={styles.eyebrow}>{section.kicker}</p>\n' +
      '        <h2>{section.title}</h2>\n' +
      '        <p>{section.body}</p>\n' +
      '        <ul>{section.bullets.map(item=><li key={item}>{item}</li>)}</ul>\n' +
      '      </article>)}\n' +
      '    </section>\n' +
      '\n' +
      '    <section id="environments" className={styles.section}>\n' +
      '      <div className={styles.sectionHead}><p className={styles.eyebrow}>ENVIRONMENTS</p><h2>LIVE vs TEST</h2><p>The application behavior is the same product flow, but authentication and release purpose are different.</p></div>\n' +
      '      <div className={styles.environmentGrid}>\n' +
      '        <article><strong>LIVE</strong><h3>Stable product</h3><p>Use LIVE for the real production experience. Authentication is enabled and sign-in uses the invite-only email magic-link flow.</p></article>\n' +
      '        <article><strong>TEST</strong><h3>Working preview</h3><p>Use TEST for tuning and verification before the next release. Preview authentication is bypassed so you can open the app directly.</p></article>\n' +
      '        <article><strong>BASELINE</strong><h3>Frozen comparison</h3><p>The old pre-MVP LIVE is kept only as a visual and behavioral reference. Do not develop new features there.</p></article>\n' +
      '      </div>\n' +
      '    </section>\n' +
      '\n' +
      '    <section id="troubleshooting" className={styles.section}>\n' +
      '      <div className={styles.sectionHead}><p className={styles.eyebrow}>WHEN SOMETHING LOOKS WRONG</p><h2>Troubleshooting</h2></div>\n' +
      '      <div className={styles.troubleshooting}>\n' +
      '        <article><h3>LIVE sends me to Sign in</h3><p>That is expected when there is no valid production session. Request a new magic link and open it in the same browser.</p></article>\n' +
      '        <article><h3>TEST asks for a magic link</h3><p>Make sure you are using the TEST preview URL rather than the production LIVE URL.</p></article>\n' +
      '        <article><h3>No jobs appear</h3><p>Check that a Search Profile is saved, a CV is ready, the freshness window is appropriate and the current filters are not hiding all results.</p></article>\n' +
      '        <arti'... 744 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import styles from './help.module.css'
    
    export const metadata={
      title:'Help · ApplyPilot',
      description:'ApplyPilot user manual and quick start guide'
    }
    
    const quickSteps=[
      ['1','Load your CVs','Open Search Profile and add up to three ready CVs to the CV Library.'],
      ['2','Build the Search Profile','Review primary roles, adjacent roles, locations, work models and exclusions, then save the profile.'],
      ['3','Choose freshness','Select 1, 3, 7 or 14 days under Posted Within.'],
      ['4','Search LinkedIn','Run the search. ApplyPilot reads public LinkedIn vacancies and returns the worthwhile matches it found.'],
      ['5','Review the shortlist','Use Filters, status controls and the vacancy panel to decide which roles deserve attention.'],
      ['6','Compare CV and job','Run Expertise Match, then Find best CV when more than one CV is ready.'],
      ['7','Adapt and export','Run CV Adaptation, review every suggested change, pass Truth Guard and download the tailored DOCX.']
    ]
    
    const sections=[
      {
        id:'search-profile',
        kicker:'SETUP',
        title:'Search Profile',
        body:'The Search Profile tells ApplyPilot what to search for. It is the source of truth for role directions and job-search preferences.',
        bullets:[
          'Primary roles are your main search directions.',
          'Adjacent roles expand discovery without replacing the primary roles.',
          'Locations and work models define where you are willing to work.',
          'Exclusions help remove clearly unwanted job families or patterns.',
          'Save the profile before running a new search so the current choices are used.'
        ]
      },
      {
        id:'cv-library',
        kicker:'SETUP',
        title:'CV Library',
        body:'The CV Library holds the source CVs ApplyPilot is allowed to use. A source CV stays unchanged; vacancy-specific versions are derived from it.',
        bullets:[
          'You can keep up to three CVs ready.',
          'Each CV is parsed and kept as its own source. CVs are never merged.',
          'The primary CV supports profile building and remains a separate source document.',
          'Replacing a CV creates a new source version so stale adaptation results are not reused.'
        ]
      },
      {
        id:'search-filters',
        kicker:'SEARCH',
        title:'Search & Filters',
        body:'Posted Within controls the LinkedIn freshness window. Filters work on the result list after the search has returned jobs.',
        bullets:[
          'Choose 1, 3, 7 or 14 days, then press Search LinkedIn.',
          'The search summary shows jobs discovered, full JDs read and worthwhile jobs evaluated.',
          'Use Search Areas and Work Model filters to narrow the current result list.',
          'All filters restores the complete current shortlist. Filters do not rewrite the Search Profile.'
        ]
      },
      {
        id:'vacancy-review',
        kicker:'REVIEW',
        title:'Vacancy review',
        body:'Select a vacancy in Live matches to open its review panel. The left list is for triage; the right panel is for evidence and action.',
        bullets:[
          'High, Medium and Low describe search relevance for the returned vacancy.',
          'Area, employment type and work model are shown separately from professional expertise.',
          'Open LinkedIn vacancy takes you to the public job page.',
          'Use Status to keep track of your own decision or application state.'
        ]
      },
      {
        id:'expertise-match',
        kicker:'COMPARE',
        title:'Expertise Match',
        body:'Expertise Match compares the full job description with the selected source CV for professional evidence only.',
        bullets:[
          'Location, salary, commute, work model and posting date are not part of the expertise score.',
          'Matched means the Source CV contains supporting evidence.',
          'Partial means related evidence exists but does not fully cover the requirement.',
          'Not evidenced means the requirement is not supported by the Source CV; it does not claim that the candidate lacks the skill.'
        ]
      },
      {
        id:'best-cv',
        kicker:'COMPARE',
        title:'Best CV',
        body:'Find best CV compares the ready CVs for the selected vacancy and recommends the strongest source for adaptation.',
        bullets:[
          'The comparison uses each CV independently.',
          'No content is copied from one CV into another.',
          'You can keep the recommended CV or deliberately choose another ready CV before adaptation.'
        ]
      },
      {
        id:'adaptation',
        kicker:'TAILOR',
        title:'CV Adaptation',
        body:'CV Adaptation creates a vacancy-specific derived version. It does not edit the stored Source CV.',
        bullets:[
          'Only the approved adaptation blocks are changed: Professional Summary and the overviews of the two most recent roles.',
          'Review every proposed change against the original text.',
          'Choose Accept or Keep for every block. Keep leaves the Source CV wording unchanged.',
          'You may edit an AI suggestion before accepting it, but the final text still has to pass evidence checks.'
        ]
      },
      {
        id:'truth-guard',
        kicker:'SAFETY',
        title:'Truth Guard',
        body:'Truth Guard is the factual gate between AI suggestions and a ready tailored CV.',
        bullets:[
          'Unsupported metrics, achievements or responsibilities must not be introduced.',
          'Evidence for a role must come from that role when adapting a role overview.',
          'A CV is only ready when the selected job, CV and source version still match and all review decisions are complete.'
        ]
      },
      {
        id:'download',
        kicker:'EXPORT',
        title:'Download tailored CV',
        body:'When the adaptation is ready, attach the matching source DOCX and let ApplyPilot create the vacancy-specific document.',
        bullets:[
          'Use the DOCX that corresponds to the selected Source CV.',
          'Only accepted replacements are written into the export.',
          'The original source file remains unchanged.'
        ]
      }
    ]
    
    export default function HelpPage(){
      return <main className={styles.page}>
        <header className={styles.hero}>
          <p className={styles.eyebrow}>APPLYPILOT · HELP</p>
          <h1>From search profile to tailored CV.</h1>
          <p className={styles.lede}>A practical user manual for the complete ApplyPilot MVP flow. Start with Quick start, then use the detailed sections when you need them.</p>
          <nav className={styles.jump} aria-label="Help sections">
            <a href="#quick-start">Quick start</a>
            <a href="#search-profile">Search Profile</a>
            <a href="#search-filters">Search & Filters</a>
            <a href="#vacancy-review">Vacancy review</a>
            <a href="#adaptation">CV Adaptation</a>
            <a href="#troubleshooting">Troubleshooting</a>
          </nav>
        </header>
    
        <section id="quick-start" className={styles.section}>
          <div className={styles.sectionHead}><p className={styles.eyebrow}>START HERE</p><h2>Quick start</h2><p>Seven steps cover the normal end-to-end workflow.</p></div>
          <div className={styles.steps}>{quickSteps.map(([number,title,text])=><article className={styles.step} key={number}><span>{number}</span><div><h3>{title}</h3><p>{text}</p></div></article>)}</div>
        </section>
    
        <section className={styles.screenshotSection} aria-labelledby="results-example">
          <div className={styles.sectionHead}><p className={styles.eyebrow}>CURRENT MVP</p><h2 id="results-example">Search results and vacancy review</h2><p>The shortlist stays on the left; the selected vacancy, conditions, Expertise Match and CV actions stay on the right.</p></div>
          <figure className={styles.figure}>
            <img src="/help/applypilot-live-results.webp" alt="ApplyPilot Live matches list and selected vacancy review panel"/>
            <figcaption>Example of the current V16 result-review workspace. Additional screenshots can be added to this Help page as the walkthrough evolves.</figcaption>
          </figure>
        </section>
    
        <section className={styles.manualGrid} aria-label="Detailed user manual">
          {sections.map(section=><article id={section.id} className={styles.card} key={section.id}>
            <p className={styles.eyebrow}>{section.kicker}</p>
            <h2>{section.title}</h2>
            <p>{section.body}</p>
            <ul>{section.bullets.map(item=><li key={item}>{item}</li>)}</ul>
          </article>)}
        </section>
    
        <section id="environments" className={styles.section}>
          <div className={styles.sectionHead}><p className={styles.eyebrow}>ENVIRONMENTS</p><h2>LIVE vs TEST</h2><p>The application behavior is the same product flow, but authentication and release purpose are different.</p></div>
          <div className={styles.environmentGrid}>
            <article><strong>LIVE</strong><h3>Stable product</h3><p>Use LIVE for the real production experience. Authentication is enabled and sign-in uses the invite-only email magic-link flow.</p></article>
            <article><strong>TEST</strong><h3>Working preview</h3><p>Use TEST for tuning and verification before the next release. Preview authentication is bypassed so you can open the app directly.</p></article>
            <article><strong>BASELINE</strong><h3>Frozen comparison</h3><p>The old pre-MVP LIVE is kept only as a visual and behavioral reference. Do not develop new features there.</p></article>
          </div>
        </section>
    
        <section id="troubleshooting" className={styles.section}>
          <div className={styles.sectionHead}><p className={styles.eyebrow}>WHEN SOMETHING LOOKS WRONG</p><h2>Troubleshooting</h2></div>
          <div className={styles.troubleshooting}>
            <article><h3>LIVE sends me to Sign in</h3><p>That is expected when there is no valid production session. Request a new magic link and open it in the same browser.</p></article>
            <article><h3>TEST asks for a magic link</h3><p>Make sure you are using the TEST preview URL rather than the production LIVE URL.</p></article>
            <article><h3>No jobs appear</h3><p>Check that a Search Profile is saved, a CV is ready, the freshness window is appropriate and the current filters are not hiding all results.</p></article>
            <article><h3>Expertise Match or adaptation is stale</h3><p>Re-select the vacancy and source CV. Replacing a source CV creates a new version and old derived results must not be reused.</p></article>
            <article><h3>Tailored DOCX will not export</h3><p>Confirm that all adaptation decisions are complete and that the attached DOCX matches the selected Source CV.</p></article>
            <article><h3>A suggestion is not supported by the CV</h3><p>Do not accept it. Keep the original wording or edit the suggestion so it is fully supported by the Source CV.</p></article>
          </div>
        </section>
    
        <footer className={styles.footer}><a href="/">← Back to ApplyPilot</a><span>ApplyPilot · Search less. Apply better.</span></footer>
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/help-page.contract.test.mjs:22:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: requestJobAnalysis sends active Source CV version plus selected full JD to analyze_job
ok 217 - requestJobAnalysis sends active Source CV version plus selected full JD to analyze_job
  ---
  duration_ms: 2.029403
  type: 'test'
  ...
# Subtest: requestJobAnalysis surfaces safe API errors
ok 218 - requestJobAnalysis surfaces safe API errors
  ---
  duration_ms: 0.710111
  type: 'test'
  ...
# Subtest: Adapt & review CV runs direct selected-CV adaptation only on explicit user action
ok 219 - Adapt & review CV runs direct selected-CV adaptation only on explicit user action
  ---
  duration_ms: 2.805542
  type: 'test'
  ...
# Subtest: obsolete JD ANALYSIS PRETEST dashboard is removed from M4.11 review
ok 220 - obsolete JD ANALYSIS PRETEST dashboard is removed from M4.11 review
  ---
  duration_ms: 0.397398
  type: 'test'
  ...
# Subtest: M4 review exposes all three AI-updated CV blocks instead of Summary-only pretest UI
ok 221 - M4 review exposes all three AI-updated CV blocks instead of Summary-only pretest UI
  ---
  duration_ms: 0.260526
  type: 'test'
  ...
# Subtest: JD is sent with the selected CV and qualification detail is not duplicated in review UI
ok 222 - JD is sent with the selected CV and qualification detail is not duplicated in review UI
  ---
  duration_ms: 0.208638
  type: 'test'
  ...
# Subtest: JD analysis cache is scoped by job id and CV source version
ok 223 - JD analysis cache is scoped by job id and CV source version
  ---
  duration_ms: 1.033079
  type: 'test'
  ...
# Subtest: JD analysis cache restores analysis and token
ok 224 - JD analysis cache restores analysis and token
  ---
  duration_ms: 1.292032
  type: 'test'
  ...
# Subtest: JD analysis cache is not reused for a new CV version
ok 225 - JD analysis cache is not reused for a new CV version
  ---
  duration_ms: 0.24969
  type: 'test'
  ...
# Subtest: Area is scored only against explicit preferred locations
ok 226 - Area is scored only against explicit preferred locations
  ---
  duration_ms: 20.610294
  type: 'test'
  ...
# Subtest: Salary remains factual information and no longer depends on a hidden profile floor
ok 227 - Salary remains factual information and no longer depends on a hidden profile floor
  ---
  duration_ms: 0.402776
  type: 'test'
  ...
# Subtest: Employment and work-model scores are N/A until explicit preferences exist
ok 228 - Employment and work-model scores are N/A until explicit preferences exist
  ---
  duration_ms: 0.259994
  type: 'test'
  ...
# Subtest: Employment and work-model become deterministic binary preference matches when explicit preferences are supplied
ok 229 - Employment and work-model become deterministic binary preference matches when explicit preferences are supplied
  ---
  duration_ms: 1.976856
  type: 'test'
  ...
# Subtest: maps representative Danish locations into agreed search areas
ok 230 - maps representative Danish locations into agreed search areas
  ---
  duration_ms: 1.24368
  type: 'test'
  ...
# Subtest: classifies existing filter buckets without changing search data
ok 231 - classifies existing filter buckets without changing search data
  ---
  duration_ms: 1.415495
  type: 'test'
  ...
# Subtest: exposes three work models plus two remote scopes in UI order
ok 232 - exposes three work models plus two remote scopes in UI order
  ---
  duration_ms: 0.868305
  type: 'test'
  ...
# Subtest: Remote is a separate work-model switch and remote scope ignores Search Areas
ok 233 - Remote is a separate work-model switch and remote scope ignores Search Areas
  ---
  duration_ms: 0.741052
  type: 'test'
  ...
# Subtest: all selected is a no-op for the existing pool, including unclassified jobs
ok 234 - all selected is a no-op for the existing pool, including unclassified jobs
  ---
  duration_ms: 0.196571
  type: 'test'
  ...
# Subtest: status filters expose no status, considering, applied and ignored in UI order
ok 235 - status filters expose no status, considering, applied and ignored in UI order
  ---
  duration_ms: 0.26361
  type: 'test'
  ...
# Subtest: status filter defaults to no status, considering and applied while hiding ignored
ok 236 - status filter defaults to no status, considering and applied while hiding ignored
  ---
  duration_ms: 0.369477
  type: 'test'
  ...
# Subtest: status filter can isolate applied and explicitly include ignored
ok 237 - status filter can isolate applied and explicitly include ignored
  ---
  duration_ms: 0.284572
  type: 'test'
  ...
# Subtest: legacy ignored helper still preserves the old default behavior
ok 238 - legacy ignored helper still preserves the old default behavior
  ---
  duration_ms: 0.569213
  type: 'test'
  ...
# Subtest: manual job statuses are local informational metadata with three explicit states
ok 239 - manual job statuses are local informational metadata with three explicit states
  ---
  duration_ms: 0.988073
  type: 'test'
  ...
# Subtest: manual job statuses never enter Search and only filter the post-search list
ok 240 - manual job statuses never enter Search and only filter the post-search list
  ---
  duration_ms: 0.347924
  type: 'test'
  ...
# Subtest: Area, Employment type and Work model render directly under the vacancy header before Best CV
ok 241 - Area, Employment type and Work model render directly under the vacancy header before Best CV
  ---
  duration_ms: 0.18934
  type: 'test'
  ...
# Subtest: All filters is the first filter row and controls only Search Areas and Work Model
ok 242 - All filters is the first filter row and controls only Search Areas and Work Model
  ---
  duration_ms: 0.218643
  type: 'test'
  ...
# Subtest: Status is a third independent filter group and replaces Show ignored
ok 243 - Status is a third independent filter group and replaces Show ignored
  ---
  duration_ms: 0.219144
  type: 'test'
  ...
# Subtest: keeps clear IT delivery and IT project leadership roles
ok 244 - keeps clear IT delivery and IT project leadership roles
  ---
  duration_ms: 3.928534
  type: 'test'
  ...
# Subtest: keeps generic Project Manager only when JD confirms technology context
ok 245 - keeps generic Project Manager only when JD confirms technology context
  ---
  duration_ms: 2.404649
  type: 'test'
  ...
# Subtest: rejects construction project roles
ok 246 - rejects construction project roles
  ---
  duration_ms: 0.25708
  type: 'test'
  ...
# Subtest: rejects academic and health research roles even when project words appear
ok 247 - rejects academic and health research roles even when project words appear
  ---
  duration_ms: 0.239164
  type: 'test'
  ...
# Subtest: rejects admin and hands-on specialist false positives
ok 248 - rejects admin and hands-on specialist false positives
  ---
  duration_ms: 0.312223
  type: 'test'
  ...
# Subtest: v2 requires multiple concrete IT delivery signals for generic project titles
ok 249 - v2 requires multiple concrete IT delivery signals for generic project titles
  ---
  duration_ms: 0.451798
  type: 'test'
  ...
# Subtest: v2 trusts explicit technology delivery titles without weakening generic-title gate
ok 250 - v2 trusts explicit technology delivery titles without weakening generic-title gate
  ---
  duration_ms: 0.222259
  type: 'test'
  ...
# Subtest: requestLatestRoleOverview continues the selected-CV chain after Professional Summary
ok 251 - requestLatestRoleOverview continues the selected-CV chain after Professional Summary
  ---
  duration_ms: 6.165064
  type: 'test'
  ...
# Subtest: requestLatestRoleOverview fails safely if the latest-role stage is rejected
ok 252 - requestLatestRoleOverview fails safely if the latest-role stage is rejected
  ---
  duration_ms: 1.43943
  type: 'test'
  ...
# Subtest: latest role writer contract accepts grounded claims
ok 253 - latest role writer contract accepts grounded claims
  ---
  duration_ms: 2.538577
  type: 'test'
  ...
# Subtest: latest role writer contract rejects claims without evidence IDs
ok 254 - latest role writer contract rejects claims without evidence IDs
  ---
  duration_ms: 0.826213
  type: 'test'
  ...
# Subtest: writeLatestRoleOverview uses only evidence from the detected latest employment section
ok 255 - writeLatestRoleOverview uses only evidence from the detected latest employment section
  ---
  duration_ms: 11.807396
  type: 'test'
  ...
# Subtest: writeLatestRoleOverview rejects evidence IDs from another employment role
ok 256 - writeLatestRoleOverview rejects evidence IDs from another employment role
  ---
  duration_ms: 0.941854
  type: 'test'
  ...
# Subtest: writeLatestRoleOverview rejects numbers absent from latest-role evidence
ok 257 - writeLatestRoleOverview rejects numbers absent from latest-role evidence
  ---
  duration_ms: 0.723521
  type: 'test'
  ...
# Subtest: writeLatestRoleOverview enforces the existing role overview length window
ok 258 - writeLatestRoleOverview enforces the existing role overview length window
  ---
  duration_ms: 0.738984
  type: 'test'
  ...
# Subtest: writeLatestRoleOverview returns unavailable without inventing content when latest overview is missing
ok 259 - writeLatestRoleOverview returns unavailable without inventing content when latest overview is missing
  ---
  duration_ms: 0.604264
  type: 'test'
  ...
# Subtest: 14-day discovery strategy contains repeated 7-day collection plus a 14-day extension
ok 260 - 14-day discovery strategy contains repeated 7-day collection plus a 14-day extension
  ---
  duration_ms: 2.862677
  type: 'test'
  ...
# Subtest: stable search uses repeated discovery passes and unions job IDs before detail fetches
ok 261 - stable search uses repeated discovery passes and unions job IDs before detail fetches
  ---
  duration_ms: 7.281767
  type: 'test'
  ...
# Subtest: multi-pass discovery unions reshuffled LinkedIn pages until a clean pass adds no new job IDs
ok 262 - multi-pass discovery unions reshuffled LinkedIn pages until a clean pass adds no new job IDs
  ---
  duration_ms: 2.545488
  type: 'test'
  ...
# Subtest: a failed discovery request cannot falsely mark a pass stable
ok 263 - a failed discovery request cannot falsely mark a pass stable
  ---
  duration_ms: 0.658525
  type: 'test'
  ...
# Subtest: 7-day strategy repeats deep discovery passes so page shuffling can be unioned
ok 264 - 7-day strategy repeats deep discovery passes so page shuffling can be unioned
  ---
  duration_ms: 1.921644
  type: 'test'
  ...
# Subtest: 14-day strategy repeats the recent 7-day slice then extends coverage to 14 days without exceeding V4 search-request count
ok 265 - 14-day strategy repeats the recent 7-day slice then extends coverage to 14 days without exceeding V4 search-request count
  ---
  duration_ms: 0.464117
  type: 'test'
  ...
# Subtest: 7-day multi-pass strategy stays within an 80-request discovery ceiling for ten queries
ok 266 - 7-day multi-pass strategy stays within an 80-request discovery ceiling for ten queries
  ---
  duration_ms: 0.316539
  type: 'test'
  ...
# Subtest: master pool cache is isolated by Search Profile fingerprint
ok 267 - master pool cache is isolated by Search Profile fingerprint
  ---
  duration_ms: 2.181508
  type: 'test'
  ...
# Subtest: master pool cache safely returns empty for missing or invalid data
ok 268 - master pool cache safely returns empty for missing or invalid data
  ---
  duration_ms: 0.239335
  type: 'test'
  ...
# Subtest: master pool snapshot persists verified jobs together with discovery candidates
ok 269 - master pool snapshot persists verified jobs together with discovery candidates
  ---
  duration_ms: 0.453952
  type: 'test'
  ...
# Subtest: fresh-cache helper only allows short local view reuse
ok 270 - fresh-cache helper only allows short local view reuse
  ---
  duration_ms: 0.255178
  type: 'test'
  ...
# Subtest: stable search uses multi-pass union discovery instead of a one-shot discovery plan
ok 271 - stable search uses multi-pass union discovery instead of a one-shot discovery plan
  ---
  duration_ms: 5.423156
  type: 'test'
  ...
# Subtest: stable search exposes discovery pass diagnostics so live testing can tell whether LinkedIn actually converged
ok 272 - stable search exposes discovery pass diagnostics so live testing can tell whether LinkedIn actually converged
  ---
  duration_ms: 1.946722
  type: 'test'
  ...
# Subtest: 7-day strategy includes deeper LinkedIn offsets and 14-day strategy keeps a bounded second page
ok 273 - 7-day strategy includes deeper LinkedIn offsets and 14-day strategy keeps a bounded second page
  ---
  duration_ms: 2.921013
  type: 'test'
  ...
# Subtest: stable search sends each discovery-pass start offset to LinkedIn instead of hard-coding start=0
ok 274 - stable search sends each discovery-pass start offset to LinkedIn instead of hard-coding start=0
  ---
  duration_ms: 6.187017
  type: 'test'
  ...
# Subtest: realistic DHI MUSE JD is rejected because the primary delivery object is a physical offshore monitoring system
ok 275 - realistic DHI MUSE JD is rejected because the primary delivery object is a physical offshore monitoring system
  ---
  duration_ms: 6.976255
  type: 'test'
  ...
# Subtest: data-centre built-environment PM is rejected explicitly as physical delivery, not mistaken for IT infrastructure
ok 276 - data-centre built-environment PM is rejected explicitly as physical delivery, not mistaken for IT infrastructure
  ---
  duration_ms: 4.034922
  type: 'test'
  ...
# Subtest: IT infrastructure PM remains eligible when enterprise technology infrastructure is the primary delivery object
ok 277 - IT infrastructure PM remains eligible when enterprise technology infrastructure is the primary delivery object
  ---
  duration_ms: 0.322877
  type: 'test'
  ...
# Subtest: enterprise software platform implementation PM remains eligible
ok 278 - enterprise software platform implementation PM remains eligible
  ---
  duration_ms: 0.250421
  type: 'test'
  ...
# Subtest: Software Developer profile keeps a Software Engineer vacancy and rejects an unrelated PM returned by LinkedIn
ok 279 - Software Developer profile keeps a Software Engineer vacancy and rejects an unrelated PM returned by LinkedIn
  ---
  duration_ms: 16.538898
  type: 'test'
  ...
# Subtest: QA/Test profile recognizes Test Manager as the same professional direction
ok 280 - QA/Test profile recognizes Test Manager as the same professional direction
  ---
  duration_ms: 1.73695
  type: 'test'
  ...
# Subtest: existing IT Project Manager profile still keeps a credible PM baseline vacancy
ok 281 - existing IT Project Manager profile still keeps a credible PM baseline vacancy
  ---
  duration_ms: 1.113908
  type: 'test'
  ...
# Subtest: deterministic company exclusion rejects an otherwise relevant profile match
ok 282 - deterministic company exclusion rejects an otherwise relevant profile match
  ---
  duration_ms: 1.742709
  type: 'test'
  ...
# Subtest: profile-driven search returns legacy-compatible result shape and full-JD statistics
ok 283 - profile-driven search returns legacy-compatible result shape and full-JD statistics
  ---
  duration_ms: 23.062764
  type: 'test'
  ...
# Subtest: 7-day profile discovery uses repeated deep LinkedIn pages instead of shadow start=0 only
not ok 284 - 7-day profile discovery uses repeated deep LinkedIn pages instead of shadow start=0 only
  ---
  duration_ms: 7.106848
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/linkedin-profile-search.test.mjs:82:1'
  failureType: 'testCodeFailure'
  error: |-
    The expression evaluated to a falsy value:
    
      assert.ok(searchStarts.includes(25))
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/linkedin-profile-search.test.mjs:92:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: role evaluation uses the full approved Search Profile, not only the discovery path
ok 285 - role evaluation uses the full approved Search Profile, not only the discovery path
  ---
  duration_ms: 1.785152
  type: 'test'
  ...
# Subtest: same verified job and Search Profile keep the same role decision and score across different discovery paths
ok 286 - same verified job and Search Profile keep the same role decision and score across different discovery paths
  ---
  duration_ms: 3.196902
  type: 'test'
  ...
# Subtest: each freshness view performs fresh LinkedIn discovery for its selected window
ok 287 - each freshness view performs fresh LinkedIn discovery for its selected window
  ---
  duration_ms: 4.613858
  type: 'test'
  ...
# Subtest: 1/3/7/14-day views are local subsets over the same discovered jobs
ok 288 - 1/3/7/14-day views are local subsets over the same discovered jobs
  ---
  duration_ms: 7.996185
  type: 'test'
  ...
# Subtest: previous in-window candidate remains in the 14-day master pool when LinkedIn omits it on refresh
ok 289 - previous in-window candidate remains in the 14-day master pool when LinkedIn omits it on refresh
  ---
  duration_ms: 0.910487
  type: 'test'
  ...
# Subtest: master pool drops a previous candidate once its known posting date is outside 14 days
ok 290 - master pool drops a previous candidate once its known posting date is outside 14 days
  ---
  duration_ms: 0.35825
  type: 'test'
  ...
# Subtest: cached verified JD is reused without another LinkedIn detail request
ok 291 - cached verified JD is reused without another LinkedIn detail request
  ---
  duration_ms: 0.518017
  type: 'test'
  ...
# Subtest: fresh discovery always runs while cached verified JDs avoid repeat detail fetches
ok 292 - fresh discovery always runs while cached verified JDs avoid repeat detail fetches
  ---
  duration_ms: 0.700326
  type: 'test'
  ...
# Subtest: scoring calibration keeps broad recall but reserves 9+ for strong direct role evidence
ok 293 - scoring calibration keeps broad recall but reserves 9+ for strong direct role evidence
  ---
  duration_ms: 0.896096
  type: 'test'
  ...
# Subtest: adjacent exact role stays visible but does not automatically outrank a strong primary match
ok 294 - adjacent exact role stays visible but does not automatically outrank a strong primary match
  ---
  duration_ms: 0.742519
  type: 'test'
  ...
# Subtest: exact Technical Project Manager in a non-enterprise engineering context is demoted but kept
ok 295 - exact Technical Project Manager in a non-enterprise engineering context is demoted but kept
  ---
  duration_ms: 1.665275
  type: 'test'
  ...
# Subtest: exact Technical Project Manager with strong enterprise IT context remains High
not ok 296 - exact Technical Project Manager with strong enterprise IT context remains High
  ---
  duration_ms: 1.735869
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/linkedin-profile-search.test.mjs:391:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    
    0 !== 0.2
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 0.2
  actual: 0
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/linkedin-profile-search.test.mjs:404:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: specialist adjacent Implementation Manager remains visible but payroll context lowers top-score inflation
ok 297 - specialist adjacent Implementation Manager remains visible but payroll context lowers top-score inflation
  ---
  duration_ms: 1.471698
  type: 'test'
  ...
# Subtest: generic hands-on engineer titles are rejected before JD fetch
ok 298 - generic hands-on engineer titles are rejected before JD fetch
  ---
  duration_ms: 4.683161
  type: 'test'
  ...
# Subtest: director and stream-owner career tracks are rejected before JD fetch
ok 299 - director and stream-owner career tracks are rejected before JD fetch
  ---
  duration_ms: 0.544456
  type: 'test'
  ...
# Subtest: people-manager hybrid title is rejected even though it contains Project Manager
ok 300 - people-manager hybrid title is rejected even though it contains Project Manager
  ---
  duration_ms: 0.254797
  type: 'test'
  ...
# Subtest: explicit ERP-specialist project-manager titles are rejected
ok 301 - explicit ERP-specialist project-manager titles are rejected
  ---
  duration_ms: 0.226035
  type: 'test'
  ...
# Subtest: generic Project Manager is rejected when technology words belong to a physical/scientific product domain rather than enterprise IT
ok 302 - generic Project Manager is rejected when technology words belong to a physical/scientific product domain rather than enterprise IT
  ---
  duration_ms: 10.073429
  type: 'test'
  ...
# Subtest: industrial company Project Manager still passes when the JD clearly owns corporate enterprise IT transformation
ok 303 - industrial company Project Manager still passes when the JD clearly owns corporate enterprise IT transformation
  ---
  duration_ms: 7.325111
  type: 'test'
  ...
# Subtest: Business Application Manager is rejected for BAU application ownership but can pass for genuine transformation delivery
ok 304 - Business Application Manager is rejected for BAU application ownership but can pass for genuine transformation delivery
  ---
  duration_ms: 0.841735
  type: 'test'
  ...
# Subtest: stable search applies a title role gate before JD fetch and a full role gate before scoring
ok 305 - stable search applies a title role gate before JD fetch and a full role gate before scoring
  ---
  duration_ms: 4.546088
  type: 'test'
  ...
# Subtest: obvious non-project/delivery professions are rejected by title before JD fetching
ok 306 - obvious non-project/delivery professions are rejected by title before JD fetching
  ---
  duration_ms: 5.118907
  type: 'test'
  ...
# Subtest: clear project/delivery role families remain eligible for JD verification
ok 307 - clear project/delivery role families remain eligible for JD verification
  ---
  duration_ms: 1.086599
  type: 'test'
  ...
# Subtest: project-like titles in explicitly excluded content, NPD and hardware domains are rejected
ok 308 - project-like titles in explicitly excluded content, NPD and hardware domains are rejected
  ---
  duration_ms: 0.242869
  type: 'test'
  ...
# Subtest: borderline manager/specialist titles stay ambiguous rather than being thrown away by title alone
ok 309 - borderline manager/specialist titles stay ambiguous rather than being thrown away by title alone
  ---
  duration_ms: 0.421503
  type: 'test'
  ...
# Subtest: clear target title passes only when the JD confirms technology delivery ownership
ok 310 - clear target title passes only when the JD confirms technology delivery ownership
  ---
  duration_ms: 15.089182
  type: 'test'
  ...
# Subtest: ambiguous titles need stronger JD proof than clear project/delivery titles
ok 311 - ambiguous titles need stronger JD proof than clear project/delivery titles
  ---
  duration_ms: 0.435024
  type: 'test'
  ...
# Subtest: explicit non-target profession stays rejected even when its JD contains generic delivery language
ok 312 - explicit non-target profession stays rejected even when its JD contains generic delivery language
  ---
  duration_ms: 0.288807
  type: 'test'
  ...
# Subtest: borderline technology manager without real project delivery ownership is rejected
ok 313 - borderline technology manager without real project delivery ownership is rejected
  ---
  duration_ms: 0.25633
  type: 'test'
  ...
# Subtest: engineering context does not override an explicit project/program delivery profession
ok 314 - engineering context does not override an explicit project/program delivery profession
  ---
  duration_ms: 0.496004
  type: 'test'
  ...
# Subtest: Danish plural projektledere is classified as a target project-management title
ok 315 - Danish plural projektledere is classified as a target project-management title
  ---
  duration_ms: 1.566278
  type: 'test'
  ...
# Subtest: target Delivery Manager reaches scoring with credible delivery ownership even when JD uses weak IT vocabulary
ok 316 - target Delivery Manager reaches scoring with credible delivery ownership even when JD uses weak IT vocabulary
  ---
  duration_ms: 7.406612
  type: 'test'
  ...
# Subtest: target Project Manager reaches scoring when delivery is clear even without an explicit enterprise-IT anchor
ok 317 - target Project Manager reaches scoring when delivery is clear even without an explicit enterprise-IT anchor
  ---
  duration_ms: 3.21623
  type: 'test'
  ...
# Subtest: Service & Project Delivery Manager reaches scoring instead of being rejected only for missing IT keywords
ok 318 - Service & Project Delivery Manager reaches scoring instead of being rejected only for missing IT keywords
  ---
  duration_ms: 0.426952
  type: 'test'
  ...
# Subtest: recall relaxation does not rescue built-environment project management
ok 319 - recall relaxation does not rescue built-environment project management
  ---
  duration_ms: 0.584079
  type: 'test'
  ...
# Subtest: recall relaxation does not rescue engineering people-lead titles
ok 320 - recall relaxation does not rescue engineering people-lead titles
  ---
  duration_ms: 1.154599
  type: 'test'
  ...
# Subtest: exact target PM title is deferred to evaluation even when role-gate keyword evidence is sparse
ok 321 - exact target PM title is deferred to evaluation even when role-gate keyword evidence is sparse
  ---
  duration_ms: 0.287265
  type: 'test'
  ...
# Subtest: salary is informational only for permanent Search evaluation
ok 322 - salary is informational only for permanent Search evaluation
  ---
  duration_ms: 19.338611
  type: 'test'
  ...
# Subtest: salary is informational only for contract Search evaluation
ok 323 - salary is informational only for contract Search evaluation
  ---
  duration_ms: 1.640358
  type: 'test'
  ...
# Subtest: audit record preserves exact and expanded discovery provenance
ok 324 - audit record preserves exact and expanded discovery provenance
  ---
  duration_ms: 1.570143
  type: 'test'
  ...
# Subtest: stable search exposes an audit terminal stage for every existing rejection/keep branch
ok 325 - stable search exposes an audit terminal stage for every existing rejection/keep branch
  ---
  duration_ms: 5.813273
  type: 'test'
  ...
# Subtest: audit helper allow-list cannot copy CV or JD source payloads
ok 326 - audit helper allow-list cannot copy CV or JD source payloads
  ---
  duration_ms: 1.810629
  type: 'test'
  ...
# Subtest: creates a privacy-safe discovery audit record
not ok 327 - creates a privacy-safe discovery audit record
  ---
  duration_ms: 3.551706
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/linkedin-search-audit.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly deep-equal:
    + actual - expected
    
      {
        company: 'Example',
        decision: 'PENDING',
    +   discoveryProvenance: [],
        jobId: '123',
        reason: null,
        score: null,
        stage: 'DISCOVERED',
        title: 'Senior Project Manager'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
    jobId: '123'
    title: 'Senior Project Manager'
    company: 'Example'
    stage: 'DISCOVERED'
    decision: 'PENDING'
    reason: ~
    score: ~
  actual:
    jobId: '123'
    title: 'Senior Project Manager'
    company: 'Example'
    stage: 'DISCOVERED'
    decision: 'PENDING'
    reason: ~
    score: ~
    discoveryProvenance:
  operator: 'deepStrictEqual'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/linkedin-search-audit.test.mjs:7:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: updates one audit record without leaking arbitrary source fields
not ok 328 - updates one audit record without leaking arbitrary source fields
  ---
  duration_ms: 0.868255
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/linkedin-search-audit.test.mjs:13:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly deep-equal:
    + actual - expected
    
      [
        {
          company: 'Co',
          decision: 'REJECT',
    +     discoveryProvenance: [],
          jobId: '123',
          reason: 'Poor fit',
          score: 5.8,
          stage: 'BELOW_60',
          title: 'PM'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
    0:
      jobId: '123'
      title: 'PM'
      company: 'Co'
      stage: 'BELOW_60'
      decision: 'REJECT'
      reason: 'Poor fit'
      score: 5.8
  actual:
    0:
      jobId: '123'
      title: 'PM'
      company: 'Co'
      stage: 'BELOW_60'
      decision: 'REJECT'
      reason: 'Poor fit'
      score: 5.8
      discoveryProvenance:
  operator: 'deepStrictEqual'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/linkedin-search-audit.test.mjs:16:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: root layout loads the shared ApplyPilot spinner styles
ok 329 - root layout loads the shared ApplyPilot spinner styles
  ---
  duration_ms: 6.184113
  type: 'test'
  ...
# Subtest: LinkedIn loading state reuses the double mint spinner in Live matches
ok 330 - LinkedIn loading state reuses the double mint spinner in Live matches
  ---
  duration_ms: 1.602933
  type: 'test'
  ...
# Subtest: LinkedIn search route allows the full five-minute Hobby Fluid Compute window
ok 331 - LinkedIn search route allows the full five-minute Hobby Fluid Compute window
  ---
  duration_ms: 6.514501
  type: 'test'
  ...
# Subtest: LinkedIn search route uses the stable fetcher for discovery and job details
ok 332 - LinkedIn search route uses the stable fetcher for discovery and job details
  ---
  duration_ms: 1.329698
  type: 'test'
  ...
# Subtest: stable fetcher retries HTTP 429 and honors Retry-After before succeeding
ok 333 - stable fetcher retries HTTP 429 and honors Retry-After before succeeding
  ---
  duration_ms: 33.608206
  type: 'test'
  ...
# Subtest: stable fetcher does not retry non-retryable HTTP errors
ok 334 - stable fetcher does not retry non-retryable HTTP errors
  ---
  duration_ms: 1.175299
  type: 'test'
  ...
# Subtest: stable fetcher limits concurrent LinkedIn requests
ok 335 - stable fetcher limits concurrent LinkedIn requests
  ---
  duration_ms: 9.138676
  type: 'test'
  ...
# Subtest: stable search returns every worthwhile match instead of silently capping results at ten
ok 336 - stable search returns every worthwhile match instead of silently capping results at ten
  ---
  duration_ms: 169.278869
  type: 'test'
  ...
# Subtest: stable search never labels a run complete when a job detail still failed
ok 337 - stable search never labels a run complete when a job detail still failed
  ---
  duration_ms: 3.816769
  type: 'test'
  ...
# Subtest: parses LinkedIn public search card
ok 338 - parses LinkedIn public search card
  ---
  duration_ms: 4.352211
  type: 'test'
  ...
# Subtest: parses full JD from JobPosting JSON-LD
ok 339 - parses full JD from JobPosting JSON-LD
  ---
  duration_ms: 6.710371
  type: 'test'
  ...
# Subtest: hybrid delivery methodology is not treated as hybrid work model
ok 340 - hybrid delivery methodology is not treated as hybrid work model
  ---
  duration_ms: 0.536453
  type: 'test'
  ...
# Subtest: Danish preferred is not a hard reject
ok 341 - Danish preferred is not a hard reject
  ---
  duration_ms: 8.813084
  type: 'test'
  ...
# Subtest: mandatory Danish is a hard reject
ok 342 - mandatory Danish is a hard reject
  ---
  duration_ms: 1.063363
  type: 'test'
  ...
# Subtest: construction PM is rejected even with generic PM language
ok 343 - construction PM is rejected even with generic PM language
  ---
  duration_ms: 1.624624
  type: 'test'
  ...
# Subtest: corporate IT in research-heavy company is not rejected as R&D
ok 344 - corporate IT in research-heavy company is not rejected as R&D
  ---
  duration_ms: 4.158344
  type: 'test'
  ...
# Subtest: remote Europe does not prove Denmark eligibility
ok 345 - remote Europe does not prove Denmark eligibility
  ---
  duration_ms: 3.370408
  type: 'test'
  ...
# Subtest: monthly DKK salary range is parsed conservatively
ok 346 - monthly DKK salary range is parsed conservatively
  ---
  duration_ms: 2.321195
  type: 'test'
  ...
# Subtest: expired explicit validThrough closes vacancy
ok 347 - expired explicit validThrough closes vacancy
  ---
  duration_ms: 0.997647
  type: 'test'
  ...
# Subtest: one-source E2E: LinkedIn guest search -> guest detail -> evaluator returns VELUX fixture
ok 348 - one-source E2E: LinkedIn guest search -> guest detail -> evaluator returns VELUX fixture
  ---
  duration_ms: 4.400584
  type: 'test'
  ...
# Subtest: experience score comes from the supplied Source CV text
ok 349 - experience score comes from the supplied Source CV text
  ---
  duration_ms: 4.800504
  type: 'test'
  ...
# Subtest: all LinkedIn search failures are surfaced, never fake zero results
ok 350 - all LinkedIn search failures are surfaced, never fake zero results
  ---
  duration_ms: 1.305532
  type: 'test'
  ...
# Subtest: job evaluation requires an uploaded Source CV instead of a built-in fallback
ok 351 - job evaluation requires an uploaded Source CV instead of a built-in fallback
  ---
  duration_ms: 0.671854
  type: 'test'
  ...
# Subtest: LinkedIn search requires Source CV text before discovery/evaluation starts
ok 352 - LinkedIn search requires Source CV text before discovery/evaluation starts
  ---
  duration_ms: 0.30411
  type: 'test'
  ...
# Subtest: expanded direction searches by query while preserving original source role and provenance
ok 353 - expanded direction searches by query while preserving original source role and provenance
  ---
  duration_ms: 6.010205
  type: 'test'
  ...
# Subtest: deduplicates one LinkedIn query shared by exact and expanded directions while preserving both source provenances
ok 354 - deduplicates one LinkedIn query shared by exact and expanded directions while preserving both source provenances
  ---
  duration_ms: 1.441193
  type: 'test'
  ...
# Subtest: issues exactly one start=0 Denmark search request per approved direction and never requests job detail
ok 355 - issues exactly one start=0 Denmark search request per approved direction and never requests job detail
  ---
  duration_ms: 7.134029
  type: 'test'
  ...
# Subtest: deduplicates jobs and aggregates every finding direction with provenance
ok 356 - deduplicates jobs and aggregates every finding direction with provenance
  ---
  duration_ms: 0.814506
  type: 'test'
  ...
# Subtest: preserves successful directions when another direction fails
ok 357 - preserves successful directions when another direction fails
  ---
  duration_ms: 0.698244
  type: 'test'
  ...
# Subtest: throws when every attempted direction request fails
ok 358 - throws when every attempted direction request fails
  ---
  duration_ms: 2.840384
  type: 'test'
  ...
# Subtest: empty plan returns a valid empty result without network calls
ok 359 - empty plan returns a valid empty result without network calls
  ---
  duration_ms: 0.349297
  type: 'test'
  ...
# Subtest: continues to the next LinkedIn discovery page so wider windows do not lose fresh jobs pushed off page 1
ok 360 - continues to the next LinkedIn discovery page so wider windows do not lose fresh jobs pushed off page 1
  ---
  duration_ms: 1.455824
  type: 'test'
  ...
# Subtest: duplicate-only intermediate page does not stop bounded paging early
ok 361 - duplicate-only intermediate page does not stop bounded paging early
  ---
  duration_ms: 1.854234
  type: 'test'
  ...
# Subtest: shadow route is authenticated, isolated from legacy engine, and accepts only shadow inputs
ok 362 - shadow route is authenticated, isolated from legacy engine, and accepts only shadow inputs
  ---
  duration_ms: 7.963186
  type: 'test'
  ...
# Subtest: default stable fetcher keeps retrying 429 beyond five attempts while budget remains
ok 363 - default stable fetcher keeps retrying 429 beyond five attempts while budget remains
  ---
  duration_ms: 34.949236
  type: 'test'
  ...
# Subtest: default LinkedIn pacing serializes requests to reduce rate-limit pressure
ok 364 - default LinkedIn pacing serializes requests to reduce rate-limit pressure
  ---
  duration_ms: 25.453772
  type: 'test'
  ...
# Subtest: semaphore does not exceed its configured concurrency when a 429 retry races queued requests
ok 365 - semaphore does not exceed its configured concurrency when a 429 retry races queued requests
  ---
  duration_ms: 17.928976
  type: 'test'
  ...
# Subtest: explicit Retry-After is honored before retrying
ok 366 - explicit Retry-After is honored before retrying
  ---
  duration_ms: 2.656932
  type: 'test'
  ...
# Subtest: non-retryable LinkedIn errors still fail immediately
ok 367 - non-retryable LinkedIn errors still fail immediately
  ---
  duration_ms: 0.917027
  type: 'test'
  ...
# Subtest: explicit concurrency greater than one is still respected exactly
ok 368 - explicit concurrency greater than one is still respected exactly
  ---
  duration_ms: 18.144945
  type: 'test'
  ...
# Subtest: Arup-style built-environment data-centre PM is rejected from realistic JD wording
ok 369 - Arup-style built-environment data-centre PM is rejected from realistic JD wording
  ---
  duration_ms: 11.054451
  type: 'test'
  ...
# Subtest: ANDRITZ-style plant and equipment PM is rejected from realistic JD wording
ok 370 - ANDRITZ-style plant and equipment PM is rejected from realistic JD wording
  ---
  duration_ms: 3.794526
  type: 'test'
  ...
# Subtest: hardware and R&D project manager is not hard-rejected when the profession is genuine project delivery
ok 371 - hardware and R&D project manager is not hard-rejected when the profession is genuine project delivery
  ---
  duration_ms: 0.388725
  type: 'test'
  ...
# Subtest: IT infrastructure PM stays eligible even when the programme includes physical data-centre locations
ok 372 - IT infrastructure PM stays eligible even when the programme includes physical data-centre locations
  ---
  duration_ms: 0.259895
  type: 'test'
  ...
# Subtest: engineering people-lead titles are rejected before JD evidence can rescue them
ok 373 - engineering people-lead titles are rejected before JD evidence can rescue them
  ---
  duration_ms: 0.406922
  type: 'test'
  ...
# Subtest: Arup-style built-environment cluster is rejected even without old exact hard-gate phrases
ok 374 - Arup-style built-environment cluster is rejected even without old exact hard-gate phrases
  ---
  duration_ms: 0.375837
  type: 'test'
  ...
# Subtest: engineering team-lead punctuation variants are also rejected before JD
ok 375 - engineering team-lead punctuation variants are also rejected before JD
  ---
  duration_ms: 0.262759
  type: 'test'
  ...
# Subtest: strong enterprise IT delivery object overrides incidental built-environment cluster
ok 376 - strong enterprise IT delivery object overrides incidental built-environment cluster
  ---
  duration_ms: 0.306694
  type: 'test'
  ...
# Subtest: built-environment disciplines count as independent cluster signals
ok 377 - built-environment disciplines count as independent cluster signals
  ---
  duration_ms: 0.649832
  type: 'test'
  ...
# Subtest: explicit Delivery Manager title is evaluated instead of hard-rejected when JD wording yields zero responsibility categories
ok 378 - explicit Delivery Manager title is evaluated instead of hard-rejected when JD wording yields zero responsibility categories
  ---
  duration_ms: 18.968624
  type: 'test'
  ...
# Subtest: ambiguous title with the same weak ownership evidence still receives the zero-ownership hard exclusion
ok 379 - ambiguous title with the same weak ownership evidence still receives the zero-ownership hard exclusion
  ---
  duration_ms: 3.194899
  type: 'test'
  ...
# Subtest: main UX polish has a dedicated final override stylesheet
ok 380 - main UX polish has a dedicated final override stylesheet
  ---
  duration_ms: 1.246265
  type: 'test'
  ...
# Subtest: CV recommendation and selection are presented as one workflow
ok 381 - CV recommendation and selection are presented as one workflow
  ---
  duration_ms: 0.196751
  type: 'test'
  ...
# Subtest: right panel follows evaluation then CV workflow then application pack
ok 382 - right panel follows evaluation then CV workflow then application pack
  ---
  duration_ms: 0.477727
  type: 'test'
  ...
# Subtest: primary-action hierarchy and generated state are visually explicit
ok 383 - primary-action hierarchy and generated state are visually explicit
  ---
  duration_ms: 0.368886
  type: 'test'
  ...
# Subtest: hero, application pack and job list receive compact professional polish
ok 384 - hero, application pack and job list receive compact professional polish
  ---
  duration_ms: 0.343849
  type: 'test'
  ...
# Subtest: Search Profile role step is library-aware rather than CV1-only
ok 385 - Search Profile role step is library-aware rather than CV1-only
  ---
  duration_ms: 2.944608
  type: 'test'
  ...
# Subtest: page builds Search Profile roles from ready CV library entries
ok 386 - page builds Search Profile roles from ready CV library entries
  ---
  duration_ms: 0.204593
  type: 'test'
  ...
# Subtest: LinkedIn Search request remains CV1 payload during Step 1
ok 387 - LinkedIn Search request remains CV1 payload during Step 1
  ---
  duration_ms: 0.213666
  type: 'test'
  ...
# Subtest: requestPreviousRoleOverview continues the selected-CV chain after latest role writing
ok 388 - requestPreviousRoleOverview continues the selected-CV chain after latest role writing
  ---
  duration_ms: 4.727687
  type: 'test'
  ...
# Subtest: requestPreviousRoleOverview fails safely if the previous-role stage is rejected
ok 389 - requestPreviousRoleOverview fails safely if the previous-role stage is rejected
  ---
  duration_ms: 1.009594
  type: 'test'
  ...
# Subtest: writePreviousRoleOverview uses only evidence from the detected previous employment section
not ok 390 - writePreviousRoleOverview uses only evidence from the detected previous employment section
  ---
  duration_ms: 6.973852
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:54:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:56:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: writePreviousRoleOverview rejects evidence IDs from the latest employment role
not ok 391 - writePreviousRoleOverview rejects evidence IDs from the latest employment role
  ---
  duration_ms: 0.554731
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:86:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:88:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: writePreviousRoleOverview rejects numbers absent from previous-role evidence
not ok 392 - writePreviousRoleOverview rejects numbers absent from previous-role evidence
  ---
  duration_ms: 0.45915
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:98:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:100:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: writePreviousRoleOverview enforces the existing previous-role overview length window
not ok 393 - writePreviousRoleOverview enforces the existing previous-role overview length window
  ---
  duration_ms: 2.063123
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:110:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:112:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: writePreviousRoleOverview returns unavailable when the selected CV has no previous role
not ok 394 - writePreviousRoleOverview returns unavailable when the selected CV has no previous role
  ---
  duration_ms: 0.586387
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:119:1'
  failureType: 'testCodeFailure'
  error: |-
    Expected values to be strictly equal:
    + actual - expected
    
    + 'undefined'
    - 'function'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: 'function'
  actual: 'undefined'
  operator: 'strictEqual'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/previous-role-overview.test.mjs:121:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: LIVE Search uses the profile-driven endpoint when a saved Union Search Plan exists
ok 395 - LIVE Search uses the profile-driven endpoint when a saved Union Search Plan exists
  ---
  duration_ms: 2.543735
  type: 'test'
  ...
# Subtest: LIVE Search keeps the frozen legacy endpoint as fallback
ok 396 - LIVE Search keeps the frozen legacy endpoint as fallback
  ---
  duration_ms: 0.218843
  type: 'test'
  ...
# Subtest: profile-driven LIVE endpoint exists separately from the frozen legacy endpoint
ok 397 - profile-driven LIVE endpoint exists separately from the frozen legacy endpoint
  ---
  duration_ms: 0.321446
  type: 'test'
  ...
# Subtest: 1 day means the current Denmark calendar day from 00:00, not yesterday
ok 398 - 1 day means the current Denmark calendar day from 00:00, not yesterday
  ---
  duration_ms: 26.187007
  type: 'test'
  ...
# Subtest: mergeProfile fills missing v0.4 search-profile fields without changing saved values
ok 399 - mergeProfile fills missing v0.4 search-profile fields without changing saved values
  ---
  duration_ms: 2.273946
  type: 'test'
  ...
# Subtest: resumeToProfile mirrors parsed master CV without changing search preferences
ok 400 - resumeToProfile mirrors parsed master CV without changing search preferences
  ---
  duration_ms: 0.374614
  type: 'test'
  ...
# Subtest: deriveReviewTerms uses live LinkedIn job text but does not mutate the search item
ok 401 - deriveReviewTerms uses live LinkedIn job text but does not mutate the search item
  ---
  duration_ms: 1.130303
  type: 'test'
  ...
# Subtest: buildReviewChanges only applies the approved conservative v0.4 rewrites
ok 402 - buildReviewChanges only applies the approved conservative v0.4 rewrites
  ---
  duration_ms: 3.001002
  type: 'test'
  ...
# Subtest: application pack becomes reviewable only when verified fact-bank evidence exists
ok 403 - application pack becomes reviewable only when verified fact-bank evidence exists
  ---
  duration_ms: 0.402115
  type: 'test'
  ...
# Subtest: live LinkedIn request uses freshness plus the active Source CV text, but not Search Profile data
ok 404 - live LinkedIn request uses freshness plus the active Source CV text, but not Search Profile data
  ---
  duration_ms: 1.624154
  type: 'test'
  ...
# Subtest: review helpers accept current live LinkedIn result shape
ok 405 - review helpers accept current live LinkedIn result shape
  ---
  duration_ms: 1.227397
  type: 'test'
  ...
# Subtest: merged UI restores Search Profile persistence without wiring it into LinkedIn search
not ok 406 - merged UI restores Search Profile persistence without wiring it into LinkedIn search
  ---
  duration_ms: 1.581921
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:77:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Step \{profileStep\} of 5/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:82:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: merged UI keeps Application Pack and exposes the direct M4 review flow
not ok 407 - merged UI keeps Application Pack and exposes the direct M4 review flow
  ---
  duration_ms: 1.603082
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:86:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Adapt & review CV/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:89:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: buildReviewChanges omits CV evidence when normalized original and updated text are the same
ok 408 - buildReviewChanges omits CV evidence when normalized original and updated text are the same
  ---
  duration_ms: 0.804631
  type: 'test'
  ...
# Subtest: M4 CV review shows a neutral empty state when AI returns no changed block
ok 409 - M4 CV review shows a neutral empty state when AI returns no changed block
  ---
  duration_ms: 0.647398
  type: 'test'
  ...
# Subtest: Source CV upload entry points keep the current five-step Search Profile flow
not ok 410 - Source CV upload entry points keep the current five-step Search Profile flow
  ---
  duration_ms: 1.674669
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:116:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Upload CV<\/button>/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:122:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: profile status requires a complete ready Source CV and search uses that Source CV
ok 411 - profile status requires a complete ready Source CV and search uses that Source CV
  ---
  duration_ms: 0.343027
  type: 'test'
  ...
# Subtest: Version 2 proposes a JD-specific safe rewrite when the Master CV already supports the requirement
ok 412 - Version 2 proposes a JD-specific safe rewrite when the Master CV already supports the requirement
  ---
  duration_ms: 1.049393
  type: 'test'
  ...
# Subtest: Version 2 Step 1.1 creates a recruiter-length Summary with professional identity first and no generated filler
ok 413 - Version 2 Step 1.1 creates a recruiter-length Summary with professional identity first and no generated filler
  ---
  duration_ms: 14.472079
  type: 'test'
  ...
# Subtest: Version 2 Step 1.1 de-emphasises less relevant Summary content instead of retaining every sentence
ok 414 - Version 2 Step 1.1 de-emphasises less relevant Summary content instead of retaining every sentence
  ---
  duration_ms: 1.127909
  type: 'test'
  ...
# Subtest: Version 2 Step 1 can read the Master CV summary from a complete saved preview
ok 415 - Version 2 Step 1 can read the Master CV summary from a complete saved preview
  ---
  duration_ms: 0.890077
  type: 'test'
  ...
# Subtest: M4 UI supersedes the legacy Summary-only review with three direct AI blocks
ok 416 - M4 UI supersedes the legacy Summary-only review with three direct AI blocks
  ---
  duration_ms: 0.577735
  type: 'test'
  ...
# Subtest: Version 2 Step 1 stops Professional Summary extraction before Core Competences
ok 417 - Version 2 Step 1 stops Professional Summary extraction before Core Competences
  ---
  duration_ms: 0.505128
  type: 'test'
  ...
# Subtest: Version 2 Step 1 refuses a truncated legacy preview instead of tailoring a partial Summary
ok 418 - Version 2 Step 1 refuses a truncated legacy preview instead of tailoring a partial Summary
  ---
  duration_ms: 0.184603
  type: 'test'
  ...
# Subtest: page promotes the saved Union Search Plan to LIVE while preserving legacy fallback payload
not ok 419 - page promotes the saved Union Search Plan to LIVE while preserving legacy fallback payload
  ---
  duration_ms: 9.155675
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-shadow-wiring.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /setJobs\(Array\.isArray\(data\.jobs\)\?data\.jobs:\[\]\)/. Input:
    
    'async function search(){\n' +
      '  if(!resumeLoaded){\n' +
      "    setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      '  if(!selectedSources.length){\n' +
      "    setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      "  setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0\n' +
      '  try{\n' +
      '    const tasks=[]\n' +
      '\n' +
      "    if(selectedSources.includes('linkedin')) tasks.push((async()=>{\n" +
      '      let res\n' +
      '      if(hasProfilePlan){\n' +
      '        const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint\n' +
      '        const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})\n' +
      "        res=await fetch('/api/linkedin-profile-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({\n' +
      '            freshnessDays,\n' +
      '            unionSearchPlan:profile.unionSearchPlan,\n' +
      '            exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '            previousCandidates:poolSnapshot.candidates,\n' +
      '            previousVerifiedJobs:poolSnapshot.verifiedJobs,\n' +
      '          }),\n' +
      '        })\n' +
      '      }else{\n' +
      "        res=await fetch('/api/linkedin-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),\n' +
      '        })\n' +
      '      }\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'LinkedIn search failed')\n" +
      '      if(hasProfilePlan&&Array.isArray(data.masterCandidates)){\n' +
      '        writeLinkedInMasterPool({\n' +
      '          storage:localStorage,\n' +
      '          fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,\n' +
      '          candidates:data.masterCandidates,\n' +
      '          verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],\n' +
      '        })\n' +
      '      }\n' +
      "      return {source:'linkedin',data}\n" +
      '    })())\n' +
      '\n' +
      "    if(selectedSources.includes('jobindex')) tasks.push((async()=>{\n" +
      "      if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')\n" +
      "      const res=await fetch('/api/jobindex-profile-search',{\n" +
      "        method:'POST',\n" +
      "        headers:{'Content-Type':'application/json'},\n" +
      '        body:JSON.stringify({\n' +
      '          freshnessDays,\n' +
      '          unionSearchPlan:profile.unionSearchPlan,\n' +
      '          exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '        }),\n' +
      '      })\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'Jobindex search failed')\n" +
      "      return {source:'jobindex',data}\n" +
      '    })())\n' +
      '\n' +
      "    if(selectedSources.includes('jobnet')) tasks.push((async()=>{\n" +
      "      if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')\n" +
      "      const res=await fetch('/api/jobnet-profile-search',{\n" +
      "        method:'POST',\n" +
      "        headers:{'Content-Type':'application/json'},\n" +
      '        body:JSON.stringify({\n' +
      '          freshnessDays,\n' +
      '          unionSearchPlan:profile.unionSearchPlan,\n' +
      '          exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '        }),\n' +
      '      })\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'Jobnet search failed')\n" +
      "      return {source:'jobnet',data}\n" +
      '    })())\n' +
      '\n' +
      '    const settled=await Promise.allSettled(tasks)\n' +
      "    const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)\n" +
      "    const failed=settled.filter(item=>item.status==='rejected')\n" +
      "    if(!successful.length) throw failed[0]?.reason||new Error('Search failed')\n" +
      '\n' +
      '    const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))\n' +
      '    setJobs(mergedJobs)\n' +
      '\n' +
      '    const stats={\n' +
      '      masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),\n' +
      '      fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),\n' +
      '      returned:mergedJobs.length,\n' +
      '    }\n' +
      '    const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))\n' +
      "    const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')\n" +
      '    setState({\n' +
      '      loading:false,\n' +
      "      error:'',\n" +
      "      coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},\n" +
      '      stats,\n' +
      '      fetchedAt:new Date().toISOString(),\n' +
      '      audit,\n' +
      '    })\n' +
      "  }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }\n" +
      '}'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-shadow-wiring.test.mjs:18:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: parse-cv endpoint accepts resume uploads instead of being retired
ok 420 - parse-cv endpoint accepts resume uploads instead of being retired
  ---
  duration_ms: 0.992208
  type: 'test'
  ...
# Subtest: Feature 2 UI persists the complete active Source CV and supports legacy migration
ok 421 - Feature 2 UI persists the complete active Source CV and supports legacy migration
  ---
  duration_ms: 0.275537
  type: 'test'
  ...
# Subtest: Feature 2 retains the extracted Professional Summary separately from the short preview
ok 422 - Feature 2 retains the extracted Professional Summary separately from the short preview
  ---
  duration_ms: 0.14873
  type: 'test'
  ...
# Subtest: right panel uses dedicated Expertise Match analysis and keeps the agreed vacancy header
not ok 423 - right panel uses dedicated Expertise Match analysis and keeps the agreed vacancy header
  ---
  duration_ms: 2.898861
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/right-panel-expertise-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Source: LinkedIn/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/right-panel-expertise-ui.contract.test.mjs:16:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: right panel renders the four smaller independent job-condition indicators and no Overall Match
ok 424 - right panel renders the four smaller independent job-condition indicators and no Overall Match
  ---
  duration_ms: 0.498478
  type: 'test'
  ...
# Subtest: right panel preserves Application Pack and gives Expertise Match stronger visual hierarchy
ok 425 - right panel preserves Application Pack and gives Expertise Match stronger visual hierarchy
  ---
  duration_ms: 0.252434
  type: 'test'
  ...
# Subtest: job conditions never alter Expertise Match
ok 426 - job conditions never alter Expertise Match
  ---
  duration_ms: 19.510737
  type: 'test'
  ...
# Subtest: page wires API audit rows into a closed-by-default Search Audit component
not ok 427 - page wires API audit rows into a closed-by-default Search Audit component
  ---
  duration_ms: 8.284871
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-audit-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /audit:Array\.isArray\(data\.audit\)\?data\.audit:\[\]/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/search-audit-ui.contract.test.mjs:14:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: validates primary and adjacent role suggestions
ok 428 - validates primary and adjacent role suggestions
  ---
  duration_ms: 1.7837
  type: 'test'
  ...
# Subtest: rejects empty primary roles
ok 429 - rejects empty primary roles
  ---
  duration_ms: 0.52702
  type: 'test'
  ...
# Subtest: builds roles from CV 1 in one structured AI call
ok 430 - builds roles from CV 1 in one structured AI call
  ---
  duration_ms: 1.733866
  type: 'test'
  ...
# Subtest: removes duplicate role names case-insensitively
ok 431 - removes duplicate role names case-insensitively
  ---
  duration_ms: 0.285071
  type: 'test'
  ...
# Subtest: cache key changes when CV sourceVersion changes
ok 432 - cache key changes when CV sourceVersion changes
  ---
  duration_ms: 1.163753
  type: 'test'
  ...
# Subtest: writes and reads generated roles for unchanged CV 1
ok 433 - writes and reads generated roles for unchanged CV 1
  ---
  duration_ms: 1.386852
  type: 'test'
  ...
# Subtest: keeps sibling CV role caches independent when one sourceVersion changes
ok 434 - keeps sibling CV role caches independent when one sourceVersion changes
  ---
  duration_ms: 1.598766
  type: 'test'
  ...
# Subtest: requests Search Profile roles from CV 1 only
ok 435 - requests Search Profile roles from CV 1 only
  ---
  duration_ms: 1.944177
  type: 'test'
  ...
# Subtest: requests structured exclusions only when caller explicitly invokes save processing
ok 436 - requests structured exclusions only when caller explicitly invokes save processing
  ---
  duration_ms: 0.545467
  type: 'test'
  ...
# Subtest: unchanged saved exclusions use zero AI calls
ok 437 - unchanged saved exclusions use zero AI calls
  ---
  duration_ms: 1.653487
  type: 'test'
  ...
# Subtest: changed exclusions call AI once and repeated text uses cache
ok 438 - changed exclusions call AI once and repeated text uses cache
  ---
  duration_ms: 2.01344
  type: 'test'
  ...
# Subtest: blank exclusions use zero AI calls
ok 439 - blank exclusions use zero AI calls
  ---
  duration_ms: 0.192335
  type: 'test'
  ...
# Subtest: custom exclusions are compiled only on Save profile and then passed to profile-driven LIVE Search
not ok 440 - custom exclusions are compiled only on Save profile and then passed to profile-driven LIVE Search
  ---
  duration_ms: 5.215199
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-exclusions-save-ui.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JSON\.stringify\(\{freshnessDays,unionSearchPlan:profile\.unionSearchPlan,exclusionRules:Array\.isArray\(profile\.exclusionRules\)\?profile\.exclusionRules:\[\]\}\)/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/search-profile-exclusions-save-ui.test.mjs:16:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: new unsaved profiles do not inherit another user’s exclusions
ok 441 - new unsaved profiles do not inherit another user’s exclusions
  ---
  duration_ms: 0.621149
  type: 'test'
  ...
# Subtest: parses arbitrary exclusions in one structured AI call
ok 442 - parses arbitrary exclusions in one structured AI call
  ---
  duration_ms: 1.237322
  type: 'test'
  ...
# Subtest: blank exclusions use zero AI calls
ok 443 - blank exclusions use zero AI calls
  ---
  duration_ms: 0.76983
  type: 'test'
  ...
# Subtest: validator keeps only valid unique structured rules
ok 444 - validator keeps only valid unique structured rules
  ---
  duration_ms: 0.177102
  type: 'test'
  ...
# Subtest: builds independent role profiles without consolidating professions
ok 445 - builds independent role profiles without consolidating professions
  ---
  duration_ms: 2.265758
  type: 'test'
  ...
# Subtest: combines primary roles from all CVs in stable slot order
ok 446 - combines primary roles from all CVs in stable slot order
  ---
  duration_ms: 0.651184
  type: 'test'
  ...
# Subtest: promotes a role to primary when any CV classifies it primary
ok 447 - promotes a role to primary when any CV classifies it primary
  ---
  duration_ms: 1.413743
  type: 'test'
  ...
# Subtest: deduplicates roles case and whitespace insensitively and records all supporting CVs
ok 448 - deduplicates roles case and whitespace insensitively and records all supporting CVs
  ---
  duration_ms: 0.787811
  type: 'test'
  ...
# Subtest: one-CV union preserves the CV role proposal semantics
ok 449 - one-CV union preserves the CV role proposal semantics
  ---
  duration_ms: 0.235128
  type: 'test'
  ...
# Subtest: library fingerprint is stable by slot and changes only with CV membership/sourceVersion or builder version
ok 450 - library fingerprint is stable by slot and changes only with CV membership/sourceVersion or builder version
  ---
  duration_ms: 0.348676
  type: 'test'
  ...
# Subtest: Search Profile wizard has five steps and no salary screen or salary scoring label
not ok 451 - Search Profile wizard has five steps and no salary screen or salary scoring label
  ---
  duration_ms: 4.159836
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-no-salary-ui.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /profileStep\/5\*100/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/search-profile-no-salary-ui.test.mjs:7:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: migrates legacy geography into separate locations and work models
ok 452 - migrates legacy geography into separate locations and work models
  ---
  duration_ms: 2.124294
  type: 'test'
  ...
# Subtest: preserves explicit structured preferences
ok 453 - preserves explicit structured preferences
  ---
  duration_ms: 0.227256
  type: 'test'
  ...
# Subtest: builds a backward-compatible geography array for current consumers
ok 454 - builds a backward-compatible geography array for current consumers
  ---
  duration_ms: 0.303129
  type: 'test'
  ...
# Subtest: Search Profile role editor preserves spaces while typing and normalizes only on blur
ok 455 - Search Profile role editor preserves spaces while typing and normalizes only on blur
  ---
  duration_ms: 0.978008
  type: 'test'
  ...
# Subtest: Search Profile step 2 combines all ready CVs while legacy Search keeps CV 1
ok 456 - Search Profile step 2 combines all ready CVs while legacy Search keeps CV 1
  ---
  duration_ms: 3.250601
  type: 'test'
  ...
# Subtest: Search Profile Step 3 separates location from work model
not ok 457 - Search Profile Step 3 separates location from work model
  ---
  duration_ms: 3.177042
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-step3-ui-contract.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /<SearchProfileLocationStep/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/search-profile-step3-ui-contract.test.mjs:20:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: normalizes, removes exact and duplicate queries, and caps each role at three
ok 458 - normalizes, removes exact and duplicate queries, and caps each role at three
  ---
  duration_ms: 2.580399
  type: 'test'
  ...
# Subtest: is domain agnostic and preserves creative role family output
ok 459 - is domain agnostic and preserves creative role family output
  ---
  duration_ms: 1.841786
  type: 'test'
  ...
# Subtest: builds expansions in one structured AI call
ok 460 - builds expansions in one structured AI call
  ---
  duration_ms: 0.783229
  type: 'test'
  ...
# Subtest: builds exact plus expanded directions without changing the source role
ok 461 - builds exact plus expanded directions without changing the source role
  ---
  duration_ms: 0.448844
  type: 'test'
  ...
# Subtest: falls back to exact-only discovery when expansion fails
ok 462 - falls back to exact-only discovery when expansion fails
  ---
  duration_ms: 0.502593
  type: 'test'
  ...
# Subtest: deterministic discovery expansion returns the same queries for the same roles on every call
ok 463 - deterministic discovery expansion returns the same queries for the same roles on every call
  ---
  duration_ms: 1.671424
  type: 'test'
  ...
# Subtest: default discovery search plan is deterministic and does not depend on AI output
ok 464 - default discovery search plan is deterministic and does not depend on AI output
  ---
  duration_ms: 0.775968
  type: 'test'
  ...
# Subtest: shadow audit is explicitly diagnostic and renders discovery delta provenance
ok 465 - shadow audit is explicitly diagnostic and renders discovery delta provenance
  ---
  duration_ms: 6.406342
  type: 'test'
  ...
# Subtest: compares against all legacy audit IDs and partitions new candidates without double counting
ok 466 - compares against all legacy audit IDs and partitions new candidates without double counting
  ---
  duration_ms: 1.706436
  type: 'test'
  ...
# Subtest: accepts legacy audit sourceJobId and ignores invalid IDs
ok 467 - accepts legacy audit sourceJobId and ignores invalid IDs
  ---
  duration_ms: 0.246015
  type: 'test'
  ...
# Subtest: Expertise Match, Find best CV and Generate CV update share the exact primary action style
ok 468 - Expertise Match, Find best CV and Generate CV update share the exact primary action style
  ---
  duration_ms: 0.861906
  type: 'test'
  ...
# Subtest: search engine has no hard-coded candidate CV fallback
ok 469 - search engine has no hard-coded candidate CV fallback
  ---
  duration_ms: 0.975494
  type: 'test'
  ...
# Subtest: LinkedIn API requires cvText and passes it into search evaluation
ok 470 - LinkedIn API requires cvText and passes it into search evaluation
  ---
  duration_ms: 0.159977
  type: 'test'
  ...
# Subtest: Search LinkedIn is blocked in the browser when Source CV is not ready
ok 471 - Search LinkedIn is blocked in the browser when Source CV is not ready
  ---
  duration_ms: 0.136522
  type: 'test'
  ...
# Subtest: browser sends the active uploaded Source CV text to LinkedIn evaluation
ok 472 - browser sends the active uploaded Source CV text to LinkedIn evaluation
  ---
  duration_ms: 0.164784
  type: 'test'
  ...
# Subtest: Source CV record retains complete extracted text and source identity metadata
ok 473 - Source CV record retains complete extracted text and source identity metadata
  ---
  duration_ms: 2.176031
  type: 'test'
  ...
# Subtest: Source CV readiness requires complete text and stable source identity
ok 474 - Source CV readiness requires complete text and stable source identity
  ---
  duration_ms: 0.298943
  type: 'test'
  ...
# Subtest: legacy stored CV without complete text is preserved only as needs-reupload, never ready
ok 475 - legacy stored CV without complete text is preserved only as needs-reupload, never ready
  ---
  duration_ms: 0.395956
  type: 'test'
  ...
# Subtest: stored complete Source CV normalizes to ready and keeps full cvText
ok 476 - stored complete Source CV normalizes to ready and keeps full cvText
  ---
  duration_ms: 0.261337
  type: 'test'
  ...
# Subtest: declares the approved Supabase auth dependencies
ok 477 - declares the approved Supabase auth dependencies
  ---
  duration_ms: 6.672957
  type: 'test'
  ...
# Subtest: returns the two approved public Supabase settings
ok 478 - returns the two approved public Supabase settings
  ---
  duration_ms: 2.125826
  type: 'test'
  ...
# Subtest: rejects missing Supabase URL
ok 479 - rejects missing Supabase URL
  ---
  duration_ms: 0.70294
  type: 'test'
  ...
# Subtest: rejects missing Supabase publishable key
ok 480 - rejects missing Supabase publishable key
  ---
  duration_ms: 0.374355
  type: 'test'
  ...
# Subtest: tailor-cv POST accepts selected CV plus JD through one direct adaptation action
ok 481 - tailor-cv POST accepts selected CV plus JD through one direct adaptation action
  ---
  duration_ms: 1.199896
  type: 'test'
  ...
# Subtest: tailor-cv route keeps GET retired and does not log CV or JD payloads
ok 482 - tailor-cv route keeps GET retired and does not log CV or JD payloads
  ---
  duration_ms: 0.333714
  type: 'test'
  ...
# Subtest: CV Library uses Word DOCX for source-file adaptation
ok 483 - CV Library uses Word DOCX for source-file adaptation
  ---
  duration_ms: 0.888394
  type: 'test'
  ...
# Subtest: source DOCX is kept only in session memory and can be re-uploaded
ok 484 - source DOCX is kept only in session memory and can be re-uploaded
  ---
  duration_ms: 0.240667
  type: 'test'
  ...
# Subtest: download is enabled only after review decisions and sends accepted blocks only
ok 485 - download is enabled only after review decisions and sends accepted blocks only
  ---
  duration_ms: 0.271041
  type: 'test'
  ...
# Subtest: export route updates at most the three approved sections and returns DOCX without storage
ok 486 - export route updates at most the three approved sections and returns DOCX without storage
  ---
  duration_ms: 0.140437
  type: 'test'
  ...
# Subtest: uses the complete active Source CV and selected JD
ok 487 - uses the complete active Source CV and selected JD
  ---
  duration_ms: 1.34446
  type: 'test'
  ...
# Subtest: refuses tailoring when Source CV is not ready
ok 488 - refuses tailoring when Source CV is not ready
  ---
  duration_ms: 0.577054
  type: 'test'
  ...
# Subtest: refuses a vacancy without usable JD text
ok 489 - refuses a vacancy without usable JD text
  ---
  duration_ms: 0.235298
  type: 'test'
  ...
# Subtest: does not mutate Source CV or selected job
ok 490 - does not mutate Source CV or selected job
  ---
  duration_ms: 2.033099
  type: 'test'
  ...
# Subtest: analyzeJob returns only grounded structured priorities
ok 491 - analyzeJob returns only grounded structured priorities
  ---
  duration_ms: 6.126718
  type: 'test'
  ...
# Subtest: embedded prompt-like JD text is passed as source data and does not become a priority
ok 492 - embedded prompt-like JD text is passed as source data and does not become a priority
  ---
  duration_ms: 0.562372
  type: 'test'
  ...
# Subtest: analyzeJob rejects model output whose quoted evidence is not present in the JD
ok 493 - analyzeJob rejects model output whose quoted evidence is not present in the JD
  ---
  duration_ms: 0.711803
  type: 'test'
  ...
# Subtest: analyzeJob rejects must-have evidence that is not present in the JD
ok 494 - analyzeJob rejects must-have evidence that is not present in the JD
  ---
  duration_ms: 0.557055
  type: 'test'
  ...
# Subtest: mapSelectedCvEvidence maps evidence using only the selected CV and exact section IDs
ok 495 - mapSelectedCvEvidence maps evidence using only the selected CV and exact section IDs
  ---
  duration_ms: 4.37831
  type: 'test'
  ...
# Subtest: mapSelectedCvEvidence rejects evidence assigned to the wrong role section
ok 496 - mapSelectedCvEvidence rejects evidence assigned to the wrong role section
  ---
  duration_ms: 0.906562
  type: 'test'
  ...
# Subtest: mapSelectedCvEvidence rejects invented CV evidence
ok 497 - mapSelectedCvEvidence rejects invented CV evidence
  ---
  duration_ms: 0.799393
  type: 'test'
  ...
# Subtest: writeProfessionalSummary uses only verified selected-CV evidence and excludes unsupported requirements
ok 498 - writeProfessionalSummary uses only verified selected-CV evidence and excludes unsupported requirements
  ---
  duration_ms: 1.48649
  type: 'test'
  ...
# Subtest: writeProfessionalSummary rejects an unknown or unsupported evidence ID
ok 499 - writeProfessionalSummary rejects an unknown or unsupported evidence ID
  ---
  duration_ms: 1.055191
  type: 'test'
  ...
# Subtest: writeProfessionalSummary rejects numbers absent from selected-CV evidence
ok 500 - writeProfessionalSummary rejects numbers absent from selected-CV evidence
  ---
  duration_ms: 0.850549
  type: 'test'
  ...
# Subtest: writeProfessionalSummary leaves the detected selected-CV structure unchanged
ok 501 - writeProfessionalSummary leaves the detected selected-CV structure unchanged
  ---
  duration_ms: 0.737852
  type: 'test'
  ...
# Subtest: writeProfessionalSummary can change positioning when supported vacancy evidence changes
ok 502 - writeProfessionalSummary can change positioning when supported vacancy evidence changes
  ---
  duration_ms: 0.616653
  type: 'test'
  ...
# Subtest: runTruthGuard repairs an ownership overclaim once using the same evidence set
ok 503 - runTruthGuard repairs an ownership overclaim once using the same evidence set
  ---
  duration_ms: 2.532939
  type: 'test'
  ...
# Subtest: runTruthGuard fails closed after one repair and safe blocks continue independently
ok 504 - runTruthGuard fails closed after one repair and safe blocks continue independently
  ---
  duration_ms: 0.935455
  type: 'test'
  ...
# Subtest: signs and verifies an intact short-lived tailoring token
ok 505 - signs and verifies an intact short-lived tailoring token
  ---
  duration_ms: 6.726666
  type: 'test'
  ...
# Subtest: rejects a modified signed token
ok 506 - rejects a modified signed token
  ---
  duration_ms: 0.981392
  type: 'test'
  ...
# Subtest: rejects an expired tailoring token
ok 507 - rejects an expired tailoring token
  ---
  duration_ms: 0.512558
  type: 'test'
  ...
# Subtest: confirm preview exposes plan counts, directions, tiers and provenance labels
ok 508 - confirm preview exposes plan counts, directions, tiers and provenance labels
  ---
  duration_ms: 6.54764
  type: 'test'
  ...
# Subtest: page wires the edited draft into a saved Union Search Plan preview
ok 509 - page wires the edited draft into a saved Union Search Plan preview
  ---
  duration_ms: 6.066819
  type: 'test'
  ...
# Subtest: saved Union Search Plan drives profile LIVE while legacy fallback stays isolated
not ok 510 - saved Union Search Plan drives profile LIVE while legacy fallback stays isolated
  ---
  duration_ms: 3.93195
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/union-search-plan-wiring.test.mjs:22:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JSON\.stringify\(\{freshnessDays,unionSearchPlan:profile\.unionSearchPlan,exclusionRules:Array\.isArray\(profile\.exclusionRules\)\?profile\.exclusionRules:\[\]\}\)/. Input:
    
    'async function search(){\n' +
      '  if(!resumeLoaded){\n' +
      "    setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      '  if(!selectedSources.length){\n' +
      "    setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      '    return\n' +
      '  }\n' +
      "  setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0\n' +
      '  try{\n' +
      '    const tasks=[]\n' +
      '\n' +
      "    if(selectedSources.includes('linkedin')) tasks.push((async()=>{\n" +
      '      let res\n' +
      '      if(hasProfilePlan){\n' +
      '        const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint\n' +
      '        const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})\n' +
      "        res=await fetch('/api/linkedin-profile-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({\n' +
      '            freshnessDays,\n' +
      '            unionSearchPlan:profile.unionSearchPlan,\n' +
      '            exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '            previousCandidates:poolSnapshot.candidates,\n' +
      '            previousVerifiedJobs:poolSnapshot.verifiedJobs,\n' +
      '          }),\n' +
      '        })\n' +
      '      }else{\n' +
      "        res=await fetch('/api/linkedin-search',{\n" +
      "          method:'POST',\n" +
      "          headers:{'Content-Type':'application/json'},\n" +
      '          body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),\n' +
      '        })\n' +
      '      }\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'LinkedIn search failed')\n" +
      '      if(hasProfilePlan&&Array.isArray(data.masterCandidates)){\n' +
      '        writeLinkedInMasterPool({\n' +
      '          storage:localStorage,\n' +
      '          fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,\n' +
      '          candidates:data.masterCandidates,\n' +
      '          verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],\n' +
      '        })\n' +
      '      }\n' +
      "      return {source:'linkedin',data}\n" +
      '    })())\n' +
      '\n' +
      "    if(selectedSources.includes('jobindex')) tasks.push((async()=>{\n" +
      "      if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')\n" +
      "      const res=await fetch('/api/jobindex-profile-search',{\n" +
      "        method:'POST',\n" +
      "        headers:{'Content-Type':'application/json'},\n" +
      '        body:JSON.stringify({\n' +
      '          freshnessDays,\n' +
      '          unionSearchPlan:profile.unionSearchPlan,\n' +
      '          exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '        }),\n' +
      '      })\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'Jobindex search failed')\n" +
      "      return {source:'jobindex',data}\n" +
      '    })())\n' +
      '\n' +
      "    if(selectedSources.includes('jobnet')) tasks.push((async()=>{\n" +
      "      if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')\n" +
      "      const res=await fetch('/api/jobnet-profile-search',{\n" +
      "        method:'POST',\n" +
      "        headers:{'Content-Type':'application/json'},\n" +
      '        body:JSON.stringify({\n' +
      '          freshnessDays,\n' +
      '          unionSearchPlan:profile.unionSearchPlan,\n' +
      '          exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],\n' +
      '        }),\n' +
      '      })\n' +
      '      const data=await res.json()\n' +
      "      if(!res.ok) throw new Error(data.error||'Jobnet search failed')\n" +
      "      return {source:'jobnet',data}\n" +
      '    })())\n' +
      '\n' +
      '    const settled=await Promise.allSettled(tasks)\n' +
      "    const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)\n" +
      "    const failed=settled.filter(item=>item.status==='rejected')\n" +
      "    if(!successful.length) throw failed[0]?.reason||new Error('Search failed')\n" +
      '\n' +
      '    const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))\n' +
      '    setJobs(mergedJobs)\n' +
      '\n' +
      '    const stats={\n' +
      '      masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),\n' +
      '      fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),\n' +
      '      returned:mergedJobs.length,\n' +
      '    }\n' +
      '    const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))\n' +
      "    const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')\n" +
      '    setState({\n' +
      '      loading:false,\n' +
      "      error:'',\n" +
      "      coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},\n" +
      '      stats,\n' +
      '      fetchedAt:new Date().toISOString(),\n' +
      '      audit,\n' +
      '    })\n' +
      "  }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }\n" +
      '}'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/union-search-plan-wiring.test.mjs:30:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: compiles edited roles in primary-then-adjacent order and primary wins duplicates
ok 511 - compiles edited roles in primary-then-adjacent order and primary wins duplicates
  ---
  duration_ms: 2.333233
  type: 'test'
  ...
# Subtest: removed raw CV-proposed roles do not survive the edited plan
ok 512 - removed raw CV-proposed roles do not survive the edited plan
  ---
  duration_ms: 1.295357
  type: 'test'
  ...
# Subtest: manual roles are preserved without invented CV provenance
ok 513 - manual roles are preserved without invented CV provenance
  ---
  duration_ms: 0.423817
  type: 'test'
  ...
# Subtest: CV-supported roles preserve all supporting CV ids, slots and raw support kinds
ok 514 - CV-supported roles preserve all supporting CV ids, slots and raw support kinds
  ---
  duration_ms: 0.367043
  type: 'test'
  ...
# Subtest: user retiering changes plan tier while preserving original CV support classification
ok 515 - user retiering changes plan tier while preserving original CV support classification
  ---
  duration_ms: 0.449456
  type: 'test'
  ...
# Subtest: deduplicates case and whitespace while preserving first visible spelling
ok 516 - deduplicates case and whitespace while preserving first visible spelling
  ---
  duration_ms: 0.289658
  type: 'test'
  ...
# Subtest: empty edited lists produce an empty valid plan
ok 517 - empty edited lists produce an empty valid plan
  ---
  duration_ms: 0.390447
  type: 'test'
  ...
# Subtest: fingerprint is stable for identical execution inputs
ok 518 - fingerprint is stable for identical execution inputs
  ---
  duration_ms: 0.190912
  type: 'test'
  ...
# Subtest: fingerprint changes on add remove reorder retier or provenance change
ok 519 - fingerprint changes on add remove reorder retier or provenance change
  ---
  duration_ms: 0.593439
  type: 'test'
  ...
# Subtest: selected CV baseline carries Best CV update focus
ok 520 - selected CV baseline carries Best CV update focus
  ---
  duration_ms: 2.565878
  type: 'test'
  ...
# Subtest: single adaptation request sends Best CV update focus
ok 521 - single adaptation request sends Best CV update focus
  ---
  duration_ms: 0.68894
  type: 'test'
  ...
# Subtest: single AI call receives Best CV update focus as guidance
ok 522 - single AI call receives Best CV update focus as guidance
  ---
  duration_ms: 0.73641
  type: 'test'
  ...
# Subtest: Best CV passes update focus only with the recommended CV selection
ok 523 - Best CV passes update focus only with the recommended CV selection
  ---
  duration_ms: 0.493841
  type: 'test'
  ...
# Subtest: tailor-cv forwards update focus into the one AI writer
ok 524 - tailor-cv forwards update focus into the one AI writer
  ---
  duration_ms: 0.305312
  type: 'test'
  ...
# Subtest: layout imports V15 polish stylesheet
ok 525 - layout imports V15 polish stylesheet
  ---
  duration_ms: 1.112807
  type: 'test'
  ...
# Subtest: idle Expertise Match says Not analysed without changing scored state
ok 526 - idle Expertise Match says Not analysed without changing scored state
  ---
  duration_ms: 0.376067
  type: 'test'
  ...
# Subtest: footer replaces milestone developer note with product footer
ok 527 - footer replaces milestone developer note with product footer
  ---
  duration_ms: 0.235969
  type: 'test'
  ...
# Subtest: A plus arrow favicon exists
ok 528 - A plus arrow favicon exists
  ---
  duration_ms: 0.225955
  type: 'test'
  ...
# Subtest: main condition grid hides Salary and uses three columns
ok 529 - main condition grid hides Salary and uses three columns
  ---
  duration_ms: 0.839392
  type: 'test'
  ...
# Subtest: CV Update Review is driven by explicit selected-CV adaptation rather than a standalone JD cache pretest
ok 530 - CV Update Review is driven by explicit selected-CV adaptation rather than a standalone JD cache pretest
  ---
  duration_ms: 2.239054
  type: 'test'
  ...
# Subtest: CV Update Review keeps a focused ORIGINAL UPDATED review without the obsolete Truth Guard panel
ok 531 - CV Update Review keeps a focused ORIGINAL UPDATED review without the obsolete Truth Guard panel
  ---
  duration_ms: 0.326934
  type: 'test'
  ...
# Subtest: review cards show Original, Updated, Why changed and independent decisions
not ok 532 - review cards show Original, Updated, Why changed and independent decisions
  ---
  duration_ms: 1.498849
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:24:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /change\.updated/. Input:
    
    "'use client'\n" +
      "import { useEffect, useMemo, useState } from 'react'\n" +
      "import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'\n" +
      "import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'\n" +
      "import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'\n" +
      "import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'\n" +
      "import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'\n" +
      "import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'\n" +
      "import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'\n" +
      "import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'\n" +
      "import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'\n" +
      "import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'\n" +
      "import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'\n" +
      "import {requestCvAdaptation} from './lib/cv-adaptation-client.js'\n" +
      "import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'\n" +
      "import {requestExpertiseMatch} from './lib/expertise-match-client.js'\n" +
      "import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'\n" +
      "import {evaluateJobConditions} from './lib/job-conditions.js'\n" +
      "import {fitLabel} from './lib/fit-label.js'\n" +
      "import {compareShadowToLegacy} from './lib/shadow-search-compare.js'\n" +
      "import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'\n" +
      "import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'\n" +
      "import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'\n" +
      "import SearchAudit from './components/search-audit.js'\n" +
      "import ShadowSearchAudit from './components/shadow-search-audit.js'\n" +
      "import CvLibraryStep from './components/cv-library-step.js'\n" +
      "import SearchProfileRolesStep from './components/search-profile-roles-step.js'\n" +
      "import SearchPlanPreview from './components/search-plan-preview.js'\n" +
      "import BestCvPanel from './components/best-cv-panel.js'\n" +
      "import filterStyles from './components/job-filters.module.css'\n" +
      '\n' +
      'const WINDOWS=[1,3,7,14]\n' +
      "const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}\n" +
      "const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}\n" +
      '\n' +
      "function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }\n" +
      "function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }\n" +
      "function conditionScore(value){ return value==null?'N/A':`${value}%` }\n" +
      "function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }\n" +
      "function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}\n" +
      "function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}\n" +
      "function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}\n" +
      "function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}\n" +
      "function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}\n" +
      "function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\\b(a\\/s|as)\\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}\n" +
      "function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}\n" +
      '\n' +
      'export default function Home(){\n' +
      '  const [freshnessDays,setFreshnessDays]=useState(7)\n' +
      '  const [jobs,setJobs]=useState([])\n' +
      '  const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])\n' +
      '  const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))\n' +
      '  const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))\n' +
      '  const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])\n' +
      '  const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))\n' +
      '  const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0\n' +
      '  const [selected,setSelected]=useState(null)\n' +
      '  const [jobStatuses,setJobStatuses]=useState({})\n' +
      "  const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})\n" +
      "  const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})\n" +
      '  const [cvData,setCvData]=useState(null)\n' +
      '  const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())\n' +
      "  const [cvState,setCvState]=useState({loadingSlot:null,error:''})\n" +
      '  const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)\n' +
      '  const [profileOpen,setProfileOpen]=useState(false)\n' +
      '  const [profileStep,setProfileStep]=useState(1)\n' +
      '  const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)\n' +
      "  const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})\n" +
      '  const [reviewOpen,setReviewOpen]=useState(false)\n' +
      '  const [adaptationSelections,setAdaptationSelections]=useState({})\n' +
      '  const [adaptationBaselines,setAdaptationBaselines]=useState({})\n' +
      '  const [adaptationResults,setAdaptationResults]=useState({})\n' +
      "  const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})\n" +
      "  const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})\n" +
      '  const [decisions,setDecisions]=useState({})\n' +
      '  const [editedUpdates,setEditedUpdates]=useState({})\n' +
      '  const [sourceDocxFiles,setSourceDocxFiles]=useState({})\n' +
      "  const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})\n" +
      '  const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])\n' +
      '  const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    try{\n' +
      '      const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)\n' +
      '      const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)\n' +
      '      const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      const storedCvRaw=savedSourceRaw||legacyCvRaw\n' +
      '      const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)\n' +
      '      const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)\n' +
      '      const primaryCv=getPrimaryCv(library)\n' +
      "      const savedProfileRaw=localStorage.getItem('applypilot-profile')\n" +
      "      const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})\n" +
      '      const preferences=normalizeSearchPreferences(savedProfile)\n' +
      '      const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}\n' +
      '\n' +
      '      setSelectedSources(readSearchSources(localStorage))\n' +
      '      setCvLibrary(library)\n' +
      '      if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))\n' +
      '      if(primaryCv){\n' +
      '        setCvData(primaryCv)\n' +
      '        localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))\n' +
      '        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)\n' +
      '      }\n' +
      '      setProfile(hydrated)\n' +
      '      setDraft(hydrated)\n' +
      '    }catch{}\n' +
      '  },[])\n' +
      '\n' +
      '  useEffect(()=>{\n' +
      '    setJobStatuses(readJobStatuses(localStorage))\n' +
      '  },[])\n' +
      '\n' +
      '  const profileReady=Boolean(profile.savedAt)\n' +
      '  const resumeLoaded=isSourceCvReady(cvData)\n' +
      '  const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])\n' +
      '  const cvReadyCount=readyCvs.length\n' +
      '  const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])'... 43930 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import { useEffect, useMemo, useState } from 'react'
    import {DEFAULT_PROFILE,mergeProfile,resumeToProfile,applicationPackState} from './lib/profile-review.js'
    import {SOURCE_CV_STORAGE_KEY,LEGACY_CV_STORAGE_KEY,buildSourceCvRecord,normalizeStoredSourceCv,isSourceCvReady} from './lib/source-cv.js'
    import {CV_LIBRARY_STORAGE_KEY,MAX_CVS,createCvLibrary,normalizeCvLibrary,upsertCvSlot,removeCvSlot,getPrimaryCv,readyCvCount} from './lib/cv-library.js'
    import {requestSearchProfileRoles,requestSearchProfileExclusions} from './lib/search-profile-client.js'
    import {SEARCH_PROFILE_BUILDER_VERSION,readSearchProfileCache,writeSearchProfileCache,resolveSearchProfileExclusions} from './lib/search-profile-cache.js'
    import {buildCvRoleProfile,combineCvRoleProfiles,searchProfileLibraryFingerprint} from './lib/search-profile-library.js'
    import {buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION} from './lib/union-search-plan.js'
    import {normalizeSearchPreferences,legacyGeographyFromPreferences} from './lib/search-profile-preferences.js'
    import {SEARCH_AREAS,WORK_MODELS,JOB_STATUS_FILTERS,DEFAULT_JOB_STATUS_FILTERS,classifySearchArea,classifyWorkModel,classifyJobStatus,filterJobItems,filterJobItemsByStatus} from './lib/job-list-filters.js'
    import {selectAdaptationCv,selectedAdaptationCv} from './lib/cv-adaptation-selection.js'
    import {buildAdaptationBaseline,baselineKey,baselineMatches} from './lib/cv-adaptation-baseline.js'
    import {requestCvAdaptation} from './lib/cv-adaptation-client.js'
    import {ADAPTATION_DECISION,readAdaptationDecision,setAdaptationDecision,adaptationReviewBlocks} from './lib/cv-adaptation-decisions.js'
    import {requestExpertiseMatch} from './lib/expertise-match-client.js'
    import {readExpertiseMatchCache,writeExpertiseMatchCache} from './lib/expertise-match-cache.js'
    import {evaluateJobConditions} from './lib/job-conditions.js'
    import {fitLabel} from './lib/fit-label.js'
    import {compareShadowToLegacy} from './lib/shadow-search-compare.js'
    import {JOB_STATUS_OPTIONS,readJobStatuses,writeJobStatus} from './lib/job-statuses.js'
    import {readLinkedInMasterPoolSnapshot,writeLinkedInMasterPool} from './lib/linkedin-master-pool-cache.js'
    import {DEFAULT_SEARCH_SOURCES,readSearchSources,writeSearchSources} from './lib/search-sources.js'
    import SearchAudit from './components/search-audit.js'
    import ShadowSearchAudit from './components/shadow-search-audit.js'
    import CvLibraryStep from './components/cv-library-step.js'
    import SearchProfileRolesStep from './components/search-profile-roles-step.js'
    import SearchPlanPreview from './components/search-plan-preview.js'
    import BestCvPanel from './components/best-cv-panel.js'
    import filterStyles from './components/job-filters.module.css'
    
    const WINDOWS=[1,3,7,14]
    const EMPTY_SEARCH_PROFILE={...DEFAULT_PROFILE,exclusions:''}
    const EMPTY_ROLE_STATE={status:'idle',error:'',source:'',totalCount:0,analysedCount:0,failedCvs:[]}
    
    function dateText(value){ if(!value) return 'Date unavailable'; const d=new Date(value); if(!Number.isFinite(d.getTime())) return 'Date unavailable'; const days=Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)); return days===0?'Today':days===1?'1 day ago':`${days} days ago` }
    function salary(job){ if(job.salaryMinDkkMonth==null&&job.salaryMaxDkkMonth==null) return 'Insufficient data'; if(job.salaryMinDkkMonth!=null&&job.salaryMaxDkkMonth!=null) return `${job.salaryMinDkkMonth.toLocaleString('en-DK')}–${job.salaryMaxDkkMonth.toLocaleString('en-DK')} DKK/month`; return `${(job.salaryMinDkkMonth??job.salaryMaxDkkMonth).toLocaleString('en-DK')} DKK/month` }
    function conditionScore(value){ return value==null?'N/A':`${value}%` }
    function acceptedWorkModels(geography=[]){ const joined=(geography||[]).join(' ').toLowerCase(); return [joined.includes('hybrid')?'hybrid':'',joined.includes('remote')?'remote':'',joined.includes('onsite')?'onsite':''].filter(Boolean) }
    function roleList(value){return Array.isArray(value)?value.map(item=>String(item??'').trim()).filter(Boolean):[]}
    function legacyRoles(value=''){return String(value??'').split(',').map(item=>item.trim()).filter(Boolean)}
    function combinedRoles(primary=[],adjacent=[]){return [...roleList(primary),...roleList(adjacent)].join(', ')}
    function workModelText(values=[]){return values.map(value=>value==='onsite'?'On-site':value==='hybrid'?'Hybrid':'Remote').join(' · ')}
    function jobSourceLabel(job={}){const source=String(job?.source||job?.sourceRecords?.[0]?.source||'').toLowerCase();if(source==='jobindex')return 'Jobindex';if(source==='jobnet')return 'Jobnet';if(source==='linkedin')return 'LinkedIn';const url=String(job?.originalUrl||'');if(url.includes('jobnet.dk'))return 'Jobnet';if(url.includes('linkedin.com'))return 'LinkedIn';return 'Source'}
    function sourceDedupeKey(job={}){const company=String(job.company||'').toLowerCase().replace(/\b(a\/s|as)\b/g,'as').replace(/[.,]/g,'').trim();const title=String(job.title||'').toLowerCase().replace(/\s+/g,' ').trim();const location=String(job.location||'').toLowerCase().replace(/\s+/g,' ').trim();return company&&title?company+'|'+title+'|'+location:''}
    function mergeSourceItems(groups=[]){const out=[];const byKey=new Map();for(const item of groups.flat()){const key=sourceDedupeKey(item?.job);if(key&&byKey.has(key)){const index=byKey.get(key);const current=out[index];const richer=String(item?.job?.description||item?.job?.fullJd||'').length>String(current?.job?.description||current?.job?.fullJd||'').length?item:current;out[index]=richer;continue}if(key)byKey.set(key,out.length);out.push(item)}return out}
    
    export default function Home(){
      const [freshnessDays,setFreshnessDays]=useState(7)
      const [jobs,setJobs]=useState([])
      const [selectedSources,setSelectedSources]=useState(()=>[...DEFAULT_SEARCH_SOURCES])
      const [selectedAreas,setSelectedAreas]=useState(()=>SEARCH_AREAS.map(({id})=>id))
      const [selectedWorkModels,setSelectedWorkModels]=useState(()=>WORK_MODELS.map(({id})=>id))
      const [selectedStatuses,setSelectedStatuses]=useState(()=>[...DEFAULT_JOB_STATUS_FILTERS])
      const allFiltersSelected=SEARCH_AREAS.every(({id})=>selectedAreas.includes(id))&&WORK_MODELS.every(({id})=>selectedWorkModels.includes(id))
      const someFiltersSelected=selectedAreas.length>0||selectedWorkModels.length>0
      const [selected,setSelected]=useState(null)
      const [jobStatuses,setJobStatuses]=useState({})
      const [state,setState]=useState({loading:false,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      const [shadowState,setShadowState]=useState({status:'idle',error:'',stats:null,coverage:null,comparison:null})
      const [cvData,setCvData]=useState(null)
      const [cvLibrary,setCvLibrary]=useState(()=>createCvLibrary())
      const [cvState,setCvState]=useState({loadingSlot:null,error:''})
      const [profile,setProfile]=useState(EMPTY_SEARCH_PROFILE)
      const [draft,setDraft]=useState(EMPTY_SEARCH_PROFILE)
      const [profileOpen,setProfileOpen]=useState(false)
      const [profileStep,setProfileStep]=useState(1)
      const [profileRoleState,setProfileRoleState]=useState(EMPTY_ROLE_STATE)
      const [profileSaveState,setProfileSaveState]=useState({loading:false,error:''})
      const [reviewOpen,setReviewOpen]=useState(false)
      const [adaptationSelections,setAdaptationSelections]=useState({})
      const [adaptationBaselines,setAdaptationBaselines]=useState({})
      const [adaptationResults,setAdaptationResults]=useState({})
      const [adaptationRun,setAdaptationRun]=useState({loading:false,error:'',jobKey:'',baselineKey:''})
      const [expertiseState,setExpertiseState]=useState({loading:false,error:'',analysis:null,jobKey:''})
      const [decisions,setDecisions]=useState({})
      const [editedUpdates,setEditedUpdates]=useState({})
      const [sourceDocxFiles,setSourceDocxFiles]=useState({})
      const [exportState,setExportState]=useState({loading:false,error:'',baselineKey:''})
      const visibleJobs=useMemo(()=>filterJobItemsByStatus(filterJobItems(jobs,selectedAreas,selectedWorkModels),jobStatuses,selectedStatuses),[jobs,selectedAreas,selectedWorkModels,jobStatuses,selectedStatuses])
      const active=visibleJobs.find(({job})=>job.sourceJobId===selected?.job?.sourceJobId)||visibleJobs[0]||null
    
      useEffect(()=>{
        try{
          const savedLibraryRaw=localStorage.getItem(CV_LIBRARY_STORAGE_KEY)
          const savedSourceRaw=localStorage.getItem(SOURCE_CV_STORAGE_KEY)
          const legacyCvRaw=savedSourceRaw?null:localStorage.getItem(LEGACY_CV_STORAGE_KEY)
          const storedCvRaw=savedSourceRaw||legacyCvRaw
          const savedCv=normalizeStoredSourceCv(storedCvRaw?JSON.parse(storedCvRaw):null)
          const library=normalizeCvLibrary(savedLibraryRaw?JSON.parse(savedLibraryRaw):null,savedCv)
          const primaryCv=getPrimaryCv(library)
          const savedProfileRaw=localStorage.getItem('applypilot-profile')
          const savedProfile=mergeProfile(savedProfileRaw?JSON.parse(savedProfileRaw):{exclusions:''})
          const preferences=normalizeSearchPreferences(savedProfile)
          const hydrated={...resumeToProfile(savedProfile,primaryCv),...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)}
    
          setSelectedSources(readSearchSources(localStorage))
          setCvLibrary(library)
          if(readyCvCount(library)>0) localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(library))
          if(primaryCv){
            setCvData(primaryCv)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
          }
          setProfile(hydrated)
          setDraft(hydrated)
        }catch{}
      },[])
    
      useEffect(()=>{
        setJobStatuses(readJobStatuses(localStorage))
      },[])
    
      const profileReady=Boolean(profile.savedAt)
      const resumeLoaded=isSourceCvReady(cvData)
      const readyCvs=useMemo(()=>Array.isArray(cvLibrary?.cvs)?cvLibrary.cvs.filter(isSourceCvReady):[],[cvLibrary])
      const cvReadyCount=readyCvs.length
      const areaCounts=useMemo(()=>Object.fromEntries(SEARCH_AREAS.map(({id})=>[id,jobs.filter(item=>classifySearchArea(item.job)===id).length])),[jobs])
      const workModelCounts=useMemo(()=>Object.fromEntries(WORK_MODELS.map(({id})=>[id,jobs.filter(item=>classifyWorkModel(item.job)===id).length])),[jobs])
      const statusCounts=useMemo(()=>Object.fromEntries(JOB_STATUS_FILTERS.map(({id})=>[id,jobs.filter(item=>classifyJobStatus(item?.job?.sourceJobId,jobStatuses)===id).length])),[jobs,jobStatuses])
      const savedUnionSearchPlan=profile?.unionSearchPlan
      const profileSearchPlanSummary=profileReady&&Array.isArray(savedUnionSearchPlan?.directions)&&savedUnionSearchPlan.directions.length
        ? `${savedUnionSearchPlan.directions.length} search directions · ${Number(savedUnionSearchPlan.primaryCount)||0} primary · ${Number(savedUnionSearchPlan.adjacentCount)||0} adjacent`
        : 'Search profile not configured'
      const rolesLibraryFingerprint=searchProfileLibraryFingerprint(readyCvs,SEARCH_PROFILE_BUILDER_VERSION)
      const draftCvRoleProfiles=Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]
      const analysedRoleProfileCount=draftCvRoleProfiles.filter(roleProfile=>readyCvs.some(cv=>cv.id===roleProfile.cvId&&cv.sourceVersion===roleProfile.sourceVersion)).length
      const draftPrimaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):(profileReady?legacyRoles(draft.roles):[])
      const draftAdjacentRoles=roleList(draft.adjacentRoles)
      const draftUnionSearchPlan=useMemo(()=>buildUnionSearchPlan({primaryRoles:draftPrimaryRoles,adjacentRoles:draftAdjacentRoles,roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[]}),[draftPrimaryRoles,draftAdjacentRoles,draft.roleSources,draft.cvRoleProfiles])
      const draftPreferences=normalizeSearchPreferences(draft)
      const draftLocations=draftPreferences.locations
      const draftWorkModels=draftPreferences.workModels
      const pack=applicationPackState(resumeLoaded?cvData:null)
      const jobKey=active?.job?.sourceJobId||''
      const selectedAdaptationCvRecord=selectedAdaptationCv(adaptationSelections,jobKey,readyCvs)
      const storedAdaptationBaseline=adaptationBaselines[jobKey]||null
      const activeAdaptationBaseline=storedAdaptationBaseline&&selectedAdaptationCvRecord&&baselineMatches({baseline:storedAdaptationBaseline,job:active?.job,cv:selectedAdaptationCvRecord})?storedAdaptationBaseline:null
      const activeBaselineKey=activeAdaptationBaseline?baselineKey(activeAdaptationBaseline):''
      const currentAdaptationResult=activeBaselineKey?adaptationResults[activeBaselineKey]||null:null
      const reviewChanges=adaptationReviewBlocks({blocks:currentAdaptationResult?.blocks})
      const conditionProfile=useMemo(()=>({...profile,acceptedWorkModels:acceptedWorkModels(profile.geography)}),[profile])
      const jobConditions=useMemo(()=>active?evaluateJobConditions(active.job,conditionProfile):null,[active,conditionProfile])
      const reviewedCount=activeAdaptationBaseline?reviewChanges.filter(change=>readAdaptationDecision(decisions,{jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId:change.blockId})).length:0
      const allReviewDecisionsMade=Boolean(currentAdaptationResult)&&reviewedCount===reviewChanges.length
      const selectedSourceDocx=activeAdaptationBaseline?sourceDocxFiles[activeAdaptationBaseline.sourceVersion]||null:null
      const profileCompletion=useMemo(()=>{
        const fields=[resumeLoaded,draft.roles,(draftLocations.length&&draftWorkModels.length)]
        return Math.round(fields.filter(Boolean).length/fields.length*100)
      },[draft.roles,resumeLoaded,draftLocations.length,draftWorkModels.length])
    
      useEffect(()=>{
        if(!active||!resumeLoaded){ setExpertiseState({loading:false,error:'',analysis:null,jobKey:''}); return }
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cached=readExpertiseMatchCache({storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion})
        setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
      },[jobKey,resumeLoaded,cvData?.sourceVersion])
    
      async function parseCv(file,slot=1){
        if(!file) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setCvState({loadingSlot:null,error:'Please upload a Word DOCX file.'}); return }
        setCvState({loadingSlot:slot,error:''})
        try{
          const form=new FormData()
          form.append('file',file)
          const res=await fetch('/api/parse-cv',{method:'POST',body:form})
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'CV parsing failed.')
          const saved=buildSourceCvRecord(data,new Date().toISOString())
          const nextLibrary=upsertCvSlot(cvLibrary,slot,saved)
          setSourceDocxFiles(current=>({...current,[saved.sourceVersion]:file}))
          localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
          setCvLibrary(nextLibrary)
          setProfileRoleState(EMPTY_ROLE_STATE)
    
          if(slot===1){
            const primaryCv=getPrimaryCv(nextLibrary)
            localStorage.setItem(SOURCE_CV_STORAGE_KEY,JSON.stringify(primaryCv))
            localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
            setCvData(primaryCv)
            setDecisions({})
            setReviewOpen(false)
            setProfile(current=>{
              const next=resumeToProfile(current,primaryCv)
              if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
              return next
            })
            setDraft(current=>resumeToProfile(current,primaryCv))
          }
          setCvState({loadingSlot:null,error:''})
        }catch(error){
          setCvState({loadingSlot:null,error:error.message||'CV parsing failed. Please try another DOCX.'})
        }
      }
    
      function removeCv(slot){
        const nextLibrary=removeCvSlot(cvLibrary,slot)
        localStorage.setItem(CV_LIBRARY_STORAGE_KEY,JSON.stringify(nextLibrary))
        setCvLibrary(nextLibrary)
        setCvState({loadingSlot:null,error:''})
        setProfileRoleState(EMPTY_ROLE_STATE)
        const removed=cvLibrary?.cvs?.[slot-1]||null
        if(removed?.sourceVersion) setSourceDocxFiles(current=>{const next={...current};delete next[removed.sourceVersion];return next})
        if(slot!==1) return
    
        localStorage.removeItem(SOURCE_CV_STORAGE_KEY)
        localStorage.removeItem(LEGACY_CV_STORAGE_KEY)
        setCvData(null)
        setDecisions({})
        setReviewOpen(false)
        setProfile(current=>{
          const next={...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}
          if(current.savedAt) localStorage.setItem('applypilot-profile',JSON.stringify(next))
          return next
        })
        setDraft(current=>({...current,cvName:'',factBank:[],skills:[],cvParsedAt:''}))
      }
    
      function changeJobStatus(jobId,status){
        setJobStatuses(current=>writeJobStatus({storage:localStorage,statuses:current,jobId,status}))
      }
    
      function toggleJobFilter(setter,id){
        setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])
      }
    
      function toggleAllJobFilters(event){
        const checked=event.target.checked
        setSelectedAreas(checked?SEARCH_AREAS.map(({id})=>id):[])
        setSelectedWorkModels(checked?WORK_MODELS.map(({id})=>id):[])
      }
    
      function toggleSource(source){
        setSelectedSources(current=>{
          const next=current.includes(source)?current.filter(value=>value!==source):[...current,source]
          return writeSearchSources(localStorage,next)
        })
      }
    
      async function search(){
      if(!resumeLoaded){
        setState({loading:false,error:'Please Upload Your CV',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      if(!selectedSources.length){
        setState({loading:false,error:'Select at least one search source.',coverage:null,stats:null,fetchedAt:null,audit:[]})
        return
      }
      setJobs([]); setState({loading:true,error:'',coverage:null,stats:null,fetchedAt:null,audit:[]})
      setShadowState({status:'skipped',error:'',stats:null,coverage:null,comparison:null})
      const hasProfilePlan=Array.isArray(profile?.unionSearchPlan?.directions)&&profile.unionSearchPlan.directions.length>0
      try{
        const tasks=[]
    
        if(selectedSources.includes('linkedin')) tasks.push((async()=>{
          let res
          if(hasProfilePlan){
            const fingerprint=profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint
            const poolSnapshot=readLinkedInMasterPoolSnapshot({storage:localStorage,fingerprint})
            res=await fetch('/api/linkedin-profile-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({
                freshnessDays,
                unionSearchPlan:profile.unionSearchPlan,
                exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
                previousCandidates:poolSnapshot.candidates,
                previousVerifiedJobs:poolSnapshot.verifiedJobs,
              }),
            })
          }else{
            res=await fetch('/api/linkedin-search',{
              method:'POST',
              headers:{'Content-Type':'application/json'},
              body:JSON.stringify({freshnessDays,cvText:cvData.cvText}),
            })
          }
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'LinkedIn search failed')
          if(hasProfilePlan&&Array.isArray(data.masterCandidates)){
            writeLinkedInMasterPool({
              storage:localStorage,
              fingerprint:profile.unionSearchPlanFingerprint||profile.unionSearchPlan?.fingerprint,
              candidates:data.masterCandidates,
              verifiedJobs:Array.isArray(data.masterVerifiedJobs)?data.masterVerifiedJobs:[],
            })
          }
          return {source:'linkedin',data}
        })())
    
        if(selectedSources.includes('jobindex')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobindex requires a saved Search Profile.')
          const res=await fetch('/api/jobindex-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobindex search failed')
          return {source:'jobindex',data}
        })())
    
        if(selectedSources.includes('jobnet')) tasks.push((async()=>{
          if(!hasProfilePlan) throw new Error('Jobnet requires a saved Search Profile.')
          const res=await fetch('/api/jobnet-profile-search',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({
              freshnessDays,
              unionSearchPlan:profile.unionSearchPlan,
              exclusionRules:Array.isArray(profile.exclusionRules)?profile.exclusionRules:[],
            }),
          })
          const data=await res.json()
          if(!res.ok) throw new Error(data.error||'Jobnet search failed')
          return {source:'jobnet',data}
        })())
    
        const settled=await Promise.allSettled(tasks)
        const successful=settled.filter(item=>item.status==='fulfilled').map(item=>item.value)
        const failed=settled.filter(item=>item.status==='rejected')
        if(!successful.length) throw failed[0]?.reason||new Error('Search failed')
    
        const mergedJobs=mergeSourceItems(successful.map(result=>Array.isArray(result.data.jobs)?result.data.jobs:[]))
        setJobs(mergedJobs)
    
        const stats={
          masterPoolSize:successful.reduce((sum,result)=>sum+Number(result.data.stats?.masterPoolSize??result.data.stats?.discovered??0),0),
          fullJdVerified:successful.reduce((sum,result)=>sum+Number(result.data.stats?.fullJdVerified??0),0),
          returned:mergedJobs.length,
        }
        const audit=successful.flatMap(result=>(Array.isArray(result.data.audit)?result.data.audit:[]).map(row=>({...row,source:result.source})))
        const limited=failed.length>0||successful.some(result=>result.data.coverage?.status==='ACCESS LIMITED')
        setState({
          loading:false,
          error:'',
          coverage:{source:selectedSources.join('+'),freshnessDays,status:limited?'ACCESS LIMITED':mergedJobs.length?'SEARCHED':'NO RELEVANT RESULTS',detail:failed[0]?.reason?.message||null},
          stats,
          fetchedAt:new Date().toISOString(),
          audit,
        })
      }catch(error){ setState({loading:false,error:error.message||'Search failed',coverage:null,stats:null,fetchedAt:null,audit:[]}) }
    }
      function startProfile(){
        const base=resumeToProfile(profile,cvData)
        const preferences=normalizeSearchPreferences(base)
        setDraft({...base,...preferences,geography:legacyGeographyFromPreferences(preferences.locations,preferences.workModels)})
        setProfileStep(1)
        setProfileRoleState(EMPTY_ROLE_STATE)
        setProfileSaveState({loading:false,error:''})
        setProfileOpen(true)
        setCvState({loadingSlot:null,error:''})
      }
    
      function closeProfile(){setProfileOpen(false)}
    
      function applyProfileRoleLibrary({profiles=[],combined={},source='ai',failedCvs=[],totalCount=readyCvs.length,fingerprint=rolesLibraryFingerprint}={}){
        const primaryRoles=roleList(combined.primaryRoles)
        const adjacentRoles=roleList(combined.adjacentRoles)
        const roleSources=Array.isArray(combined.roleSources)?combined.roleSources:[]
        const status=failedCvs.length?'partial':'ready'
        const error=failedCvs.length?`Could not analyse ${failedCvs.map(cv=>`CV ${cv.slot} (${cv.fileName})`).join(' · ')}. Successful CV role directions are preserved.`:''
        setDraft(current=>({...current,primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||'',cvRoleProfiles:profiles,roleSources,rolesLibraryFingerprint:fingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION}))
        setProfileRoleState({status,error,source,totalCount,analysedCount:profiles.length,failedCvs})
      }
    
      async function buildProfileRoles({forceCvIds=[]}={}){
        if(!readyCvs.length) return
        const forceSet=new Set(Array.isArray(forceCvIds)?forceCvIds:[])
        const savedProfiles=Array.isArray(profile.cvRoleProfiles)?profile.cvRoleProfiles:[]
        const savedProfilesCurrent=savedProfiles.length===readyCvs.length&&readyCvs.every(cv=>savedProfiles.some(roleProfile=>roleProfile.cvId===cv.id&&roleProfile.sourceVersion===cv.sourceVersion))
    
        if(!forceSet.size&&profile.savedAt&&profile.rolesLibraryFingerprint===rolesLibraryFingerprint&&savedProfilesCurrent&&roleList(profile.primaryRoles).length){
          const roleSources=Array.isArray(profile.roleSources)?profile.roleSources:combineCvRoleProfiles(savedProfiles).roleSources
          applyProfileRoleLibrary({profiles:savedProfiles,combined:{primaryRoles:profile.primaryRoles,adjacentRoles:profile.adjacentRoles,roleSources},source:'saved'})
          return
        }
    
        setProfileRoleState({status:'loading',error:'',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs:[]})
        const profiles=[]
        const failedCvs=[]
        let aiCount=0
        let cacheCount=0
    
        for(const cv of readyCvs){
          try{
            let roles=forceSet.has(cv.id)?null:readSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion})
            if(roles){
              cacheCount++
            }else{
              roles=await requestSearchProfileRoles({cvText:cv.cvText})
              writeSearchProfileCache({storage:localStorage,sourceVersion:cv.sourceVersion,roles})
              aiCount++
            }
            profiles.push(buildCvRoleProfile(cv,roles))
          }catch(error){
            failedCvs.push({cvId:cv.id,slot:cv.slot,fileName:cv.fileName,error:error.message||'Search Profile generation failed safely.'})
          }
        }
    
        if(!profiles.length){
          setProfileRoleState({status:'error',error:failedCvs.map(cv=>`CV ${cv.slot}: ${cv.error}`).join(' · ')||'Search Profile generation failed safely. Please try again.',source:'',totalCount:readyCvs.length,analysedCount:0,failedCvs})
          return
        }
    
        const combined=combineCvRoleProfiles(profiles)
        const source=aiCount&&cacheCount?'mixed':aiCount?'ai':'cache'
        applyProfileRoleLibrary({profiles,combined,source,failedCvs})
      }
    
      function updateDraftRoles(field,roles){
        setDraft(current=>{
          const primaryRoles=field==='primaryRoles'?roleList(roles):(roleList(current.primaryRoles).length?roleList(current.primaryRoles):(profileReady?legacyRoles(current.roles):[]))
          const adjacentRoles=field==='adjacentRoles'?roleList(roles):roleList(current.adjacentRoles)
          return {...current,[field]:roleList(roles),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||current.rolesSourceVersion||''}
        })
      }
    
      function nextProfileStep(){
        if(profileStep===1){
          setProfileStep(2)
          void buildProfileRoles()
          return
        }
        setProfileStep(step=>step+1)
      }
    
      async function saveProfile(){
        setProfileSaveState({loading:true,error:''})
        try{
          const primaryRoles=roleList(draft.primaryRoles).length?roleList(draft.primaryRoles):legacyRoles(draft.roles)
          const adjacentRoles=roleList(draft.adjacentRoles)
          const {locations,workModels}=normalizeSearchPreferences(draft)
          const geography=legacyGeographyFromPreferences(locations,workModels)
          const compiledExclusions=await resolveSearchProfileExclusions({storage:localStorage,exclusionsText:draft.exclusions,savedProfile:profile,parse:requestSearchProfileExclusions})
          const exclusions=String(draft.exclusions??'').replace(/\s+/g,' ').trim()
          const saved={...resumeToProfile(draft,cvData),primaryRoles,adjacentRoles,roles:combinedRoles(primaryRoles,adjacentRoles),rolesSourceVersion:cvData?.sourceVersion||draft.rolesSourceVersion||'',cvRoleProfiles:Array.isArray(draft.cvRoleProfiles)?draft.cvRoleProfiles:[],roleSources:Array.isArray(draft.roleSources)?draft.roleSources:[],rolesLibraryFingerprint:draft.rolesLibraryFingerprint||rolesLibraryFingerprint,rolesBuilderVersion:SEARCH_PROFILE_BUILDER_VERSION,unionSearchPlan:draftUnionSearchPlan,unionSearchPlanVersion:UNION_SEARCH_PLAN_VERSION,unionSearchPlanFingerprint:draftUnionSearchPlan.fingerprint,locations,workModels,geography,exclusions,exclusionRules:compiledExclusions.rules,exclusionsFingerprint:compiledExclusions.fingerprint,exclusionsParserVersion:compiledExclusions.parserVersion,exclusionsParsedAt:exclusions?new Date().toISOString():'',savedAt:new Date().toISOString()}
          localStorage.setItem('applypilot-profile',JSON.stringify(saved))
          setProfile(saved)
          setDraft(saved)
          setProfileSaveState({loading:false,error:''})
          setProfileOpen(false)
          setProfileStep(1)
        }catch(error){
          setProfileSaveState({loading:false,error:error.message||'Search Profile exclusions processing failed. Please try again.'})
        }
      }
    
      async function runExpertiseMatch(){
        if(!active||!resumeLoaded) return
        const runKey=active.job.sourceJobId||`${active.job.title}|${active.job.company}`
        const cacheArgs={storage:localStorage,jobId:active.job.sourceJobId,sourceVersion:cvData?.sourceVersion}
        const cached=readExpertiseMatchCache(cacheArgs)
        if(cached){
          setExpertiseState({loading:false,error:'',analysis:cached,jobKey:runKey})
          return
        }
        setExpertiseState({loading:true,error:'',analysis:null,jobKey:runKey})
        try{
          const analysis=await requestExpertiseMatch({job:active.job,cvText:cvData.cvText})
          writeExpertiseMatchCache({...cacheArgs,analysis})
          setExpertiseState({loading:false,error:'',analysis,jobKey:runKey})
        }catch(error){
          setExpertiseState({loading:false,error:error.message||'Expertise Match analysis failed safely. Please try again.',analysis:null,jobKey:runKey})
        }
      }
    
      function chooseAdaptationCv(cv){
        if(!active||!jobKey||!cv) return
        const baseline=buildAdaptationBaseline({job:active.job,cv})
        setAdaptationSelections(current=>selectAdaptationCv(current,{jobKey,cvId:cv.id,readyCvs}))
        setAdaptationBaselines(current=>({...current,[jobKey]:baseline}))
        setAdaptationRun({loading:false,error:'',jobKey:'',baselineKey:''})
        setExportState({loading:false,error:'',baselineKey:''})
        setReviewOpen(false)
      }
    
      async function runCvAdaptationReview(){
        if(!active||!activeAdaptationBaseline||adaptationRun.loading||currentAdaptationResult) return
        const runJobKey=jobKey
        const runBaseline=activeAdaptationBaseline
        const runBaselineKey=baselineKey(runBaseline)
        setReviewOpen(true)
        setAdaptationRun({loading:true,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        try{
          const result=await requestCvAdaptation({baseline:runBaseline,job:active.job})
          setAdaptationResults(current=>({...current,[runBaselineKey]:result}))
          setAdaptationRun({loading:false,error:'',jobKey:runJobKey,baselineKey:runBaselineKey})
        }catch(error){
          setAdaptationRun({loading:false,error:error.message||'CV adaptation failed safely. Please try again.',jobKey:runJobKey,baselineKey:runBaselineKey})
        }
      }
    
      function decisionIdentity(blockId){
        if(!activeAdaptationBaseline) return null
        return {jobId:activeAdaptationBaseline.jobId,cvId:activeAdaptationBaseline.cvId,sourceVersion:activeAdaptationBaseline.sourceVersion,blockId}
      }
    
      function decisionFor(blockId){
        const identity=decisionIdentity(blockId)
        return identity?readAdaptationDecision(decisions,identity):null
      }
    
      function editedUpdateKey(blockId){
        return activeBaselineKey&&blockId?`${activeBaselineKey}|${blockId}`:''
      }
    
      function editedUpdateFor(change){
        const key=editedUpdateKey(change?.blockId)
        return key&&Object.prototype.hasOwnProperty.call(editedUpdates,key)?editedUpdates[key]:change?.updated||''
      }
    
      function setEditedUpdate(blockId,value){
        const key=editedUpdateKey(blockId)
        if(!key) return
        setEditedUpdates(current=>({...current,[key]:value}))
      }
    
      function setDecision(blockId,value){
        const identity=decisionIdentity(blockId)
        if(!identity) return
        setDecisions(current=>setAdaptationDecision(current,identity,value))
      }
    
      function acceptAll(){
        if(!activeAdaptationBaseline) return
        setDecisions(current=>reviewChanges.reduce((next,change)=>setAdaptationDecision(next,decisionIdentity(change.blockId),ADAPTATION_DECISION.ACCEPTED),current))
      }
    
      function attachSourceDocx(file){
        if(!file||!activeAdaptationBaseline) return
        if(!String(file.name||'').toLowerCase().endsWith('.docx')){ setExportState({loading:false,error:'Please choose the matching source DOCX file.',baselineKey:activeBaselineKey}); return }
        setSourceDocxFiles(current=>({...current,[activeAdaptationBaseline.sourceVersion]:file}))
        setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
      }
    
      async function downloadTailoredCv(){
        if(!active||!activeAdaptationBaseline||!selectedSourceDocx||!allReviewDecisionsMade||exportState.loading) return
        const replacements=reviewChanges
          .filter(change=>decisionFor(change.blockId)===ADAPTATION_DECISION.ACCEPTED)
          .map(change=>({blockId:change.blockId,originalText:change.original,newText:editedUpdateFor(change)}))
        const company=String(active.job.company||'tailored').replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'')||'tailored'
        const base=String(activeAdaptationBaseline.fileName||'CV').replace(/\.docx$/i,'')
        const outputName=`${base}_${company}_TAILORED.docx`
        setExportState({loading:true,error:'',baselineKey:activeBaselineKey})
        try{
          const form=new FormData()
          form.append('file',selectedSourceDocx)
          form.append('replacements',JSON.stringify(replacements))
          form.append('outputName',outputName)
          const res=await fetch('/api/export-tailored-cv',{method:'POST',body:form})
          if(!res.ok){
            let message='Tailored DOCX could not be created.'
            try{const data=await res.json();message=data.error||message}catch{}
            throw new Error(message)
          }
          const blob=await res.blob()
          const url=URL.createObjectURL(blob)
          const anchor=document.createElement('a')
          anchor.href=url
          anchor.download=outputName
          document.body.appendChild(anchor)
          anchor.click()
          anchor.remove()
          URL.revokeObjectURL(url)
          setExportState({loading:false,error:'',baselineKey:activeBaselineKey})
        }catch(error){
          setExportState({loading:false,error:error.message||'Tailored DOCX could not be created.',baselineKey:activeBaselineKey})
        }
      }
    
      return <main>
        <header><div><div className="brand">ApplyPilot</div><div className="tag">Search less. Apply better.</div></div><div className="headerActions"><div className={`sourceBadge profileStatus ${resumeLoaded?'statusReady':'statusEmpty'}`}>{resumeLoaded?'Profile ready':'Profile empty'}</div><div className="sourceBadge">LINKEDIN + JOBINDEX + JOBNET · TEST</div></div></header>
    
        <section className="hero">
          <div><p className="eyebrow">MULTI-SOURCE · END-TO-END</p><h1>Find the right roles for your Search Profile in Denmark.</h1><p>Search Profile → selected sources → full job description → worthwhile matches only.</p></div>
          <div className="metric"><b>{state.loading?'…':jobs.length}</b><span>matches</span></div>
        </section>
    
        <div className="profileStrip"><span className="profileSearchSummary">{profileSearchPlanSummary}</span><button className="profileEditButton" onClick={startProfile}>{profileReady?'Edit profile':'Search profile'}</button><button className="cvButton" onClick={startProfile}>{cvReadyCount?`✓ CVs ${cvReadyCount}/${MAX_CVS}`:'Upload CVs'}</button></div>
    
        <section className="controls">
          <div><small>POSTED WITHIN</small><div className="choices">{WINDOWS.map(days=><button key={days} className={freshnessDays===days?'choice selected':'choice'} onClick={()=>setFreshnessDays(days)}>{days} day{days===1?'':'s'}</button>)}</div></div>
          <div><small>SEARCH SOURCES</small><div className="choices"><label className="choice"><input type="checkbox" checked={selectedSources.includes('linkedin')} onChange={()=>toggleSource('linkedin')}/> LinkedIn</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobindex')} onChange={()=>toggleSource('jobindex')}/> Jobindex</label><label className="choice"><input type="checkbox" checked={selectedSources.includes('jobnet')} onChange={()=>toggleSource('jobnet')}/> Jobnet</label></div></div>
          <button className="primary" onClick={search} disabled={state.loading}>{state.loading?'Searching…':'Search'}</button>
        </section>
    
        {state.error&&<div className="errorBox"><b>{state.error==='Please Upload Your CV'?'Please Upload Your CV':'Search failed'}</b>{state.error!=='Please Upload Your CV'&&<span>{state.error}</span>}</div>}
        {state.stats&&<div className="searchMeta"><span><b>{state.stats.masterPoolSize??state.stats.discovered}</b> jobs discovered</span><span><b>{state.stats.fullJdVerified}</b> full JDs read</span><span><b>{state.stats.returned??jobs.length}</b> worthwhile after evaluation</span><span>Coverage: <b>{state.coverage?.status}</b></span></div>}
        {state.coverage?.detail&&<div className="warningBox">Partial source access: {state.coverage.detail}</div>}
    
        <section className="grid">
          <div className="list">
            <div className="listHead"><div><h2>Live matches</h2>{jobs.length>0&&<small className={filterStyles.resultCount}>{visibleJobs.length} of {jobs.length}</small>}</div><small>Newest {freshnessDays} days</small></div>
            {jobs.length>0&&<details className={filterStyles.filters}>
              <summary><span>FILTERS</span><span>{visibleJobs.length} of {jobs.length}</span></summary>
              <div className={filterStyles.body}>
                <label className={filterStyles.option}><input type="checkbox" checked={allFiltersSelected} ref={node=>{if(node)node.indeterminate=someFiltersSelected&&!allFiltersSelected}} onChange={toggleAllJobFilters}/><span>All filters</span><b></b></label>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>SEARCH AREAS</small>{SEARCH_AREAS.map(area=><label className={filterStyles.option} key={area.id}><input type="checkbox" checked={selectedAreas.includes(area.id)} onChange={()=>toggleJobFilter(setSelectedAreas,area.id)}/><span>{area.label}</span><b>{areaCounts[area.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>WORK MODEL</small>{WORK_MODELS.map(model=><label className={filterStyles.option} key={model.id}><input type="checkbox" checked={selectedWorkModels.includes(model.id)} onChange={()=>toggleJobFilter(setSelectedWorkModels,model.id)}/><span>{model.label}</span><b>{workModelCounts[model.id]||0}</b></label>)}</div>
                <div className={filterStyles.group}><small className={filterStyles.groupTitle}>STATUS</small>{JOB_STATUS_FILTERS.map(status=><label className={filterStyles.option} key={status.id}><input type="checkbox" checked={selectedStatuses.includes(status.id)} onChange={()=>toggleJobFilter(setSelectedStatuses,status.id)}/><span>{status.label}</span><b>{statusCounts[status.id]||0}</b></label>)}</div>
              </div>
            </details>}
            {!state.loading&&!state.error&&!state.stats&&<div className="empty">Run search to find matching vacancies from the selected sources.</div>}
            {state.loading&&<div className="empty">Searching selected sources and reading full job descriptions…</div>}
            {!state.loading&&state.stats&&jobs.length===0&&<div className="empty">NO STRONG NEW MATCHES FOUND.</div>}
            {!state.loading&&state.stats&&jobs.length>0&&visibleJobs.length===0&&<div className="empty">NO MATCHES IN SELECTED FILTERS.</div>}
            {visibleJobs.map(item=>{const {job,evaluation}=item; const score=Math.round(evaluation.score*10); const manualStatus=jobStatuses[job.sourceJobId]||''; return <div className="jobWrap" key={job.sourceJobId}>
              <button onClick={()=>setSelected(item)} className={'job '+(active?.job.sourceJobId===job.sourceJobId?'active':'')}>
                <span className="score">{fitLabel(score)}</span>
                <span><b>{job.title}</b><small>{job.company} · {job.location}</small><small className="sourceLine">{jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></span>
                <span>→</span>
              </button>
              <select aria-label={`Status for ${job.title}`} className={`jobStatusSelect status-${manualStatus||'none'}`} value={manualStatus} onChange={event=>changeJobStatus(job.sourceJobId,event.target.value)}>
                {JOB_STATUS_OPTIONS.map(option=><option key={option.value||'none'} value={option.value}>{option.label}</option>)}
              </select>
            </div>})}
          </div>
    
          <div className="panel">
            {active?(()=>{const {job}=active; const expertise=expertiseState.jobKey===jobKey?expertiseState.analysis:null; return <>
              <div className="panelTop expertiseHeader"><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p><small className="sourceLine">Source: {jobSourceLabel(job)} · {dateText(job.publishedAt)}</small></div></div>
    
              <div className="conditionGrid">
                <div className="conditionCard"><small>Area</small><b>{conditionScore(jobConditions?.area.score)}</b><span>{jobConditions?.area.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Salary</small><b>{conditionScore(jobConditions?.salary.score)}</b><span>{jobConditions?.salary.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Employment type</small><b>{conditionScore(jobConditions?.employmentType.score)}</b><span>{jobConditions?.employmentType.value||'Not stated'}</span></div>
                <div className="conditionCard"><small>Work model</small><b>{conditionScore(jobConditions?.workModel.score)}</b><span>{jobConditions?.workModel.value||'Not stated'}</span></div>
                <a className="secondary openLink" style={{gridColumn:'1 / -1',justifySelf:'start'}} href={job.originalUrl||job.detailUrl||job.applicationUrl} target="_blank" rel="noreferrer">Open vacancy</a>
              </div>
    
              <BestCvPanel
                job={job}
                cvLibrary={cvLibrary}
                selectedCvId={activeAdaptationBaseline?.cvId||''}
                onSelectCv={chooseAdaptationCv}
                adaptationActions={<div className="actions reviewActions">
                  <button className="primary" onClick={runCvAdaptationReview} disabled={!activeAdaptationBaseline||adaptationRun.loading||Boolean(currentAdaptationResult)}>{adaptationRun.loading&&adaptationRun.jobKey===jobKey?'Generating…':currentAdaptationResult?'Generated':'Generate CV update'}</button>
                  <button className="secondary" onClick={()=>setReviewOpen(true)} disabled={!currentAdaptationResult}>View CV update</button>
                  {job.officialUrl&&<a className="secondary openLink" href={job.officialUrl} target="_blank" rel="noreferrer">Employer link</a>}
                </div>}
              />
    
              <div className="expertiseHero">
                <div className="expertiseHeroHead"><div><p className="eyebrow">EXPERTISE MATCH</p><p className="expertiseIntro">Full JD ↔ Source CV professional expertise only</p></div><div className="expertiseScore">{expertiseState.loading&&expertiseState.jobKey===jobKey?'…':expertise?`${expertise.expertiseMatch}%`:'N/A'}</div></div>
                {expertiseState.loading&&expertiseState.jobKey===jobKey&&<div className="expertiseLoading">Analysing professional requirements and Source CV evidence…</div>}
                {expertiseState.error&&expertiseState.jobKey===jobKey&&<div className="errorBox"><b>Expertise Match analysis failed safely</b><span>{expertiseState.error}</span></div>}
                {!expertise&&!expertiseState.loading&&<button className="primary" onClick={runExpertiseMatch}>Run Expertise Match</button>}
                {expertise&&<>
                  <div className="expertiseSection"><h3>Why you fit</h3>{expertise.whyYouFit.length?expertise.whyYouFit.map((item,index)=><p key={index}>✓ {item}</p>):<p className="muted">No direct professional match evidence returned.</p>}</div>
                  {expertise.transferableStrengths?.length>0&&<div className="expertiseSection"><h3>Transferable strengths</h3>{expertise.transferableStrengths.map((item,index)=><p key={index}>↔ {item}</p>)}</div>}
                  <div className="expertiseSection"><h3>Expertise gaps</h3>{expertise.expertiseGaps.length?expertise.expertiseGaps.map((item,index)=><p key={index}>⚠ {item}</p>):<p>✓ No material expertise gap detected in the analysed requirements.</p>}</div>
                  <div className="expertiseSection"><h3>Expertise breakdown</h3><div className="expertiseBreakdown">
                    <div><span>Delivery / execution</span><b>{conditionScore(expertise.breakdown.delivery_execution?.score)}</b></div>
                    <div><span>Domain & functional expertise</span><b>{conditionScore(expertise.breakdown.domain_functional_expertise?.score)}</b></div>
                    <div><span>Technical / platform capabilities</span><b>{conditionScore(expertise.breakdown.technical_platform_capabilities?.score)}</b></div>
                    <div><span>Leadership & stakeholder scope</span><b>{conditionScore(expertise.breakdown.leadership_stakeholder_scope?.score)}</b></div>
                    <div><span>Required experience / qualifications</span><b>{conditionScore(expertise.breakdown.required_experience_qualifications?.score)}</b></div>
                  </div></div>
                </>}
              </div>
    
    
              <div className="section"><h3>Application pack</h3><div className="docs"><div>{pack.cvReady?'✓':'○'} Tailored CV <span className={pack.cvReady?'ready':'pending'}>{pack.tailoredCvLabel}</span></div><div>○ Cover letter <span className="pending">{pack.coverLetterLabel}</span></div></div></div>
            </>})():<div className="emptyPanel"><h2>No selected vacancy</h2><p>{state.loading?'Searching LinkedIn public pages…':'Run the LinkedIn search to see matching vacancies.'}</p></div>}
          </div>
        </section>
    
        {(state.audit.length>0||(shadowState.status!=='idle'&&shadowState.status!=='skipped'))&&<section className="auditLog">
          <div className="auditLogTitle">AUDIT LOG</div>
          <SearchAudit audit={state.audit}/>
          <ShadowSearchAudit shadowState={shadowState}/>
        </section>}
    
        <footer>TEST · LinkedIn + Jobindex + Jobnet multi-source search</footer>
    
        {profileOpen&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!profileSaveState.loading)closeProfile()}}><div className="modal profileModal">
          <div className="modalHead"><div><p className="eyebrow">BUILD YOUR SEARCH AGENT</p><h2>Search profile</h2></div><button className="close" onClick={closeProfile} disabled={profileSaveState.loading}>×</button></div>
          <div className="progress"><span style={{width:`${profileStep/4*100}%`}}></span></div><div className="stepMeta"><span>Step {profileStep} of 4</span><span>{profileCompletion}% profile data</span></div>
          {profileStep===1&&<CvLibraryStep library={cvLibrary} loadingSlot={cvState.loadingSlot} error={cvState.error} primarySkills={draft.skills} onUpload={parseCv} onRemove={removeCv}/>} 
          {profileStep===2&&<SearchProfileRolesStep primaryRoles={draftPrimaryRoles} adjacentRoles={draftAdjacentRoles} status={profileRoleState.status} error={profileRoleState.error} source={profileRoleState.source} totalCount={profileRoleState.totalCount||cvReadyCount} analysedCount={profileRoleState.analysedCount} failedCvs={profileRoleState.failedCvs} onPrimaryChange={roles=>updateDraftRoles('primaryRoles',roles)} onAdjacentChange={roles=>updateDraftRoles('adjacentRoles',roles)} onRetry={()=>buildProfileRoles({forceCvIds:(profileRoleState.failedCvs||[]).map(cv=>cv.cvId)})}/>} 
          {profileStep===3&&<div className="wizard"><h3>What should ApplyPilot exclude?</h3><p>Optional. Write any hard no-go roles, industries, languages or working conditions. ApplyPilot interprets this text only when you save the profile.</p><textarea value={draft.exclusions} onChange={event=>setDraft(current=>({...current,exclusions:event.target.value}))} rows="6"/></div>}
          {profileStep===4&&<div className="wizard review"><h3>Confirm your search profile</h3><p>This saves your Search Profile and activates profile-driven LinkedIn discovery for future searches.</p><div className="reviewRow"><span>CV library</span><b>{readyCvs.length?readyCvs.map(cv=>`CV ${cv.slot} · ${cv.fileName}`).join(' · '):'No CVs uploaded'}</b></div><div className="reviewRow"><span>CV preparation</span><b>{cvReadyCount?`Ready — ${cvReadyCount} complete Source CV${cvReadyCount===1?'':'s'} prepared`:'CV not ready'}</b></div><div className="reviewRow"><span>Role profiles</span><b>{cvReadyCount?`${analysedRoleProfileCount}/${cvReadyCount} CVs analysed`:'Not generated'}</b></div><div className="reviewRow"><span>Target roles</span><b>{draft.roles||'Not set'}</b></div><div className="reviewRow"><span>Exclude</span><b>{draft.exclusions||'None'}</b></div><SearchPlanPreview plan={draftUnionSearchPlan}/>{profileSaveState.error&&<div className="errorBox"><b>Search Profile save failed</b><span>{profileSaveState.error}</span></div>}<div className="truth"><b>Truth rule</b><span>ApplyPilot may rephrase verified experience, but may never invent skills, achievements, employers or responsibilities.</span></div></div>}
          <div className="modalActions"><button className="secondary" disabled={profileSaveState.loading} onClick={()=>profileStep===1?closeProfile():setProfileStep(step=>step-1)}>{profileStep===1?'Cancel':'Back'}</button>{profileStep<4?<button className="primary" disabled={(profileStep===1&&(Boolean(cvState.loadingSlot)||cvReadyCount===0))||(profileStep===2&&profileRoleState.status==='loading')} onClick={nextProfileStep}>Continue</button>:<button className="primary" disabled={profileSaveState.loading} onClick={saveProfile}>{profileSaveState.loading?'Saving profile…':'Save profile'}</button>}</div>
        </div></div>}
    
        {reviewOpen&&active&&activeAdaptationBaseline&&<div className="overlay" onMouseDown={event=>{if(event.target===event.currentTarget&&!adaptationRun.loading)setReviewOpen(false)}}><div className="modal reviewModal"><div className="modalHead"><div><p className="eyebrow">CV UPDATE REVIEW</p><h2>{active.job.title}</h2><p className="muted">{active.job.company} · {active.job.location}</p><p className="reviewBaseline">CV {selectedAdaptationCvRecord?.slot} · {activeAdaptationBaseline.fileName}</p></div><button className="close" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading}>×</button></div>
          <div className="reviewScopeLine">Professional Summary · Latest role overview · Previous role overview</div>
          {adaptationRun.loading&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="adaptationLoading"><b>Adapting selected CV…</b><span>Selected CV + JD → three AI updates.</span></div>}
          {adaptationRun.error&&adaptationRun.jobKey===jobKey&&adaptationRun.baselineKey===activeBaselineKey&&<div className="errorBox"><b>CV adaptation failed safely</b><span>{adaptationRun.error}</span></div>}
          {currentAdaptationResult&&<>
            <div className="adaptationRunStatus"><b>✓ Adaptation complete</b><span>{reviewChanges.length} change{reviewChanges.length===1?'':'s'} available for review · {reviewedCount}/{reviewChanges.length} decided</span></div>
            <div className="reviewToolbar"><div><h3>Selected-CV changes</h3><p>AI UPDATED text is shown directly. Source CV remains unchanged.</p></div>{reviewChanges.length>0&&<button className="secondary" onClick={acceptAll}>Accept all changes</button>}</div>
            {reviewChanges.map(change=>{const decision=decisionFor(change.blockId);return <div className={'changeCard '+(decision?'decided':'')} key={change.blockId}>
              <div className="changeHead"><span>{change.label}</span><b>{decision===ADAPTATION_DECISION.ACCEPTED?'Accepted':decision===ADAPTATION_DECISION.ORIGINAL?'Original kept':'Review needed'}</b></div>
              <div className="compareGrid"><div className="compareBox"><small>ORIGINAL</small><p>{change.original}</p></div><div className="compareArrow">→</div><div className="compareBox updatedBox"><small>UPDATED · EDITABLE</small><textarea className="updatedTextEditor" value={editedUpdateFor(change)} onChange={event=>setEditedUpdate(change.blockId,event.target.value)} rows="8"/></div></div>
              <div className="changeWhy"><div><small>WHY CHANGED</small><p>{change.why}</p></div></div>
              <div className="evidenceActions"><button className={'secondary '+(decision===ADAPTATION_DECISION.ORIGINAL?'chosen':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ORIGINAL)}>{decision===ADAPTATION_DECISION.ORIGINAL?'✓ Original kept':'Keep original'}</button><button className={'primary smallPrimary '+(decision===ADAPTATION_DECISION.ACCEPTED?'chosenPrimary':'')} onClick={()=>setDecision(change.blockId,ADAPTATION_DECISION.ACCEPTED)}>{decision===ADAPTATION_DECISION.ACCEPTED?'✓ Accepted':'Accept change'}</button></div>
            </div>})}
            {!reviewChanges.length&&<div className="reviewEmpty"><b>No changes to review.</b><span>AI returned no changed block. The selected Source CV remains unchanged.</span></div>}
          </>}
          {exportState.error&&exportState.baselineKey===activeBaselineKey&&<div className="errorBox"><b>DOCX update failed</b><span>{exportState.error}</span></div>}
          <div className="reviewFooter"><button className="secondary" onClick={()=>setReviewOpen(false)} disabled={adaptationRun.loading||exportState.loading}>Close review</button>{allReviewDecisionsMade&&(selectedSourceDocx?<button className="primary" onClick={downloadTailoredCv} disabled={exportState.loading}>{exportState.loading?'Creating DOCX…':'Download tailored DOCX'}</button>:<label className="secondary">Re-upload source DOCX<input type="file" accept=".docx" hidden onChange={event=>{const file=event.target.files?.[0];if(file)attachSourceDocx(file);event.target.value=''}}/></label>)}</div>
        </div></div>}
      </main>
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:30:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: legacy JD evidence accordions are absent because M4 reviews the three adapted CV blocks, not the JD pretest
ok 533 - legacy JD evidence accordions are absent because M4 reviews the three adapted CV blocks, not the JD pretest
  ---
  duration_ms: 0.219044
  type: 'test'
  ...
# Subtest: Run Expertise Match button is compact inside expertise card
ok 534 - Run Expertise Match button is compact inside expertise card
  ---
  duration_ms: 1.206966
  type: 'test'
  ...
# Subtest: missing visible vacancy facts normalize to N/A
ok 535 - missing visible vacancy facts normalize to N/A
  ---
  duration_ms: 1.614239
  type: 'test'
  ...
# Subtest: main vacancy fact cards visually promote factual values and hide match scores
ok 536 - main vacancy fact cards visually promote factual values and hide match scores
  ---
  duration_ms: 7.80056
  type: 'test'
  ...
# Subtest: preview page middleware bypasses Supabase before getUser while production auth remains
ok 537 - preview page middleware bypasses Supabase before getUser while production auth remains
  ---
  duration_ms: 6.393853
  type: 'test'
  ...
# Subtest: preview API auth bypass returns a synthetic admin user before Supabase while production still returns 401
ok 538 - preview API auth bypass returns a synthetic admin user before Supabase while production still returns 401
  ---
  duration_ms: 1.597895
  type: 'test'
  ...
# Subtest: version badge is visible before search and preview includes short Vercel commit SHA
ok 539 - version badge is visible before search and preview includes short Vercel commit SHA
  ---
  duration_ms: 0.792423
  type: 'test'
  ...
1..539
# tests 539
# suites 0
# pass 516
# fail 23
# cancelled 0
# skipped 0
# todo 0
# duration_ms 3315.49543
TEST_EXIT=1
