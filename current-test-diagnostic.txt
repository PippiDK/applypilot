--- lines 537-597 ---
  duration_ms: 0.919667
  type: 'test'
  ...
# Subtest: Best CV compares all ready CVs in one structured AI call and never requests a merged CV
ok 90 - Best CV compares all ready CVs in one structured AI call and never requests a merged CV
  ---
  duration_ms: 2.518139
  type: 'test'
  ...
# Subtest: right panel mounts Best CV flow while UX polish orders Expertise Match first
not ok 91 - right panel mounts Best CV flow while UX polish orders Expertise Match first
  ---
  duration_ms: 5.678898
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/best-cv-ui.contract.test.mjs:9:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /import BestCvPanel/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 689-744 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 870-930 ---
  type: 'test'
  ...
# company-search core tests passed
# Subtest: app/lib/company-search.test.mjs
ok 16 - app/lib/company-search.test.mjs
  ---
  duration_ms: 41.754646
  type: 'test'
  ...
# Subtest: TEST restores the inline company watch and consultant portal controls from 3716233
not ok 96 - TEST restores the inline company watch and consultant portal controls from 3716233
  ---
  duration_ms: 3.54115
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/company-watch-restoration.contract.test.mjs:15:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /DIRECT COMPANY WATCH/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 1022-1077 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 1178-1238 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/company-watch-restoration.contract.test.mjs:16:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: company and consultant sources participate in the main Search workflow
not ok 97 - company and consultant sources participate in the main Search workflow
  ---
  duration_ms: 1.567553
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/company-watch-restoration.contract.test.mjs:22:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /\/api\/company-profile-search/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 1330-1385 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 1500-1560 ---
  duration_ms: 0.345479
  type: 'test'
  ...
# Subtest: Step 5 identifies CV1 as the active Search CV
ok 99 - Step 5 identifies CV1 as the active Search CV
  ---
  duration_ms: 0.913095
  type: 'test'
  ...
# Subtest: profile strip shows a dynamic saved Union Search Plan summary instead of legacy role and score copy
not ok 100 - profile strip shows a dynamic saved Union Search Plan summary instead of legacy role and score copy
  ---
  duration_ms: 2.583081
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cosmetic-ui.contract.test.mjs:13:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /profile\??\.unionSearchPlan/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 1652-1707 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 1810-1892 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cosmetic-ui.contract.test.mjs:14:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: Search and Shadow diagnostics live together at the bottom under AUDIT LOG with technical styling
not ok 101 - Search and Shadow diagnostics live together at the bottom under AUDIT LOG with technical styling
  ---
  duration_ms: 0.347723
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cosmetic-ui.contract.test.mjs:22:1'
  failureType: 'testCodeFailure'
  error: 'main results grid must exist'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cosmetic-ui.contract.test.mjs:26:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: generated CV updates are kept by baseline key so reopening review does not call AI again
not ok 102 - generated CV updates are kept by baseline key so reopening review does not call AI again
  ---
  duration_ms: 6.920679
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-actions.contract.test.mjs:9:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /const \[adaptationResults,setAdaptationResults\]=useState\(\{\}\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 1984-2039 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 2140-2200 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-actions.contract.test.mjs:10:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: Generate and View are separate controls with Generate disabled after success
not ok 103 - Generate and View are separate controls with Generate disabled after success
  ---
  duration_ms: 1.391183
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-actions.contract.test.mjs:15:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Generate CV update/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 2292-2347 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 2450-2510 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-actions.contract.test.mjs:16:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: adaptation action row is passed into Best CV panel and removed from Application pack
not ok 104 - adaptation action row is passed into Best CV panel and removed from Application pack
  ---
  duration_ms: 0.643398
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-actions.contract.test.mjs:23:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /<BestCvPanel[\s\S]*adaptationActions=/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 2602-2657 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 2868-2928 ---
  duration_ms: 0.519286
  type: 'test'
  ...
# Subtest: review exposes only generated Truth-Guard PASS blocks and uses safeText as UPDATED
ok 122 - review exposes only generated Truth-Guard PASS blocks and uses safeText as UPDATED
  ---
  duration_ms: 0.299503
  type: 'test'
  ...
# Subtest: UPDATED review text is editable and exported from the edited value
not ok 123 - UPDATED review text is editable and exported from the edited value
  ---
  duration_ms: 6.364492
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-editable-review.contract.test.mjs:7:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /const \[editedUpdates,setEditedUpdates\]=useState\(\{\}\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 3020-3075 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 3188-3248 ---
  duration_ms: 3.698375
  type: 'test'
  ...
# Subtest: buildAdaptationInput rejects a different vacancy instead of reusing the old baseline
ok 125 - buildAdaptationInput rejects a different vacancy instead of reusing the old baseline
  ---
  duration_ms: 1.988835
  type: 'test'
  ...
# Subtest: M4 review runs direct selected-CV adaptation before opening review
not ok 126 - M4 review runs direct selected-CV adaptation before opening review
  ---
  duration_ms: 4.486786
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /requestCvAdaptation/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 3340-3395 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 3496-3556 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:9:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: review decisions use exact baseline identity and original or accepted values
not ok 127 - review decisions use exact baseline identity and original or accepted values
  ---
  duration_ms: 1.11321
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:17:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /setAdaptationDecision/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 3648-3703 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 3806-3866 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:18:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: switching vacancy or selected CV cannot expose a stale adaptation result
not ok 128 - switching vacancy or selected CV cannot expose a stale adaptation result
  ---
  duration_ms: 0.849165
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:25:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /baselineMatches\(\{baseline:storedAdaptationBaseline,job:active\?\.job,cv:selectedAdaptationCvRecord\}\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 3958-4013 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 4116-4202 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:26:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: Accept and Keep handlers mutate decision state only, not CV Library or Source CV
not ok 129 - Accept and Keep handlers mutate decision state only, not CV Library or Source CV
  ---
  duration_ms: 118.632625
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:30:1'
  failureType: 'testCodeFailure'
  error: |-
    The expression evaluated to a falsy value:
    
      assert.ok(start>=0&&end>start)
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:33:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: review UI renders the three-block contract and WHY CHANGED from direct editable AI review blocks
not ok 130 - review UI renders the three-block contract and WHY CHANGED from direct editable AI review blocks
  ---
  duration_ms: 0.639952
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-adaptation-review-ui.contract.test.mjs:41:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Professional Summary/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 4294-4349 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 4488-4548 ---
  duration_ms: 0.201868
  type: 'test'
  ...
# Subtest: user selection remains independent from the recommended CV
ok 136 - user selection remains independent from the recommended CV
  ---
  duration_ms: 0.294844
  type: 'test'
  ...
# Subtest: CV remove uses ApplyPilot confirmation, keeps Search Profile open and preserves fixed slots
not ok 137 - CV remove uses ApplyPilot confirmation, keeps Search Profile open and preserves fixed slots
  ---
  duration_ms: 2.788116
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-library-remove-ui-contract.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /removeCvSlot/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 4640-4695 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 4796-4856 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/cv-library-remove-ui-contract.test.mjs:16:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: Search Profile step 1 exposes three CV slots while CV 1 remains the Search source
not ok 138 - Search Profile step 1 exposes three CV slots while CV 1 remains the Search source
  ---
  duration_ms: 2.706864
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-library-ui-contract.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /CV_LIBRARY_STORAGE_KEY/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 4948-5003 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 5152-5212 ---
  duration_ms: 0.239149
  type: 'test'
  ...
# Subtest: slot number must stay within the MVP limit
ok 146 - slot number must stay within the MVP limit
  ---
  duration_ms: 0.267202
  type: 'test'
  ...
# Subtest: review decision buttons visibly confirm the selected choice
not ok 147 - review decision buttons visibly confirm the selected choice
  ---
  duration_ms: 2.797644
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/cv-review-decision-labels.contract.test.mjs:7:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /decision===ADAPTATION_DECISION\.ORIGINAL\?'✓ Original kept':'Keep original'/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 5304-5359 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 5736-5796 ---
  duration_ms: 0.323859
  type: 'test'
  ...
# Subtest: Expertise Match is not auto-requested from a useEffect
ok 193 - Expertise Match is not auto-requested from a useEffect
  ---
  duration_ms: 0.972346
  type: 'test'
  ...
# Subtest: Expertise Match has an explicit Run Expertise Match button
not ok 194 - Expertise Match has an explicit Run Expertise Match button
  ---
  duration_ms: 2.433019
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/expertise-match-manual-ui.contract.test.mjs:12:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Run Expertise Match/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 5888-5943 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 6046-6106 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/expertise-match-manual-ui.contract.test.mjs:13:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: manual Expertise Match flow reads cache before calling API and writes successful result
not ok 195 - manual Expertise Match flow reads cache before calling API and writes successful result
  ---
  duration_ms: 0.674426
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/expertise-match-manual-ui.contract.test.mjs:17:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /readExpertiseMatchCache/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 6198-6253 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 6536-6596 ---
  duration_ms: 0.812476
  type: 'test'
  ...
# Subtest: uses exactly one semantic AI stage and deterministic code for final percentage
ok 225 - uses exactly one semantic AI stage and deterministic code for final percentage
  ---
  duration_ms: 4.613394
  type: 'test'
  ...
# Subtest: vacancy list renders fit labels instead of percentages
not ok 226 - vacancy list renders fit labels instead of percentages
  ---
  duration_ms: 6.164937
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/fit-label-ui-contract.test.mjs:7:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /fitLabel\(score\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 6688-6743 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 6910-6970 ---
  duration_ms: 1.907803
  type: 'test'
  ...
# Subtest: requestJobAnalysis surfaces safe API errors
ok 237 - requestJobAnalysis surfaces safe API errors
  ---
  duration_ms: 0.718098
  type: 'test'
  ...
# Subtest: Adapt & review CV runs direct selected-CV adaptation only on explicit user action
not ok 238 - Adapt & review CV runs direct selected-CV adaptation only on explicit user action
  ---
  duration_ms: 2.770804
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/jd-analysis-ui.contract.test.mjs:7:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /requestCvAdaptation/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 7062-7117 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 7218-7278 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/jd-analysis-ui.contract.test.mjs:8:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: obsolete JD ANALYSIS PRETEST dashboard is removed from M4.11 review
not ok 239 - obsolete JD ANALYSIS PRETEST dashboard is removed from M4.11 review
  ---
  duration_ms: 0.651603
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/jd-analysis-ui.contract.test.mjs:14:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Adaptation complete/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 7370-7425 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 7528-7588 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/jd-analysis-ui.contract.test.mjs:20:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: M4 review exposes all three AI-updated CV blocks instead of Summary-only pretest UI
not ok 240 - M4 review exposes all three AI-updated CV blocks instead of Summary-only pretest UI
  ---
  duration_ms: 0.62839
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/jd-analysis-ui.contract.test.mjs:23:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Professional Summary/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 7680-7735 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 7838-7898 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/jd-analysis-ui.contract.test.mjs:24:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: JD is sent with the selected CV and qualification detail is not duplicated in review UI
not ok 241 - JD is sent with the selected CV and qualification detail is not duplicated in review UI
  ---
  duration_ms: 0.459774
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/jd-analysis-ui.contract.test.mjs:30:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Selected CV \+ JD → three AI updates\./. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 7990-8045 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 8244-8304 ---
  duration_ms: 0.204263
  type: 'test'
  ...
# Subtest: legacy ignored helper still preserves the old default behavior
ok 257 - legacy ignored helper still preserves the old default behavior
  ---
  duration_ms: 0.388479
  type: 'test'
  ...
# Subtest: manual job statuses are local informational metadata with three explicit states
not ok 258 - manual job statuses are local informational metadata with three explicit states
  ---
  duration_ms: 3.009111
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:10:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JOB_STATUS_OPTIONS/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 8396-8451 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 8552-8680 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:15:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: manual job statuses never enter search requests and only filter the post-search list
not ok 259 - manual job statuses never enter search requests and only filter the post-search list
  ---
  duration_ms: 0.260409
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:26:1'
  failureType: 'testCodeFailure'
  error: 'search() block must be found'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:29:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: Area, Employment type and Work model render directly under the vacancy header before Best CV
not ok 260 - Area, Employment type and Work model render directly under the vacancy header before Best CV
  ---
  duration_ms: 0.185328
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:36:1'
  failureType: 'testCodeFailure'
  error: 'vacancy header is missing'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:40:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: All filters is the first filter row and controls only Search Areas and Work Model
not ok 261 - All filters is the first filter row and controls only Search Areas and Work Model
  ---
  duration_ms: 0.194936
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:49:1'
  failureType: 'testCodeFailure'
  error: 'All filters row is missing'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:52:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: Status is a third independent filter group and replaces Show ignored
not ok 262 - Status is a third independent filter group and replaces Show ignored
  ---
  duration_ms: 0.313469
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:65:1'
  failureType: 'testCodeFailure'
  error: 'Work Model group is missing'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/job-list-ui-milestone.contract.test.mjs:68:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: Night Flight is a derived status, not a manual persisted option
ok 263 - Night Flight is a derived status, not a manual persisted option
  ---
  duration_ms: 1.511618
  type: 'test'
  ...
# Subtest: manual user statuses override the derived Night Flight state
ok 264 - manual user statuses override the derived Night Flight state
  ---
  duration_ms: 0.34593
  type: 'test'
  ...
# Subtest: Night Flight appears when no manual status exists and returns after manual status is cleared
ok 265 - Night Flight appears when no manual status exists and returns after manual status is cleared
  ---
  duration_ms: 0.21815
  type: 'test'
  ...
# Subtest: ordinary jobs keep STATUS when no manual status exists
ok 266 - ordinary jobs keep STATUS when no manual status exists
  ---
  duration_ms: 0.237646
  type: 'test'
  ...
# Subtest: keeps clear IT delivery and IT project leadership roles
ok 267 - keeps clear IT delivery and IT project leadership roles
  ---
  duration_ms: 2.563905
  type: 'test'
  ...
# Subtest: keeps generic Project Manager only when JD confirms technology context
ok 268 - keeps generic Project Manager only when JD confirms technology context
--- lines 9522-9582 ---
  duration_ms: 0.332294
  type: 'test'
  ...
# Subtest: Search Profile role step is library-aware rather than CV1-only
ok 409 - Search Profile role step is library-aware rather than CV1-only
  ---
  duration_ms: 1.302667
  type: 'test'
  ...
# Subtest: page builds Search Profile roles from ready CV library entries
not ok 410 - page builds Search Profile roles from ready CV library entries
  ---
  duration_ms: 4.021973
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/multi-cv-search-profile-contract.test.mjs:13:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /searchProfileLibraryFingerprint/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 9674-9729 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 9832-9892 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/multi-cv-search-profile-contract.test.mjs:14:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: LinkedIn Search request remains CV1 payload during Step 1
not ok 411 - LinkedIn Search request remains CV1 payload during Step 1
  ---
  duration_ms: 1.100827
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/multi-cv-search-profile-contract.test.mjs:20:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JSON\.stringify\(\{freshnessDays,cvText:cvData\.cvText\}\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 9984-10039 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 10532-10592 ---
  duration_ms: 0.282301
  type: 'test'
  ...
# Subtest: Night Flight job cards copy the saved Profile Match percentage directly into the vacancy list
ok 476 - Night Flight job cards copy the saved Profile Match percentage directly into the vacancy list
  ---
  duration_ms: 0.33508
  type: 'test'
  ...
# Subtest: Night Flight Morning Review reuses the ordinary search vacancy URL contract
not ok 477 - Night Flight Morning Review reuses the ordinary search vacancy URL contract
  ---
  duration_ms: 5.523484
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/night-flight-morning-review-vacancy-link.contract.test.mjs:11:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /href=\{job\.originalUrl\|\|job\.detailUrl\|\|job\.applicationUrl\}/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 10684-10739 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 10888-10954 ---
  duration_ms: 2.68356
  type: 'test'
  ...
# Subtest: Task 2E clears stale state after the next successful Night Flight sync
ok 485 - Task 2E clears stale state after the next successful Night Flight sync
  ---
  duration_ms: 0.378361
  type: 'test'
  ...
# Subtest: Task 2E Search Profile save commits locally after the guarded sync attempt and renders a stale-backend warning
not ok 486 - Task 2E Search Profile save commits locally after the guarded sync attempt and renders a stale-backend warning
  ---
  duration_ms: 4.621099
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/night-flight-profile-failure.test.mjs:42:1'
  failureType: 'testCodeFailure'
  error: |-
    The expression evaluated to a falsy value:
    
      assert.ok(page.includes("import {attemptNightFlightProfileSync} from './lib/night-flight-profile-failure.js'"))
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/night-flight-profile-failure.test.mjs:47:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: Task 2A provides a deterministic coherent Search Profile and primary CV state
ok 487 - Task 2A provides a deterministic coherent Search Profile and primary CV state
  ---
  duration_ms: 7.260746
  type: 'test'
  ...
# Subtest: Task 2A fingerprint changes when Search Profile changes
ok 488 - Task 2A fingerprint changes when Search Profile changes
  ---
  duration_ms: 0.571013
  type: 'test'
  ...
# Subtest: Task 2A fingerprint changes when primary CV changes
ok 489 - Task 2A fingerprint changes when primary CV changes
  ---
  duration_ms: 0.395092
  type: 'test'
  ...
# Subtest: Task 2A represents removed primary CV as an empty CV state with a new fingerprint
ok 490 - Task 2A represents removed primary CV as an empty CV state with a new fingerprint
  ---
  duration_ms: 0.593856
  type: 'test'
  ...
# Subtest: Task 2B upserts one coherent Night Flight profile row for the authenticated user
ok 491 - Task 2B upserts one coherent Night Flight profile row for the authenticated user
  ---
  duration_ms: 3.064424
  type: 'test'
  ...
# Subtest: Task 2B rejects missing authenticated user before writing
ok 492 - Task 2B rejects missing authenticated user before writing
--- lines 10992-11052 ---
  duration_ms: 6.932058
  type: 'test'
  ...
# Subtest: Task 2D browser client surfaces backend sync failure
ok 499 - Task 2D browser client surfaces backend sync failure
  ---
  duration_ms: 1.321863
  type: 'test'
  ...
# Subtest: Task 2D route and Search Profile save wiring are present, with guarded server sync before local commit
not ok 500 - Task 2D route and Search Profile save wiring are present, with guarded server sync before local commit
  ---
  duration_ms: 1.986591
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/night-flight-profile-sync.test.mjs:62:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /requestNightFlightProfileSync/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 11144-11199 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 11614-11674 ---
  duration_ms: 3.901137
  type: 'test'
  ...
# Subtest: V18-18 Profile Match content and right-panel contract remain unchanged
ok 552 - V18-18 Profile Match content and right-panel contract remain unchanged
  ---
  duration_ms: 1.556052
  type: 'test'
  ...
# Subtest: V18-19 Company Watch and Consultant Portals remain available outside Night Flight
not ok 553 - V18-19 Company Watch and Consultant Portals remain available outside Night Flight
  ---
  duration_ms: 6.754405
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/night-flight-v18-regression-guard.test.mjs:295:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Company Watch/i. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 11766-11821 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 11968-12028 ---
  duration_ms: 1.171209
  type: 'test'
  ...
# Subtest: writePreviousRoleOverview returns unavailable when the selected CV has no previous role
ok 561 - writePreviousRoleOverview returns unavailable when the selected CV has no previous role
  ---
  duration_ms: 1.296424
  type: 'test'
  ...
# Subtest: LIVE Search uses the profile-driven endpoint when a saved Union Search Plan exists
not ok 562 - LIVE Search uses the profile-driven endpoint when a saved Union Search Plan exists
  ---
  duration_ms: 2.819104
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-driven-live.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /\/api\/linkedin-profile-search/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 12120-12175 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 12276-12336 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-driven-live.contract.test.mjs:9:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: LIVE Search keeps the frozen legacy endpoint as fallback
not ok 563 - LIVE Search keeps the frozen legacy endpoint as fallback
  ---
  duration_ms: 2.100314
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-driven-live.contract.test.mjs:13:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /\/api\/linkedin-search/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 12428-12483 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 12628-12688 ---
  duration_ms: 3.542022
  type: 'test'
  ...
# Subtest: application pack becomes reviewable only when verified fact-bank evidence exists
ok 570 - application pack becomes reviewable only when verified fact-bank evidence exists
  ---
  duration_ms: 0.433724
  type: 'test'
  ...
# Subtest: live LinkedIn request uses freshness plus the active Source CV text, but not Search Profile data
not ok 571 - live LinkedIn request uses freshness plus the active Source CV text, but not Search Profile data
  ---
  duration_ms: 2.956582
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:62:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /body:JSON\.stringify\(\{freshnessDays,cvText:cvData\.cvText\}\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 12780-12835 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 12944-13004 ---
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: review helpers accept current live LinkedIn result shape
ok 572 - review helpers accept current live LinkedIn result shape
  ---
  duration_ms: 3.632101
  type: 'test'
  ...
# Subtest: merged UI restores Search Profile persistence without wiring it into LinkedIn search
not ok 573 - merged UI restores Search Profile persistence without wiring it into LinkedIn search
  ---
  duration_ms: 1.074989
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:77:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /localStorage\.getItem\('applypilot-profile'\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 13096-13151 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 13254-13314 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:79:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: merged UI keeps Application Pack and exposes the direct M4 review flow
not ok 574 - merged UI keeps Application Pack and exposes the direct M4 review flow
  ---
  duration_ms: 0.918656
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:86:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Application pack/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 13406-13461 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 13570-13630 ---
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: buildReviewChanges omits CV evidence when normalized original and updated text are the same
ok 575 - buildReviewChanges omits CV evidence when normalized original and updated text are the same
  ---
  duration_ms: 0.792107
  type: 'test'
  ...
# Subtest: M4 CV review shows a neutral empty state when AI returns no changed block
not ok 576 - M4 CV review shows a neutral empty state when AI returns no changed block
  ---
  duration_ms: 0.56969
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:110:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /No changes to review\./. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 13722-13777 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 13880-13940 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:112:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: Source CV upload entry points keep the current four-step Search Profile flow
not ok 577 - Source CV upload entry points keep the current four-step Search Profile flow
  ---
  duration_ms: 0.657896
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:117:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /className="cvButton" onClick=\{startProfile\}/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 14032-14087 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 14190-14250 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:122:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: profile status requires a complete ready Source CV and search uses that Source CV
not ok 578 - profile status requires a complete ready Source CV and search uses that Source CV
  ---
  duration_ms: 0.56992
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:131:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /const resumeLoaded=isSourceCvReady\(cvData\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 14342-14397 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 14524-14584 ---
  duration_ms: 1.010287
  type: 'test'
  ...
# Subtest: Version 2 Step 1 can read the Master CV summary from a complete saved preview
ok 582 - Version 2 Step 1 can read the Master CV summary from a complete saved preview
  ---
  duration_ms: 0.685217
  type: 'test'
  ...
# Subtest: M4 UI supersedes the legacy Summary-only review with three direct AI blocks
not ok 583 - M4 UI supersedes the legacy Summary-only review with three direct AI blocks
  ---
  duration_ms: 0.461046
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-review.test.mjs:222:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /adaptationReviewBlocks/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 14676-14731 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 14846-14930 ---
  duration_ms: 0.362471
  type: 'test'
  ...
# Subtest: Version 2 Step 1 refuses a truncated legacy preview instead of tailoring a partial Summary
ok 585 - Version 2 Step 1 refuses a truncated legacy preview instead of tailoring a partial Summary
  ---
  duration_ms: 0.179717
  type: 'test'
  ...
# Subtest: page promotes the saved Union Search Plan to LIVE while preserving legacy fallback payload
not ok 586 - page promotes the saved Union Search Plan to LIVE while preserving legacy fallback payload
  ---
  duration_ms: 10.229702
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/profile-shadow-wiring.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: 'search() block must be found'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/profile-shadow-wiring.test.mjs:9:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: parse-cv endpoint accepts resume uploads instead of being retired
ok 587 - parse-cv endpoint accepts resume uploads instead of being retired
  ---
  duration_ms: 1.465161
  type: 'test'
  ...
# Subtest: Feature 2 UI persists the complete active Source CV and supports legacy migration
not ok 588 - Feature 2 UI persists the complete active Source CV and supports legacy migration
  ---
  duration_ms: 3.812971
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/resume-upload.contract.test.mjs:27:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /\/api\/parse-cv/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 15022-15077 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 15180-15240 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/resume-upload.contract.test.mjs:28:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: Feature 2 retains the extracted Professional Summary separately from the short preview
not ok 589 - Feature 2 retains the extracted Professional Summary separately from the short preview
  ---
  duration_ms: 2.135807
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/resume-upload.contract.test.mjs:42:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /buildSourceCvRecord\(data/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 15332-15387 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 15490-15550 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/resume-upload.contract.test.mjs:45:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: right panel uses dedicated Expertise Match analysis and keeps the agreed vacancy header
not ok 590 - right panel uses dedicated Expertise Match analysis and keeps the agreed vacancy header
  ---
  duration_ms: 6.555021
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/right-panel-expertise-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /requestExpertiseMatch/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 15642-15697 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 15798-15858 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/right-panel-expertise-ui.contract.test.mjs:9:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: right panel renders the four smaller independent job-condition indicators and no Overall Match
not ok 591 - right panel renders the four smaller independent job-condition indicators and no Overall Match
  ---
  duration_ms: 0.727306
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/right-panel-expertise-ui.contract.test.mjs:20:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression />Area</. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 15950-16005 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 16108-16168 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/right-panel-expertise-ui.contract.test.mjs:21:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: right panel preserves Application Pack and gives Expertise Match stronger visual hierarchy
not ok 592 - right panel preserves Application Pack and gives Expertise Match stronger visual hierarchy
  ---
  duration_ms: 0.691919
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/right-panel-expertise-ui.contract.test.mjs:28:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Application pack/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 16260-16315 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 16424-16484 ---
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: job conditions never alter Expertise Match
ok 593 - job conditions never alter Expertise Match
  ---
  duration_ms: 18.986269
  type: 'test'
  ...
# Subtest: page wires aggregated API audit rows into a closed-by-default Search Audit component
not ok 594 - page wires aggregated API audit rows into a closed-by-default Search Audit component
  ---
  duration_ms: 8.78592
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-audit-ui.contract.test.mjs:8:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /import SearchAudit from '\.\/components\/search-audit\.js'/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 16576-16631 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 16802-16862 ---
  duration_ms: 2.559326
  type: 'test'
  ...
# Subtest: blank exclusions use zero AI calls
ok 606 - blank exclusions use zero AI calls
  ---
  duration_ms: 0.309801
  type: 'test'
  ...
# Subtest: custom exclusions are compiled only on Save profile and then passed to profile-driven LIVE Search
not ok 607 - custom exclusions are compiled only on Save profile and then passed to profile-driven LIVE Search
  ---
  duration_ms: 6.777408
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-exclusions-save-ui.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /requestSearchProfileExclusions/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 16954-17009 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 17110-17170 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/search-profile-exclusions-save-ui.test.mjs:7:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: new unsaved profiles do not inherit another user’s exclusions
not ok 608 - new unsaved profiles do not inherit another user’s exclusions
  ---
  duration_ms: 3.531232
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-exclusions-save-ui.test.mjs:23:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /EMPTY_SEARCH_PROFILE=\{\.\.\.DEFAULT_PROFILE,exclusions:''\}/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 17262-17317 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 17474-17534 ---
  duration_ms: 0.334278
  type: 'test'
  ...
# Subtest: library fingerprint is stable by slot and changes only with CV membership/sourceVersion or builder version
ok 617 - library fingerprint is stable by slot and changes only with CV membership/sourceVersion or builder version
  ---
  duration_ms: 0.505219
  type: 'test'
  ...
# Subtest: Search Profile wizard has four steps and no salary screen or salary scoring label
not ok 618 - Search Profile wizard has four steps and no salary screen or salary scoring label
  ---
  duration_ms: 3.595021
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-no-salary-ui.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /profileStep\/4\*100/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 17626-17681 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 17806-17866 ---
  duration_ms: 0.26634
  type: 'test'
  ...
# Subtest: Search Profile role editor preserves spaces while typing and normalizes only on blur
ok 622 - Search Profile role editor preserves spaces while typing and normalizes only on blur
  ---
  duration_ms: 1.593233
  type: 'test'
  ...
# Subtest: Search Profile step 2 combines all ready CVs while legacy Search keeps CV 1
not ok 623 - Search Profile step 2 combines all ready CVs while legacy Search keeps CV 1
  ---
  duration_ms: 3.67389
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-step2-ui-contract.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /requestSearchProfileRoles/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 17958-18013 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 18114-18174 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/search-profile-step2-ui-contract.test.mjs:9:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: Search Profile four-step flow keeps location and work-model normalization outside the current Step 3 exclusions screen
not ok 624 - Search Profile four-step flow keeps location and work-model normalization outside the current Step 3 exclusions screen
  ---
  duration_ms: 3.643673
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/search-profile-step3-ui-contract.test.mjs:5:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /normalizeSearchPreferences/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 18266-18321 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 18500-18560 ---
  duration_ms: 1.004677
  type: 'test'
  ...
# Subtest: LinkedIn API requires cvText and passes it into search evaluation
ok 637 - LinkedIn API requires cvText and passes it into search evaluation
  ---
  duration_ms: 0.190778
  type: 'test'
  ...
# Subtest: Search LinkedIn is blocked in the browser when Source CV is not ready
not ok 638 - Search LinkedIn is blocked in the browser when Source CV is not ready
  ---
  duration_ms: 1.818966
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/source-cv-search.contract.test.mjs:20:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /if\(!resumeLoaded\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 18652-18707 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 18810-18870 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/source-cv-search.contract.test.mjs:21:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: browser sends the active uploaded Source CV text to LinkedIn evaluation
not ok 639 - browser sends the active uploaded Source CV text to LinkedIn evaluation
  ---
  duration_ms: 1.115454
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/source-cv-search.contract.test.mjs:26:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /JSON\.stringify\(\{freshnessDays,cvText:cvData\.cvText\}\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 18962-19017 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 19216-19276 ---
  duration_ms: 0.971765
  type: 'test'
  ...
# Subtest: tailor-cv route keeps GET retired and does not log CV or JD payloads
ok 655 - tailor-cv route keeps GET retired and does not log CV or JD payloads
  ---
  duration_ms: 0.24504
  type: 'test'
  ...
# Subtest: CV Library uses Word DOCX for source-file adaptation
not ok 656 - CV Library uses Word DOCX for source-file adaptation
  ---
  duration_ms: 6.161452
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/tailored-docx-export.contract.test.mjs:10:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Please upload a Word DOCX file\./. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 19368-19423 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 19524-19584 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/tailored-docx-export.contract.test.mjs:13:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: source DOCX is kept only in session memory and can be re-uploaded
not ok 657 - source DOCX is kept only in session memory and can be re-uploaded
  ---
  duration_ms: 0.696037
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/tailored-docx-export.contract.test.mjs:16:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /const \[sourceDocxFiles,setSourceDocxFiles\]=useState\(\{\}\)/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 19676-19731 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 19834-19894 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/tailored-docx-export.contract.test.mjs:17:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: download is enabled only after review decisions and sends accepted blocks only
not ok 658 - download is enabled only after review decisions and sends accepted blocks only
  ---
  duration_ms: 0.613092
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/tailored-docx-export.contract.test.mjs:22:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /reviewedCount===reviewChanges\.length/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 19986-20041 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 20282-20342 ---
  duration_ms: 0.406112
  type: 'test'
  ...
# Subtest: confirm preview exposes plan counts, directions, tiers and provenance labels
ok 681 - confirm preview exposes plan counts, directions, tiers and provenance labels
  ---
  duration_ms: 6.770626
  type: 'test'
  ...
# Subtest: page wires the edited draft into a saved Union Search Plan preview
not ok 682 - page wires the edited draft into a saved Union Search Plan preview
  ---
  duration_ms: 6.777408
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/union-search-plan-wiring.test.mjs:7:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /import \{buildUnionSearchPlan,UNION_SEARCH_PLAN_VERSION\} from '\.\/lib\/union-search-plan\.js'/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 20434-20489 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 20588-20650 ---
      return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/union-search-plan-wiring.test.mjs:9:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: saved Union Search Plan drives profile LIVE while legacy fallback stays isolated
not ok 683 - saved Union Search Plan drives profile LIVE while legacy fallback stays isolated
  ---
  duration_ms: 3.257007
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/union-search-plan-wiring.test.mjs:22:1'
  failureType: 'testCodeFailure'
  error: 'search() block must be found'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/union-search-plan-wiring.test.mjs:26:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: compiles edited roles in primary-then-adjacent order and primary wins duplicates
ok 684 - compiles edited roles in primary-then-adjacent order and primary wins duplicates
  ---
  duration_ms: 3.350292
  type: 'test'
  ...
# Subtest: removed raw CV-proposed roles do not survive the edited plan
ok 685 - removed raw CV-proposed roles do not survive the edited plan
  ---
  duration_ms: 1.489487
  type: 'test'
  ...
# Subtest: manual roles are preserved without invented CV provenance
ok 686 - manual roles are preserved without invented CV provenance
  ---
  duration_ms: 0.496903
  type: 'test'
  ...
# Subtest: CV-supported roles preserve all supporting CV ids, slots and raw support kinds
ok 687 - CV-supported roles preserve all supporting CV ids, slots and raw support kinds
  ---
  duration_ms: 0.555563
  type: 'test'
  ...
# Subtest: user retiering changes plan tier while preserving original CV support classification
ok 688 - user retiering changes plan tier while preserving original CV support classification
  ---
  duration_ms: 0.549863
  type: 'test'
  ...
# Subtest: deduplicates case and whitespace while preserving first visible spelling
ok 689 - deduplicates case and whitespace while preserving first visible spelling
  ---
  duration_ms: 0.363743
  type: 'test'
  ...
--- lines 20720-20780 ---
  duration_ms: 0.297549
  type: 'test'
  ...
# Subtest: main condition grid hides Salary and uses three columns
ok 702 - main condition grid hides Salary and uses three columns
  ---
  duration_ms: 1.328876
  type: 'test'
  ...
# Subtest: CV Update Review is driven by explicit selected-CV adaptation rather than a standalone JD cache pretest
not ok 703 - CV Update Review is driven by explicit selected-CV adaptation rather than a standalone JD cache pretest
  ---
  duration_ms: 2.544058
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:7:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /requestCvAdaptation/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 20872-20927 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 21028-21088 ---
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:8:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: CV Update Review keeps a focused ORIGINAL UPDATED review without the obsolete Truth Guard panel
not ok 704 - CV Update Review keeps a focused ORIGINAL UPDATED review without the obsolete Truth Guard panel
  ---
  duration_ms: 0.757953
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:16:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /CV UPDATE REVIEW/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 21180-21235 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 21338-21398 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:17:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: review cards show Original, editable Updated, Why changed and independent decisions
not ok 705 - review cards show Original, editable Updated, Why changed and independent decisions
  ---
  duration_ms: 0.715633
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:24:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /ORIGINAL/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 21490-21545 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
--- lines 21648-21708 ---
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:25:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: legacy JD evidence accordions are absent because M4 reviews the three adapted CV blocks, not the JD pretest
not ok 706 - legacy JD evidence accordions are absent because M4 reviews the three adapted CV blocks, not the JD pretest
  ---
  duration_ms: 2.794861
  type: 'test'
  location: '/home/runner/work/applypilot/applypilot/app/lib/v16-cv-review.contract.test.mjs:35:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Professional Summary · Latest role overview · Previous role overview/. Input:
    
    "'use client'\n" +
      "import React,{useEffect,useState} from 'react'\n" +
      "import MainSearchBase from './main-search-base.js'\n" +
      "import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'\n" +
      "import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'\n" +
      "import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'\n" +
      "import {visibleElementKey} from './lib/night-flight-react-key.js'\n" +
      '\n' +
      "const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}\n" +
      '\n' +
      'function elementText(node){\n' +
      "  if(node==null||typeof node==='boolean') return ''\n" +
      "  if(typeof node==='string'||typeof node==='number') return String(node)\n" +
      "  if(Array.isArray(node)) return node.map(elementText).join(' ')\n" +
      "  if(!React.isValidElement(node)) return ''\n" +
      '  return elementText(node.props?.children)\n' +
      '}\n' +
      '\n' +
      'function sourceFromJobCard(node){\n' +
      '  const text=elementText(node).toLowerCase()\n' +
      "  if(text.includes('linkedin')) return 'linkedin'\n" +
      "  if(text.includes('jobindex')) return 'jobindex'\n" +
      "  if(text.includes('jobnet')) return 'jobnet'\n" +
      "  return ''\n" +
      '}\n' +
      '\n' +
      'function enrichStatusSelect(jobWrap,index){\n' +
      "  const sourceJobId=String(jobWrap.key??'').trim()\n" +
      '  if(!sourceJobId) return jobWrap\n' +
      '  const children=React.Children.toArray(jobWrap.props.children)\n' +
      "  const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))\n" +
      '  if(selectIndex<0) return jobWrap\n' +
      '  const select=children[selectIndex]\n' +
      "  const card=children.find(child=>React.isValidElement(child)&&child.type==='button')\n" +
      '  const source=sourceFromJobCard(card)\n' +
      '  const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)\n' +
      '  const job=enriched[0]?.job||{source,sourceJobId}\n' +
      "  const manualStatus=String(select.props.value||'')\n" +
      '  const displayStatus=resolveJobStatus({manualStatus,job})\n' +
      '  if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap\n' +
      "  const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')\n" +
      '  children[selectIndex]=React.cloneElement(select,{\n' +
--- lines 21800-21855 ---
      '      }catch{}\n' +
      '    })()\n' +
      '    return()=>{active=false}\n' +
      '  },[])\n' +
      '  const tree=MainSearchBase()\n' +
      '  const activeNightFlightJob=findActiveJobIdentity(tree)\n' +
      '  const cachedNightFlightAnalysis=resolveNightFlightExpertise({job:activeNightFlightJob,index:nightFlightIndex})\n' +
      '  return transformMainSearchTree(tree,nightFlightIndex,cachedNightFlightAnalysis)\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    'use client'
    import React,{useEffect,useState} from 'react'
    import MainSearchBase from './main-search-base.js'
    import {NIGHT_FLIGHT_STATUS,resolveJobStatus} from './lib/job-statuses.js'
    import {enrichSearchJobsWithNightFlight} from './lib/night-flight-main-search-bridge.js'
    import {resolveNightFlightExpertise} from './lib/night-flight-expertise-reuse.js'
    import {visibleElementKey} from './lib/night-flight-react-key.js'
    
    const NIGHT_FLIGHT_STATUS_STYLE={borderColor:'rgba(167,139,250,.72)',color:'#ddd6fe',background:'rgba(109,40,217,.22)'}
    
    function elementText(node){
      if(node==null||typeof node==='boolean') return ''
      if(typeof node==='string'||typeof node==='number') return String(node)
      if(Array.isArray(node)) return node.map(elementText).join(' ')
      if(!React.isValidElement(node)) return ''
      return elementText(node.props?.children)
    }
    
    function sourceFromJobCard(node){
      const text=elementText(node).toLowerCase()
      if(text.includes('linkedin')) return 'linkedin'
      if(text.includes('jobindex')) return 'jobindex'
      if(text.includes('jobnet')) return 'jobnet'
      return ''
    }
    
    function enrichStatusSelect(jobWrap,index){
      const sourceJobId=String(jobWrap.key??'').trim()
      if(!sourceJobId) return jobWrap
      const children=React.Children.toArray(jobWrap.props.children)
      const selectIndex=children.findIndex(child=>React.isValidElement(child)&&child.type==='select'&&String(child.props?.className||'').includes('jobStatusSelect'))
      if(selectIndex<0) return jobWrap
      const select=children[selectIndex]
      const card=children.find(child=>React.isValidElement(child)&&child.type==='button')
      const source=sourceFromJobCard(card)
      const enriched=enrichSearchJobsWithNightFlight([{job:{source,sourceJobId}}],index)
      const job=enriched[0]?.job||{source,sourceJobId}
      const manualStatus=String(select.props.value||'')
      const displayStatus=resolveJobStatus({manualStatus,job})
      if(displayStatus!==NIGHT_FLIGHT_STATUS) return jobWrap
      const nightFlightOption=React.createElement('option',{value:NIGHT_FLIGHT_STATUS,key:'night-flight-derived'},'NIGHT FLIGHT')
      children[selectIndex]=React.cloneElement(select,{
exit_code=1
